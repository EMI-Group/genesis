# LiveView Pages

## Intent
Phoenix LiveView modules and templates for the EvoDash interactive UI — real-time task management, agent inspection, runtime configuration, and user help.

## Routing Table
- `components/` → LiveComponents (`use EvoDashWeb, :live_component`) — `NodeSelectorComponent` for the SSH Remote Development node selector
- `agents_live/` → Support modules extracted from `AgentsLive`: `ToolCallDisplay` (tool-call rendering contract — structured subagent/shell rows + one-line others, constrained height)
- `projects_live/` → Support modules extracted from `ProjectsLive`: `StatePersistence`, `Project`, `Assigns`. Note: `projects_live.ex` is ~1846 lines — a future split of more event handlers into this support directory may be warranted.
- `settings_live/` → Support modules extracted from `SettingsLive`: `ModelProfileHelpers` (pure data-transformation for `[[llm.models]]` CRUD), `ConfigIO` (config loading, runtime updates, atom whitelists)
- `system_live/` → Support modules extracted from `SystemLive`: `Content` (static guides/references), `Status` (health-check status helpers)
- `tasks_live/` → Support modules extracted from `TasksLive`: `DirtyTracker` (pure dirty-check state machine gating the remote-node `:remote_poll` full-page reloads)

## API Surface

| Module | Route | Summary |
|--------|-------|---------|
| `ProjectsLive` | `GET /` | Projects page — project-based task dashboard with genesis/evolve forms, auto-mode detection, real-time task cards, and project tabs. Delegates to `TaskRegistry`, `ProjectComponents`, and `TaskFormComponents`. Top bar shows a GitHub button (owner/repo) when the active project's upstream resolves; opens the GitHub issues modal with per-issue Fix (starts an `:evolve` task). |
| `WelcomeLive` | `GET /welcome` | First-run onboarding page — stepwise quick LLM setup (provider → variant → model → credentials); `skip`/`get_started` redirect to `/welcome/complete`. |
| `WelcomeCompleteLive` | `GET /welcome/complete` | Onboarding completion page — sole focus is the example task (high-level-prompt explanation + copyable demo objective) and a "Go to Dashboard" button that completes onboarding (`EvoGit.Config.VersionState.complete_onboarding/0`, `Code.ensure_loaded?/1`-guarded) and navigates to `/`. Rendered when WelcomeLive's `skip`/`get_started` redirect here. |
| `ReviewLive` | `GET /review/:task_id` | Code review page — diff viewer with client-side syntax highlighting (highlight.js in the browser), commit list, agent summary, and action buttons (Merge, Reject, Continue, Create GitHub PR, Extract Skills). The Extract Skills action opens a modal for an optional user note, then starts an `:extract_skills` task via `TaskRegistry` that spawns a `SkillExtractor` agent to distill PR knowledge into `.agents/skills/` files. |
| `AgentsLive` | `GET /agents` | Recursive agent tree inspector with selectable agent detail panel. Reads from ETS tables, auto-refreshes every 500ms. Uses `AgentsComponents.agent_tree/1`. |
| `TasksLive` | `GET /tasks` | Task history list across all projects. |
| `SettingsLive` | `GET /settings` | Runtime scheduler configuration (concurrency, retries, depth, LLM model). Shows config status warnings. Updates via `AgentScheduler.update_config/1`. Auto-refreshes every 2s. |
| `SystemLive` | `GET /system` | System page: scheduler controls (pause/resume), system controls (restart/stop the Erlang VM), system self-check (grid of check-term cells + merged health banner), plus usage guides and references (example config, CLI usage, FAQ, credentials). |

### Example-task teaching content (onboarding)

- `EvoDashWeb.ExampleTask` (`example_task.ex`) — shared module holding the demo objective text (`example_objective/0`, verbatim string, NOT gettext-wrapped). Single source of truth for the "example task" teaching sections on the onboarding surfaces: the onboarding completion page (`WelcomeCompleteLive` at `/welcome/complete` — `welcome_live.ex` does not render the example card) and the projects-page no-project empty-state panel (`projects_live.ex`, gated by `is_nil(@active_project)` — disappears once a project is open; contains a collapsible `<details>` panel with Copy + prefill buttons).
- Copy-to-clipboard reuses the global `ClipboardCopy` hook (`data-content` + `phx-hook="ClipboardCopy"`, pushes a `"copied"` event); the pages that render example content each define their own `handle_event("copied", ...)` flash handler (pattern: `welcome_complete_live.ex`, `review_live.ex:312`).
- Dashboard prefill ("Use this example") uses a hidden `<textarea id="example-task-objective" class="hidden" readonly>` RCDATA holder + inline onclick JS: `p.value = h.value; p.dispatchEvent(new Event('input', {bubbles: true}))`. The dispatched input drives the AdaptiveInput hook — autogrow AND the client-side layout switch (the example exceeds the 600-grapheme threshold, so the hook flips `data-layout` to expanded). The hook also re-asserts the layout after any server re-seed of `data-layout` from the possibly-stale `@task_prompt` (MutationObserver on `.input-layout`, e.g. after toggling mode/model). No server event is involved — there is no `phx-change` on the prompt textarea; `@task_prompt` is only updated by `restore_state` / `task_submit`. `phx-update="ignore"` on the prompt textarea keeps the DOM value stable. Never set this via innerHTML — the example text contains literal `<link>`/`<script>` strings; always use the `.value` property.

### ReviewLive merge-target branch selector

The review page (`ReviewLive`, `GET /review/:task_id`) has a merge-target branch selector: the merge action merges into a user-selectable branch instead of always the repo's default. `load_task_data/2` loads `merge_targets` and `default_merge_target` (assigns, defaulted to `[]`/`nil` in `mount/3`) via `EvoGit.Review.list_branches/1` and `EvoGit.Review.default_merge_target/1` when `repo_path` exists (plain `case` on tuple returns, no try/rescue; degrades gracefully to `[]`/`nil`). The `merge` event handler reads `params["target_branch"]` (trimmed; non-member targets fall back to the default when a known list was loaded) and calls `EvoGit.Review.merge_branch/3`, falling back to `merge_branch/2` (default-resolving path) when no target is given. The success flash mentions the effective target branch. UI lives in `ReviewComponents.Actions.action_buttons/1` (see `components/review_components/CONTEXT.md`).

### LiveComponents (`./components/`)
- **`NodeSelectorComponent`** (`components/node_selector_component.ex`) — `EvoDashWeb.NodeSelectorComponent`, a `use EvoDashWeb, :live_component` LiveComponent rendered in the navbar (next to the brand logo, via `Layouts.app/1`). Shows the current node with a status dot + a dropdown (Local / saved targets / "Manage Connections...") and a full **connection manager modal** (add/edit/delete targets, bootstrap/connect/disconnect, status badges). Manages its own state; selects nodes by sending `{:node_selected, id}` to the parent LiveView. Subscribes to the `"remote_connections"` PubSub topic via the parent LiveView's subscription.

### Templates
- **`agents_live.html.heex`** — Companion template for `AgentsLive` with sidebar tree + detail panel layout.

### Notes for Agents — Agents page layout & legend tooltip (`agents_live.html.heex` + app.css)

The Agents page uses a fixed-viewport layout (`xl+` only): `.agents-page-layout` (grid, `overflow: hidden`), `.agents-left-col` (column flex, `overflow: hidden`), `.agents-tree-panel` (card, `flex: 1 1 0`), with the tree content scrolling inside `.agents-tree-panel .card-body` (`overflow-y: auto`) and the legend (`.agents-legend`) as a SIBLING of card-body at the panel's bottom. On mobile (<xl) it's normal flow under `#main-scroll` (`overflow: auto`) inside `#app-layout` (`overflow: hidden`).

**Legend tooltips use the `LegendTooltip` JS hook (`assets/js/hooks/legend_tooltip.js`) — NOT DaisyUI `.tooltip`.** DaisyUI's tooltip pseudo-element is absolutely positioned and horizontally CENTERED on the chip, so the long `data-tip` texts (max-width 20rem) on the right-aligned legend chips extend past the panel's right edge and get clipped by the page-level overflow containers (`#app-layout`, `#main-scroll`, `.agents-page-layout`, `.agents-left-col`). CSS-only anchoring can't fix this (the panel is ~480px wide at xl, so any single anchor overflows on some chip). The hook renders the tip as a `position: fixed` element appended to **`document.body`** — mandatory because `.agents-page-layout`'s `animate-fade-in-up` uses `animation-fill-mode: both`, so `transform: translateY(0)` PERSISTS on the layout and would trap even `position: fixed` descendants inside its `overflow: hidden`. The chips carry `data-tip` (gettext) + `phx-hook="LegendTooltip"` + unique ids (LiveView requires `id` for phx-hook); the `tooltip` class was REMOVED so DaisyUI's clipped tooltip doesn't double-render. The "Send Message" button tooltip (`tooltip` class, short text near the card edge) stays inside its container and is NOT affected — do not convert it.

### Notes for Agents — Tool-call display contract (`agents_live/tool_call_display.ex`)

The Agents page chat history viewer renders tool-call messages (both the inline detail panel and the full-message modal) through `EvoDashWeb.AgentsLive.ToolCallDisplay.display/1` (aliased as `ToolCallDisplay` in `agents_live.ex`). The module is a pure rendering contract — see `agents_live/CONTEXT.md` for the full API. Durable facts:

- **Return contract**: `display/1` returns `%{label: String.t(), kind: :structured, rows: [{String.t(), String.t()}]}` (subagent + shell calls) or `%{label: String.t(), kind: :inline, summary: String.t()}` (all other calls). The template branches on `display.kind == :structured`.
- **Structured calls**: `subagent_*`-prefixed names (core `subagent_tool_name/0` family — `subagent_manager`, `subagent_executor`, `subagent_investigator`, `subagent_task_scheduler`, `subagent_genesis_planner`, `subagent_architect`, `subagent_context_extractor`; prefix detection covers future variants) show rows `Path` (first of `"path"`/`"file_path"`) + `Objective` (truncated to 200 chars with `…`); `run_bash`/`run_powershell` shell calls show a single `Command` row (truncated to 200 chars, `"command"` from decoded args with raw-args-collapsed fallback). Raw arguments JSON is NEVER dumped for these.
- **One-line calls**: every other tool (read_file, write_file, edit_file, rg, glob, list_dir, make_dir, create_files, read_context, write_context, search_context, skill_*, complete_task, unknown, …) renders a single-line summary: path-like field (`"path"`/`"file_path"`/`"dir_path"`/`"paths"`, list values joined with `" "`) if present, else compact single-line `Jason.encode/1` of the decoded args, else whitespace-collapsed raw arguments, else `""` — truncated to 160 chars with `…`.
- **Height-constrained everywhere**: structured blocks render inside `max-h-12` (inline panel, ~2 lines) / `max-h-60` (modal) `overflow-y-auto` containers; structured rows render flat inside the parent container (no separate box/background layer) — only the parent "Tool Calls" box keeps its background. Inline summaries are one line (`whitespace-nowrap overflow-hidden text-ellipsis`). No new CSS in `app.css` — stock Tailwind 4 / DaisyUI utilities only.
- **Label/rows layout contract (both render sites)**: the tool-call label ("Shell call" / "subagent_manager" / etc.) is INLINE beside the rows — a flex row (`flex items-start gap-2`) with the label as a `shrink-0` span and the rows container as `min-w-0 flex-1`, so the label and the first structured row key ("Command" / "Path") sit on the SAME line, top-aligned. This applies in BOTH the inline detail panel and the full-message modal (the modal's label mirrors the inline panel's flex row). The key column is `w-20 shrink-0` with 10px uppercase text; the rows containers carry NO padding offsets (`p-1.5`/`pt-0.5`/`p-2` are absent). The inline-summary else-branch is a `min-w-0` span (not a div) in both sites.
- **`EvoDashWeb.Helpers.tool_call_display/1` is not used by the Agents page** — it remains a public, test-pinned helper in `helpers.ex` (do not remove it). `ToolCallDisplay` reuses Helpers' defensive extractors (`tool_call_name/1`, `tool_call_arguments/1`, `tool_call_is_shell?/1`) and adds its own non-crashing `Jason` handling via pattern-matched `case` — no `try/rescue` (project policy).
- **New gettext strings** (`Path`, `Objective`, `Command`) will not appear in the POT/PO until release-time extraction — expected, do NOT run `mix gettext.extract`/`merge`/`translate` during development.

### Node-Aware Integration (SSH Remote Development, Phase 2)
All LiveViews register the `EvoDashWeb.LiveHooks.NodeAware` on-mount hook (via the `live_view/0` macro in `evo_dash_web.ex`). As part of the node-aware pattern, each LiveView:
- Calls `assign_node/2` in `handle_params/3` (reads the `?node=<id>` query param, resolving it to a saved+connected target or falling back to local).
- Passes `current_node_id` / `current_node_name` (and `remote_targets` / `connection_statuses`) to the `Layouts.app/1` call.
- Handles `{:node_selected, _}` (delegated to `NodeAware.handle_node_selected/2`) and `{:remote_connection_status, _, _}` messages (delegated to `NodeAware.handle_connection_status/2`).

### Shared Conventions
- All LiveViews use `use EvoDashWeb, :live_view` and import `CoreComponents` and `Layouts`.
- All pages use `EvoDashWeb.Layouts.app/1` layout with `current_page` for nav highlighting.
- Styled with DaisyUI/Tailwind CSS; business logic delegated to context modules.

### UI Patterns (modals, forms, events)
These patterns are used consistently across the LiveViews. Follow them when adding new interactive features.

- **Modal pattern (rendered in the LiveView's own template/component):** A modal is conditionally rendered with `<%= if @some_assign do %>` wrapping a `<div class="modal modal-open bg-black/50" id="...">` containing a `<div class="modal-box ...">`. The open/close state is driven by a `nil`/value socket assign. Opening: a `phx-click="open_modal"` handler sets the assign (optionally carrying an `phx-value-id`). Closing: a `phx-click="close_modal"` handler resets the assign to `nil`. The backdrop `<div class="modal-backdrop" phx-click="close_modal">` also closes. See `AgentsLive`'s `view_full_message`/`close_message_modal` and `view_full_objective`/`close_objective_modal` (in `agents_live.html.heex` + `agents_live.ex`), and `EvoDashWeb.Helpers`'s reusable modal component.
- **Inline form submission (inline editor pattern):** Instead of a full modal, list editors use a `<form phx-submit="save_..." >` with hidden `<input type="hidden" name="id" value=...>` + visible inputs, submitted by a `<button type="submit">`. The edit state is toggled by an `@editing_...` assign. See `SettingsComponents.ModelProfilesEditor.model_profile_edit_form/1` (`phx-submit="save_model_profile"`).
- **Event naming:** Events use `phx-click` (buttons) and `phx-submit` (forms). Values passed via `phx-value-<key>="<value>"` (received in the handler's params map as string keys). IDs that are integers arrive as strings — `String.to_integer/1` is used to convert (e.g. `select_agent` → `%{"id" => id}`).
- **`ModalHelpers` (`live/modal_helpers.ex`):** A `__using__` macro injecting shared modal event handlers (`view_full_result/2`, `close_result_modal/1`, `view_full_options/2`, `close_options_modal/1`) into a host LiveView. **Only `TasksLive` uses `EvoDashWeb.ModalHelpers`** (ProjectsLive does not). The injected helpers read `socket.assigns.tasks`, which on TasksLive is the full TaskInfo page list.

- **Task cancellation UX (graceful cancel + force kill)** (TasksLive): TWO two-step server-side confirmation-modal flows follow the SystemLive warning-modal pattern (fixed overlay + backdrop with `phx-click` close + centered panel with warning icon/text + dismiss/confirm buttons; NO custom JS — the assigns `@confirm_cancel_task_id` / `@confirm_force_kill_task_id` drive visibility):
  - **Graceful cancel** (card's inline Cancel button, `phx-click="open_cancel_modal"`): `open_cancel_modal` → assign `:confirm_cancel_task_id`; `close_cancel_modal` → nil; `confirm_cancel_task` → `EvoDash.NodeContext.cancel_task(current_node, task_id)` (GRACEFUL — agents save + exit, result preserved; `:pending` → immediate `:cancelled`); `:ok` → collapse expanded card (`MapSet.delete(expanded_task_ids, task_id)`) + `reload_current_page/1`; `{:error, reason}` → flash `gettext("Failed to cancel task: %{reason}", reason: inspect(reason))`. Modal: title `gettext("Cancel Task?")`, confirm `gettext("Cancel Task")` (`btn-warning`), dismiss `gettext("Keep Running")` (distinct label so there are not two "Cancel" buttons).
  - **Force kill** (card's three-dot dropdown, `phx-click="open_force_kill_modal"`): `open_force_kill_modal` → assign `:confirm_force_kill_task_id`; `close_force_kill_modal` → nil; `confirm_force_kill_task` → `EvoDash.NodeContext.force_kill_task(current_node, task_id)` (BRUTAL — kills all agents, result nil'd; escalation from `:cancelling`); same `:ok` collapse+reload / error-flash handling. Modal: title `gettext("Force Kill Task?")`, confirm `gettext("Force Kill")` (`btn-error`).
  - **Modal state lifecycle**: both assigns seeded `nil` in `mount/3`, MUTUALLY EXCLUSIVE (opening one modal clears the other), and cleared on node switch in `handle_params/3` (NOT on same-node pagination/filter push_patches — node change detected by comparing the `:tasks_node_loaded` context tuple before/after `assign_node/2`).
  - **Status filter**: the dropdown includes `gettext("Cancelling")` (`value="cancelling"`). The filter is a pure SQL string comparison (`build_filters_from_assigns` → `EvoGit.Store.Queries.build_where`, status column string `"cancelling"`), so `:cancelling` round-trips — no evo_dash-side atom whitelist / `String.to_existing_atom` path exists for the status filter.
- **LiveComponent event routing:** Inside a `use EvoDashWeb, :live_component`, events use `JS.push("event", target: @myself, value: %{...})` (targets the component itself) or plain `phx-click` (targets the parent LiveView). See `NodeSelectorComponent` (`select_node` targets `@myself`, then `send(self(), {:node_selected, id})` to the parent).
- **Task-form layout — server-seeded + client-driven (no per-keystroke round trip):** The task form's layout (compact vs expanded) is seeded server-side at render from the prompt length via `TaskFormComponents.layout_for/1` (threshold > 600 graphemes OR > 16 lines) and updated client-side while typing by the AdaptiveInput JS hook (mirrors the same thresholds). The hook also re-asserts the client-computed layout whenever the server re-seeds `data-layout` from its possibly-stale `@task_prompt` — a MutationObserver on `.input-layout` (attributeFilter: ['data-layout']) re-runs the computation on any server re-render (e.g. toggling mode/model), so the layout can never snap back to compact while a long prompt remains in the box; it converges immediately (the hook only writes the attribute when the computed value differs) with zero network events. `@task_prompt` is NEVER updated per keystroke — the textarea has no `phx-change`; it is set only by `restore_state` and `task_submit`. Prompt draft persistence is purely client-side (the StatePersistence input watcher in app.js). After a successful `task_submit`, `@task_prompt` is reset to `""` by `assign_form_defaults/1` and the handler pushes a `"clear_prompt"` event; the AdaptiveInput hook empties the visible textarea (morphdom skips it under `phx-update="ignore"`), re-asserts autogrow + layout, and clears the draft prompt from the persisted `dashboard_state` sessionStorage blob — the submitted prompt does not survive reloads.

### Design Notes — Dashboard Top Bar & Command Palette Keyboard

- **Palette keyboard binding must live on the focused element:** LiveView `phx-keydown` bindings do NOT bubble to ancestor elements — the handler only fires when the focused element itself carries the binding. The command palette's `phx-keydown="palette_keydown"` must therefore be on the search input (project_components.ex), NOT on the overlay div. Since `handle_palette_key/3` (`projects_live.ex`) only acts on special keys — `"Escape"` (closes, any mode), `"ArrowDown"`/`"ArrowUp"` (menu index nav), `"Enter"` (menu selection) — and has a catch-all clause returning the socket unchanged for everything else, printable characters typed into the search input are no-ops server-side: typing + `palette_search` filtering (a separate `phx-change` event) coexist without interference. Event path: input `phx-keydown` → `handle_event("palette_keydown", %{"key" => key}, socket)` → `handle_palette_key/3` (clauses at `projects_live.ex:1381-1447`, catch-all `:1447`). Palette close event name: `close_project_palette` (`handle_event` at `projects_live.ex:649`; the backdrop uses `phx-click="close_project_palette"` and click-away uses the same name).
- **Top bar spacing (`ProjectsLive.top_bar/1`):** container `dashboard-topbar` uses `gap-3 px-4 py-3`; the Configure button is `btn btn-md btn-ghost gap-2`; dropdown section headers ("Task Options" / "Project Settings") use `text-xs`. The omnibox trigger (left side) lives in project_components.ex. All strings remain gettext-wrapped — no .pot/.po edits.
- **No-project hint + page dim:** when no project is open (`is_nil(@active_project)`) and the hint is not dismissed, the LOCAL top bar (only — gated `!@remote and !@hide_palette`) carries `data-no-project="true"`, renders a clickable `.no-project-hint` pill ("Open or create a project", `phx-click="open_project_palette"` + dismiss X with `phx-click="dismiss_no_project_hint"` + `phx-stop-propagation`), and the page body renders `<div class="no-project-overlay" aria-hidden="true">` (fixed, `pointer-events: none`, z-20 — pure visual dim below the top bar's z-30). Events/assigns: `dismiss_no_project_hint` sets `@no_project_hint_dismissed` (mount default `false`; reset to `false` in `activate_project/2` so the hint returns for a future no-project session). The hint/overlay naturally disappear once a project activates. CSS lives in `assets/css/app.css` ("No-Project Hint" section: `.no-project-overlay`, `.dashboard-topbar[data-no-project="true"]` taller padding + box-shadow pulse — NO backdrop-filter/filter/transform on the topbar, containing-block trap, see assets/CONTEXT.md Known Issue (ii); `.no-project-hint`). New gettext strings ("Open or create a project", "Dismiss", "Project path") land in the .pot at release time — do NOT run extract.
- **New-project form is a SINGLE full-path input:** `palette_new_project/1` submits `phx-submit="create_project"` with one `name="path"` field (`id="new-project-path-input"`, `phx-change="new_project_path_input"`, debounce 150, `list="new-project-path-suggestions"`). Backend: `ProjectFlow.create_project/2` takes `%{"path" => path}` and `File.mkdir_p`s missing dirs (returns plain `:ok`!). The open-by-path input placeholder is neutral ("Project path") — no example paths anywhere (`platform_placeholder/1` returns the neutral string for all platforms).
- **Node-aware path handling (cross-OS remote):** remote project paths are normalized by `ProjectFlow.normalize_remote_project_path/2` (used ONLY by `activate_remote_project/2`): POSIX- and Windows-absolute paths pass through VERBATIM with no local `Path.expand` (the dashboard's host OS must not rewrite remote paths), and tilde inputs expand on the remote node via RPC through the injectable `:evo_dash, :remote_path_expand_runner` seam. Remote recents filtering uses the shared `ProjectFlow.absolute_path_for_node?/2` predicate (local = `EvoGit.Platform.absolute_path?/1` unchanged; remote = POSIX- or Windows-absolute accepted) at the three remote `:recent_projects` sites in `projects_live.ex` (`handle_params/3` remote branch, the `{:recent_projects_updated}` handler, `activate_remote_palette_project/2`) and in `Project.path_suggestions/3`'s recents filter; `add_foreign_repo` validates remote paths with the same predicate and then stores them RAW via `ProjectFlow.build_foreign_repo/4` (remote contexts construct `%ForeignRepo{}` without local `Path.expand`; local contexts keep `ForeignRepo.new/3`'s expansion — see `projects_live/CONTEXT.md` "Foreign-repo construction — node-aware raw storage"). Local flows keep `normalize_project_path/1` and the local-only `filter_absolute_recent_projects/1` unchanged. Details: `projects_live/CONTEXT.md` → "Windows Path Handling (Project Open/Create Flow)".

### Design Notes — Remote-Connection Bootstrap Progress & Form (`settings_live.ex`)

- **Bootstrap progress steps must stay in sync with the core** (`EvoGit.RemoteConnection` bootstrap stages, `apps/evo_git/lib/evo_git/remote_connection.ex`). `settings_live.ex` renders a daisyUI `<ul class="steps steps-horizontal text-xs w-full">` in the Settings page target card, driven by a `stage_idx` case on the `bootstrap_stage` atom from `@bootstrap_progress[target.id].stage`. The mapping is a single linear sequence (monotonic in BOTH real bootstrap paths — do not reorder without checking the core):
  `:probing_platform`(0) → `:downloading`(1) → `:downloading_locally`(2) → `:uploading`(3) → `:extracting`(4) → `:setting_permissions`(5) → `:detecting_os`(6) → `:copying_config`(7) → `:generating_cookie`(8) → `:patching_binaries`(9) → `:starting_daemon`(10); `_ -> -1` fallback.
  Note: `:downloading_locally` is a distinct core-broadcast stage (auto-download path fallback when the remote download — curl, then wget — fails) with its OWN sequential step (index 2). `:detecting_os` fires ONLY in the local-tarball upload path (between `:setting_permissions` and `:copying_config`); the auto-download path never broadcasts it (OS comes from the probe). `:patching_binaries` fires ONLY when the remote host is NixOS (on-the-fly patchelf adaptation of the release binaries), between `:generating_cookie` and `:starting_daemon`. Any new core stage added in `remote_connection.ex` must be added to BOTH the `stage_idx` case and the `<li>` list here.
- **Remote-connection form fields** (Settings page): `local_binary_path` is OPTIONAL — blank means the runtime auto-downloads the `genesis_remote` release tarball on the remote (help text under the input). An optional `platform` text input (`"linux_x64"` / `"darwin_arm64"` / `"windows_x64"`, blank = auto-probe remote OS/arch) is persisted via `build_remote_target_from_params/1` (`platform: params["platform"] || ""`) into the target map; `EvoGit.RemoteConnections.get_target` carries the `platform` key (nil when unset). New-target defaults in `add_remote_target` include `platform: nil`.
- **Progressive disclosure — "Advanced settings" toggle**: the add/edit remote-connection form hides the advanced fields (`dist_port`, `local_binary_path`, `remote_path`, `platform`) behind a ghost "Advanced settings" toggle button (chevron up/down, `toggle_remote_advanced` event flipping the `remote_show_advanced` assign; reset to `false` in `add_remote_target`/`edit_remote_target`/`cancel_edit_remote` so every form opens collapsed). The four fields are wrapped in ONE `col-span-2` container with an inner `grid grid-cols-2 gap-4` (preserving the side-by-side Dist Port/Remote Path layout) that is hidden via the CSS `hidden` class — NOT `:if` — so the inputs stay in the DOM and are still submitted on save: editing an existing target with a non-default `dist_port`/`remote_path` keeps submitting those prefilled values (backend defaults only apply when the inputs are absent), and `build_remote_target_from_params/1` remains unchanged.
- **New gettext strings** (`Probing platform`, `Downloading`, `Downloading locally`, `Extracting`, `Copying config`, `Generating cookie`, `Leave blank to auto-download the release on the remote`, `Platform (optional)`, `Blank = auto-probe the remote OS/arch`) will NOT appear in the POT/PO until the next release-time extraction — expected, do NOT run `mix gettext.extract`/`merge` during development.

## Notes for Agents — Platform-specific Settings/System gating (`EvoDashWeb.PlatformInfo`)

`live/platform_info.ex` — platform detection for LiveViews with remote-node support. API:

- `os_for_node(current_node)` → `:linux | :macos | :windows | :unknown`. **Testability override FIRST**: `Application.get_env(:evo_dash, :platform_os_override, nil)` — when set to a known OS atom it is returned directly (the injection seam; `:os.type/0` is not injectable). Local node (nil/`node()`) → `EvoGit.Platform.os()`. Remote node → `EvoDash.NodeContext.call_remote(node, :os, :type, [])` mapped (`{:unix,:darwin}`→`:macos`, `{:unix,_}`→`:linux`, `{:win32,_}`→`:windows`, else `:unknown`). **Total — never raises** (justified `try/catch` at the cross-node RPC boundary, precedent `NodeContext.call_remote/4`).
- Pure predicates: `windows?/1`, `macos?/1`, `linux?/1`, `show_sandbox?/1` (→ `os in [:linux, :macos]`; conservative false for `:windows`/`:unknown`), `sandbox_backend_for/1` (`:linux→:systemd_run`, `:macos→:sandbox_exec`, `_→:none`).
- `filter_schemas_by_category(schemas_by_category, os)`: `:windows`/`:unknown` → `Map.delete(:sandbox)`; `:macos` → keep only `sub_category == nil` sandbox schemas (mode + write_paths; drops `:resources`/`:process`/`:linux` — consumed ONLY by the Linux/systemd-run machinery, see `sandbox/macos.ex`); `:linux` → unchanged. Other categories untouched (`Map.get(schema, :sub_category)` — absent key = nil).

**SettingsLive**: `schemas_by_category` filtered per node OS in `mount/3` AND re-filtered in `handle_params/3` (BEFORE `category_str_to_atom` so `?category=sandbox` resolves nil on Windows → falls back; if resolved/persisted active category is `:sandbox` but filtered map lacks it → falls back to `:llm`). **Data-safety**: filtering is BOTH the display mechanism AND the save round-trip protection — `save_category` (`settings_live.ex:~711`) only processes schemas present in the filtered list, and `params_to_category_config` (`config_io.ex:102+`) deep-deletes params absent from the form, so hidden sandbox fields (e.g. `[:sandbox,:resources]`, `[:sandbox,:linux]` on macOS) are never clobbered on save. `sandbox_backend(assigns)` private helper: local → `@scheduler_config[:sandbox_backend]` (accurate, reflects binary availability); remote → `PlatformInfo.sandbox_backend_for(@platform_os)` (`scheduler_config` is loaded from the LOCAL scheduler only — `ConfigIO.load_scheduler_config()` in mount). The Settings sandbox banner (`SettingsComponents.category_section/1` in `components/settings_components.ex`) renders an amber `:bwrap` clause ("Filesystem isolation active (bubblewrap); resource limits/syscall filtering unavailable").

**SystemLive**: Sandbox self-check **cell** (grid) wrapped in `if PlatformInfo.show_sandbox?(@platform_os)` (no "Not Available"/"Disabled" messaging on Windows/unknown); Nix Environment **cell** wrapped in `if @nix_check != nil and @nix_check.enabled and @nix_check.available` (nix hidden when disabled in config OR binary not found — `EvoGit.Nix.enabled?/0` + `EvoGit.Platform.nix_available?/0`). `platform_os` assigned in mount + handle_params (after `assign_node/2`). `safe_system_checks/1` — checks run on the LOCAL VM; only the config status is fetched remotely. The same gating predicates drive the health-banner aggregation via `health_checks/1` (see the self-check design note below).

**Tests**: platform-gating coverage lives in `test/evo_dash_web/live/platform_info_test.exs` via the `:platform_os_override` seam; `settings_live_test.exs` and `system_live_test.exs` are `async: false` (safe for `Application.put_env` + `on_exit` cleanup). `system_live_test.exs:77` shows the checks-driving pattern (`send(view.pid, {:system_checks_result, ...})`). Pin the round-trip safety via `ConfigIO.build_config_from_category_params/4` with macOS-filtered schemas.

## Notes for Agents — System self-check design (health banner + check grid, system_live.ex)

The self-check section (System page) is a **merged health banner + responsive 2D grid of check-term cells**. Durable facts:

- **Overall health banner** (full-width, top of the self-check section, ALWAYS rendered — including during `:checking`, where it shows a neutral spinning "Checking system health..." state): derived by `Status.overall_health/1` (`system_live/status.ex`) from `health_checks/1` (private in `system_live.ex`) — a map of the five check assigns plus `sandbox_shown`/`nix_shown` flags mirroring the cell gating in the template. Returns `%{status: :ok | :warning | :error | :loading, reasons: [String.t()]}`:
  - `:loading` when any ALWAYS-critical assign (supervisor/config/tools) is nil — nil checks render neutral, never as errors.
  - `:error` when any critical check fails: process tree (`Status.supervisor_healthy?/1`), configuration (`Status.config_ok?/1`), tools (`Status.tools_status/1 != :ok`), or sandbox (`Status.sandbox_status/1 != :ok` **only when `sandbox_shown`**).
  - `:warning` when only non-critical issues exist: nix enabled-but-not-built (`Status.nix_status/1 != :ok`, **only when `nix_shown`**) — deliberately NOT a hard failure (nix is optional).
  - `reasons` are gettext-wrapped human-readable strings ("Required settings are missing or invalid", "A required tool (git or ripgrep) is missing", "Sandbox is unavailable", …) rendered as a bullet list under the banner headline ("System running correctly" / "System needs attention" / "System running, but needs attention").
  - `@config_status` carries the remote config status on a remote node (via `safe_system_checks/1`), so the banner needs no extra RPC.
- **Check grid**: `div.grid.grid-cols-1.md:grid-cols-2.gap-3` with one `check_cell/1` per term: Configuration, Required Tools, Sandbox (gated `EvoDashWeb.PlatformInfo.show_sandbox?(@platform_os)`), Nix Environment (gated `@nix_check != nil and enabled and available`), LLM Connection (info status, `~p"/settings?category=llm&node=…"` link). `check_cell/1` is a `rounded-lg border` card whose `<details>/<summary>` disclosure expands to show (a) a gettext "what was checked" description line, (b) the detected values (rendered verbatim: config missing badges + validation-warning count, tool badges with version/error, sandbox backend + Enabled/Disabled + isolation/limits, nix Binary/flake.nix/Flake valid + error), and (c) a `:fix` slot hint rendered only when the status is not `:ok`/`:info` ("Install ripgrep…", "Fix the missing or invalid settings in Settings." + Open Settings link via `with_node_param(~p"/settings", …)`, per-backend sandbox hints, nix fix hint).
- **Supervisor health feeds ONLY the merged banner**: there is no "Genesis Process Tree" row and no `supervisor_status/1` component. The `supervisor_check` assign and `Status.supervisor_healthy?/1` exist and feed the banner's process-tree check.
- **Test markers**: title spans render exactly as `<span class="font-semibold text-sm">{title}</span>` so `>Sandbox</span>` / `>Nix Environment</span>` markers match; "flake.nix"/"Flake valid"/"hero-lock-closed"/"sandbox-exec (macOS)"/"bwrap (Linux)" detail strings are unchanged. Sandbox backend handling covers `:bwrap` too: `Status.format_backend/1` maps `:bwrap` → "bwrap (Linux)" (badge `badge-success` in the check grid), `Status.sandbox_status/1` treats bwrap with `capabilities.filesystem_isolation` as `:ok`, and the Settings sandbox banner (`SettingsComponents.category_section/1`) has an amber `:bwrap` clause. `system_live_test.exs` (16 tests) passes against the current markup.
- `~p` gotcha: interpolating into the PATH portion of a static route (`~p"/settings#{…}"`) emits a compile-time "no route path" warning (test_path materializes the interpolation as `"1"` → `/settings1`); use `with_node_param/2` (appends `?node=` — only safe for query-less URLs) or interpolate in the query portion only.

## Notes for Agents — Session-memory message timestamps

The agents-page chat history (`AgentsLive` → `messages_to_history_entries/1` → `message_to_history_entry/1`) consumes `%ReqLLM.Message{}` structs from `EvoGit.AgentScheduler.RemoteAPI.get_agent_history/1`, which returns the raw `AgentState.context.messages` list.

- **Timestamps are stamped at the source** by the `:evo_git` runtime into each message's `metadata[:timestamp]` (a free-form map). Representation: **Unix-seconds integer** or a **`DateTime`** (the dashboard formatter also accepts ISO8601 binaries defensively). Historical messages may lack the key.
- **Dashboard rendering**: `message_to_history_entry/1` carries the raw value as `timestamp:` in the entry map; the agents detail panel renders a short **LOCAL** 24h time (`"HH:MM:SS"`, seconds precision) next to "Turn x" via `EvoDashWeb.Helpers.format_history_timestamp/1`. Local conversion uses the offset between `:calendar.local_time/0` and `:calendar.universal_time/0` (stdlib-only; no tz DB — the default `Calendar.UTCOnlyTimeZoneDatabase` only supports "Etc/UTC"). **Graceful fallback**: when `timestamp` is absent, no time is rendered — just "Turn x", no placeholders.

## Notes for Agents — SQL-lowering of task lists (dashboard + tasks layer, live/)

- **Dashboard has NO main task list**: there is no `@tasks` assign, no `Assigns.current_tasks/1`, no `view_full_result`/`view_full_options`/`toggle_task_details` modal handlers, and no `tasks` attr on `Layouts.app`. `@running_tasks`/`@pending_tasks` (sidebar) are statuses-filtered summary maps from `TaskRegistry.list_tasks_summary([:running, :pending, :finalizing, :completed])` with keys `id, status, review_status, started_at, finished_at, type, project_path, opts, branch_name, model_id, agent_count, base_sha, commit_sha, lease_expires_at`; heavy fields (logs/usage/archive_metadata) are EXCLUDED, and `result` is not in the projection. `lightweight_task/1` and `strip_heavy_fields/1` do not exist. Template audit: no dashboard template reads logs/usage/archive_metadata from any task list. Browser notifications use the minimal id-only projection (`Assigns.build_notified_task_ids/1` → `TaskRegistry.list_task_ids([:completed, :failed, :cancelled])`); the `:node_aware_reload_tasks` handler diffs terminal ids against the notified MapSet and fetches FULL rows only for newly-terminal ids (`TaskRegistry.get_task/1` → `Project.task_notification_content/1`).
- **No `Assigns.assign_running_and_pending_tasks/2` exists** — the dashboard holds no unfiltered `@tasks` list to filter. The sidebar is served exclusively by the single `EvoDashWeb.LiveHooks.NodeAware` loader (`fetch_active_tasks/1` → `partition_active_tasks/1` → `assign_active_tasks/1`, `@active_statuses`, column-based `show_review_button?/1`; see the 'Unified sidebar Active Tasks loader' section below).
- **Sidebar reload debounce**: every LiveView with task-broadcast handlers routes through NodeAware debounce: `{:tasks_updated}` / `{:task_status, _, _}` handlers call `NodeAware.debounce_task_reload(socket)` (or `NodeAware.handle_task_info(socket, msg)`); the actual reload happens in `handle_info(:node_aware_reload_tasks, socket)`, which runs the page-specific refresh (tasks_live: `reload_current_page`; review_live: `load_task_data`; projects_live: project-config refresh + browser notifications via `Project.task_notification_content` — minimal `list_task_ids([:completed, :failed, :cancelled])` diff + per-new-id `get_task` — + sidebar, NO main list) and ends with `NodeAware.clear_task_reload_pending(socket)`. `{:recent_projects_updated}` in projects_live is NOT debounced (intentionally immediate).
- **Statuses API**: the evo_git statuses API — `EvoGit.TaskRegistry.list_tasks_summary/1`, `list_tasks_summary_by_path/2`, and `EvoDash.NodeContext.list_tasks_summary/2` / `EvoGit.RemoteNode.list_tasks_summary/2` — uses SQL-pushed-down `WHERE status IN (...)`. `tasks_live.ex`/`review_live.ex` must NOT wrap `NodeAware.handle_task_info/2` (which returns `{:noreply, socket}` and is the debounce entry point) in an extra `{:noreply, ...}` — call sites return its value directly, and the reload clauses in `handle_info(:node_aware_reload_tasks, socket)` call `NodeAware.reload_tasks/1` / `clear_task_reload_pending/1`.
- **Remote-poll dirty-check**: `tasks_live.ex` `:remote_poll` (3s, remote nodes only) is dirty-checked: each tick transfers ONLY the lightweight changed-since summaries (`EvoDash.NodeContext.list_tasks_changed_since/2`, usually `[]`) and calls `reload_current_page` only when `EvoDashWeb.TasksLive.DirtyTracker` (`tasks_live/dirty_tracker.ex`) reports `:reload` (changed) or `:resync` (deletion safeguard — forced full re-sync every 10 ticks ≈ 30s; changed-since cannot detect deleted tasks). The full page is full TaskInfo structs (logs/usage/archive_metadata) — never degraded to summaries, since the expandable task cards need those fields. The tracker is seeded in `handle_params/3` before `load_page_into_socket` on node switch (no reseed on pagination/filter push_patches — same node); the PubSub debounce reload (`:node_aware_reload_tasks`) deliberately does NOT advance the tracker (next tick re-detects and self-corrects). Guarded by `:remote_poll_timer` (no overlap), stops when node is local; the local-node path is PubSub-driven. The **sidebar has NO poll** (broadcast/debounce only). `agents_live.ex` `:remote_poll` is agent-SUMMARY based (list_agents RPC), not task lists.
- **Test strategy — remote poll**: the remote-poll dirty-check path is covered by pure `DirtyTracker` unit tests (`test/evo_dash_web/live/tasks_live/dirty_tracker_test.exs` — 15 cases: seeding, max_updated_at, evaluate actions, resync cadence, nil-baseline) plus a local-node `:remote_poll` smoke test in `tasks_live_test.exs` (no crash, polling stops). The real remote path (second BEAM + `:erpc`) is NOT exercised by the suite — it is covered by the pure tracker tests + manual remote testing.

## Notes for Agents — Welcome page stepwise quick setup (welcome_live.ex)
The quick setup is a **stepwise flow** (provider → variant → model → credentials):
- **Events**: `select_welcome_provider` (param `provider_id`; sets `selected_provider_id` + defaults `selected_variant_id` to the provider's FIRST variant; fully resets `selected_entry`/`api_key_input`/`base_url_input`/`search_query`/`llm_test_status`), `select_welcome_variant` (param `variant_id`, whitelist against the selected provider's variants; same resets), `base_url_changed` (mirrors `api_key_changed`), `select_welcome_model` (also resets `base_url_input`), `save_welcome_setup` (accepts `base_url`). Assigns: `selected_provider_id`, `selected_variant_id`, `base_url_input`. Model entries come from `flat_models` (built by `flatten_models`/`build_flat_entry` — key shape `provider_id, provider_display_name, variant_id, variant_display_name, credential_key, model_id, model_display_name, resolved_atom, model_string`); per-provider filtering via `provider_models/3` + `filtered_provider_models/2`.
- **Credentials gating**: the api_key input, merged save button, and connection test render ONLY when `@selected_entry != nil` (a model was clicked); placeholder "Select a provider and a model above to enter your API key." otherwise. The Base URL input renders only when `EvoGit.Config.LLMCatalog.requires_base_url?(@selected_entry.provider_id)` is true (catalog function; the `provider[:requires_base_url]` map field is unused); a hidden `base_url` input always carries the typed value so the save/test params shape is stable.
- **base_url threading**: `resolve_welcome_model_value/4` takes the typed base_url; opts `[base_url: String.trim(...)]` only when non-empty; error flash "Base URL cannot be empty." when required but empty. `test_llm` threads `socket.assigns.base_url_input` through the same path so the connection test verifies exactly what save configures. `do_save_model_profile` accepts the welcome page's `base_url`.
- **Custom-model providers excluded**: the provider grid skips `custom_model: true` entries (OpenRouter/OpenAI-Compatible API) — they have no preset models; the end-of-list guidance card directs users to Settings.
- **Test strategy**: `test/evo_dash_web/live/welcome_live_test.exs` derives ALL model ids/display names dynamically from `EvoGit.Config.LLMCatalog.providers()` (helpers `provider/1`, `model_string/2`) — never hardcode model ids there; the catalog changes frequently.

## Notes for Agents — Welcome page connection test & back nav (welcome_live.ex)

- **LLM connection test** (`test_llm` event + `{:llm_test_result, result}` handle_info + `llm_test_status` assign, `:idle` default): a compact inline port of the Settings page connection test, rendered as a sibling div BELOW the `phx-submit="save_welcome_setup"` form (MUST stay outside that form — nested forms are invalid HTML). The typed API key (if non-empty) is saved via `EvoGit.Config.save_credentials/1` BEFORE spawning (`EvoGit.SystemCheck.llm_test/1` reads credentials from the saved file, `apps/evo_git/lib/evo_git/system_check.ex:332-338`); `resolve_welcome_model_value/4` builds the exact model value the save flow stores, so the test verifies what will be configured. Success row renders `@selected_entry.model_display_name` (NOT `data.model` — avoids the map-render crash class). `select_welcome_model` AND `select_welcome_provider`/`select_welcome_variant` reset `llm_test_status` to `:idle` so stale results never show for a different model. Task spawned via `Task.Supervisor.start_child(EvoDash.TaskSupervisor, ...)` (same as Settings).
- **Back button**: page-body row above the header (visible in both setup/all-set states and any nav layout), pure client-side: `onclick="if (window.history.length > 1) { history.back(); } else { window.location.href = '/'; }"` — no phx event. The page passes `simple_nav={false}` (full sidebar) — the control lives in page content by design.
- **Test determinism note**: connection-test LiveView tests inject `:testing` via `:sys.replace_state` + `Phoenix.LiveView.Utils.force_assign` (async task is racy); ok/error via `send(view.pid, {:llm_test_result, ...})` + `render(view)`. A typed API key in a `test_llm` test makes the spawned task attempt a REAL provider request (fails fast with the bogus key); the unit-style handler test uses NO typed key so it fails fast without network.

## Notes for Agents — System page charts (scheduler status over time)

The System page (`system_live.ex`) renders three status charts below the self-check section: **LLM Slots**, **Tool Slots**, and **Agents**, sampled every 3 seconds over a 60-sample ring buffer (~3 minutes).

- **Charting approach — hand-rolled server-rendered SVG, zero JS deps**: there is NO plotting library in the bundle (no package.json/npm workflow; the vendored asset set is fixed — do not add JS/CSS). All geometry and markup live in the pure support module `EvoDashWeb.SystemLive.Charts` (`system_live/charts.ex`): ring-buffer `push/3` (default capacity 60), `build_sample/2` + `status_counts/1` (agent-list → sample map), `config_totals/1` (config → capacity map), series builders `llm_series/1`/`tool_series/1`/`agents_series/1`, `y_max/1` (max(capacity, observed) × 1.2, floor 4 — never zero, no div-by-zero), `y_for/2` (value → y coordinate within the fixed chart height, clamped at `y_max`), `path_for/2` (polyline + filled area as SVG `d` strings; values left-zero-padded via `pad_to_capacity/2` so fresh charts grow left-to-right), and the `charts_section/1` + `chart_card/1` components (`use Phoenix.Component`; card = legend row with last values + `<svg viewBox="0 0 300 100" preserveAspectRatio="none">` with `vector-effect="non-scaling-stroke"` strokes so lines stay crisp at any width). The LLM/Tool **Capacity** series is marked `static: true` and is rendered as a single dashed horizontal `<line>` reference line at the current config level (via `y_for/2`), not as a per-sample polyline/area — so it does not move when the cached config totals refresh. The LiveView is thin: mount assigns (`chart_samples`/`chart_node`/`chart_config_cache`/`chart_tick_count`), a `connected?`-gated `Process.send_after(self(), :system_chart_tick, 3000)` scheduled in mount + rescheduled inside the tick handler, `handle_info(:system_chart_tick, socket)` → `do_chart_tick/1`, and one `<Charts.charts_section samples={@chart_samples} paused={@scheduler_paused} />` call in `render/1`.
- **Data-source semantics + limitation (must stay truthful)**: live "used slot" holder counts are NOT observable from the dashboard — slot holders live only in the `EvoGit.AgentScheduler` GenServer loop state, are not mirrored to `:evogit_sched_meta` ETS, and are not exposed via `RemoteAPI`. "In use" is therefore a clearly-labeled proxy: the count of agents in `:running` status (the agents that acquire/hold slots); `:blocked` (agents waiting for a slot) is the "Waiting" series — a saturation signal. Capacities come from the resolved config: LLM = Σ `concurrency` over `model_profiles`, tool = `max_tool_concurrency` (via `config_totals/1`, `%{}`/unknown → zeros). The same `:running`/`:blocked` counts feed both the LLM and tool charts — only the capacity lines differ. Chart card descriptions state this proxy explicitly ("slot-use proxy — slot holders are not exposed").
- **Sampling & lifecycle**: 3s tick, ring buffer of 60 samples ≈ 3 min (cap memory). `handle_params/3` resets the buffer + config cache when `current_node` differs from `chart_node` (charts never mix nodes). Config totals are cached per node (`chart_config_cache` = `{node, totals}`) and re-fetched every 10th tick ≈ 30s (config changes rarely; avoids an :erpc round-trip per tick for remote nodes). The paused state is reused from the existing `@scheduler_paused` assign — shown as a "Scheduler Paused" badge in the charts section header.
- **Remote nodes**: everything goes through `EvoDash.NodeContext` (`list_agents/1` → `[]` on failure, `get_remote_config/1` → `%{}` on failure) — RPC failure / missing scheduler / missing config records a zero/empty sample for that tick, never crashes. **Local-node liveness gate**: `scheduler_alive?/1` (private in system_live.ex) returns `true` for remote nodes (RPC degrades gracefully) and for the local node checks `Process.whereis(EvoGit.AgentScheduler) != nil or :ets.info(:evogit_sched_meta) != :undefined` BEFORE any GenServer-backed config call — a fresh system with no scheduler records zero samples instead of raising `:noproc`. No try/rescue anywhere (project policy).
- **DOM contract — check cards**: the self-check `check_cell/1` component does NOT wrap its content in a `<details>`/`<summary>` disclosure. The header row is a plain `<div class="flex items-center gap-3 p-4">` (icon box + title + status icon), there is no chevron icon, and the details + `:fix` hint content is always rendered (the `:fix` block is gated on `@status not in [:ok, :info] and @fix != []`). ⚠️ The test `system_live_test.exs:544` ("check terms are expandable details cards…") passes vacuously because `Layouts.app`'s sidebar user-menu dropdown (`layouts.ex:367`) renders `<details>`/`<summary>` — the assertion matches the layout, not the check cells. The check cells themselves emit no `<details>`; do not "fix" the test by re-adding a disclosure.
- **Extension note**: if a future `:evo_git` change exposes real slot counters (e.g. mirrors slot-holder counts into `:evogit_sched_meta` or adds a `RemoteAPI` call), swap the status-derived proxy in `Charts.build_sample/2` for the real values — the sample map keys (`llm_used`/`tool_used`/`llm_waiting`/`tool_waiting`) and the card descriptions are the only places that need updating.

## Notes for Agents — Active-task query inventory

Consolidated map of every place the dashboard queries "active tasks" (running/pending/finalizing/completed) across all pages. All page-level task queries go through **three store projections**:

| Projection | Store fn | Keys | Decodes result? |
|---|---|---|---|
| Summary | `Store.select_tasks_summary/3` (apps/evo_git store.ex:332) | 15 keys: id, status, review_status, started_at, finished_at, type, project_path, **opts**, branch_name, model_id, agent_count, base_sha, commit_sha, lease_expires_at, updated_at | No — `result` is not selected or decoded (`decode_summary_row`, store.ex:1199) |
| Id-only | `Store.select_task_ids/1` (store.ex:204) | id, status, updated_at | No |
| Full | `safe_select_paginated_tasks/2` | all TaskInfo columns incl. logs/usage/archive_metadata | Yes (full struct) |

RPC chain for all three: LiveView → `EvoDash.NodeContext` → `EvoGit.RemoteNode` → local direct or `:erpc` → `AgentScheduler.RemoteAPI` → `TaskRegistry` → Store. Summary queries are Task-offloaded (the Store spawns a short-lived Task for query + decode + reply, so the decoded list is not allocated on the Store GenServer heap); `list_task_ids` replies inline by design (cheap).

**Every LiveView** gets the sidebar Active Tasks via `EvoDashWeb.LiveHooks.NodeAware` on_mount (applied in `evo_dash_web.ex:53`): `on_mount` → `load_running_and_pending_tasks/1` (node_aware.ex — delegating entry backed by the unified loader `fetch_active_tasks/1` → `partition_active_tasks/1` → `assign_active_tasks/1`, see "Unified sidebar Active Tasks loader" below) → statuses-filtered summary `@active_statuses = [:running,:pending,:finalizing,:completed]` (local TaskRegistry / remote NodeContext RPC; empty lists in pending-remote contexts, `fetch_active_tasks/1`). **The on_mount load is gated behind `connected?/1` (dead-render skip)** — the dead HTTP render keeps the empty-list `assign_new` assigns and fires NO query; the connected mount reload (or the first `handle_params` → `assign_node/2`) fetches the sidebar. `handle_params` → `assign_node/2` (node_aware.ex:130-184) reloads only when the `:tasks_node_loaded` context changed (dedup guard). Broadcasts → 300ms trailing debounce (`debounce_task_reload/1` node_aware.ex:362-370) → each page's `handle_info(:node_aware_reload_tasks)` → `NodeAware.reload_tasks/1` (or page-specific reload + `clear_task_reload_pending`).

Per-page active-task consumers:

| Page/module | Query | Call sites | Data used |
|---|---|---|---|
| `Layouts.app` sidebar (components/layouts.ex) | consumes `@running_tasks`/`@pending_tasks` assigns | render 138-179 | id (agents/review link), status (dot color 284-290, is_running), `opts[:objective]/[:prompt]` (task_label 257-266), started_at/finished_at (elapsed), project_path (grouping 295-329) |
| ProjectsLive (projects_live.ex) | — (NO local loader; routes through the unified `EvoDashWeb.LiveHooks.NodeAware.assign_active_tasks/1`) | task_submit:1098, cancel_task:1128, clear_task_history:1151, delete_task:1161 (immediate UX); NodeAware on_mount + assign_node dedup + broadcast-driven debounced reloads cover the rest | sidebar assigns only — ONE summary query per connected mount (dead-render skip; no duplicate queries) |
| ProjectsLive remote view | `NodeContext.list_agents/1` | handle_params 552-559 | `@remote_agents` render branch 284-362: id, agent_module, status, objective, model_id, repo_id, usage.total_tokens |
| TasksLive (tasks_live.ex) | `NodeContext.list_tasks_paginated/2` (FULL TaskInfo) | load_page 696-723 | expandable task cards (need logs/usage/archive_metadata), full-result modal, counts |
| TasksLive | `NodeContext.get_unique_paths/1` | mount:329, load_page_into_socket:758 | project filter dropdown |
| TasksLive remote poll | `NodeContext.list_task_ids/1` + `list_tasks_changed_since/2` | seed_dirty_tracker 778-798, :remote_poll 428-474 | DirtyTracker baseline/dirty-check (only updated_at read) |
| AgentsLive (agents_live.ex) | `NodeContext.list_agents/1` | load_agents:859 (mount/handle_params/full_reload/:remote_poll) | agent tree (id, parent_id, status, depth, context_path, objective, usage...) — scheduler ETS, NOT task store |
| SystemLive (system_live.ex) | `NodeContext.list_agents/1` | do_chart_tick:845 (3s chart tick) | Charts.build_sample status_counts (running/blocked proxy) |
| ReviewLive (review_live.ex) | `TaskRegistry.get_task/1` single fetch | ~692 | full TaskInfo for review page; no list queries |
| ProjectsLive notifications | `TaskRegistry.list_task_ids([:completed,:failed,:cancelled])` + per-new-id `TaskRegistry.get_task/1` | :node_aware_reload_tasks 1491-1523, `Assigns.build_notified_task_ids` 21-26 | id/status/updated_at diff; full row only for new terminal ids → `Project.task_notification_content` (needs opts + result) |
| WelcomeLive / WelcomeCompleteLive / SettingsLive | none of their own | — | only pass sidebar assigns through |

**Field needs per consumer:** sidebar + pending-candidate derivation need ONLY id, status, opts (label), started_at, finished_at, project_path, branch_name (via the single public `show_review_button?/1`), review_status. **`result` is not in the summary maps** — the sidebar's `show_review_button?/1` is column-based (single public implementation in `EvoDashWeb.LiveHooks.NodeAware`, node_aware.ex — matches `branch_name` presence, never `result`), and `branch_name` is a denormalized summary column populated at write time (`codec.ex:95` `task.branch_name || extract_branch_name(task.result)`, extract at codec.ex:107-116). TasksLive's full-result modal reads result from the paginated FULL TaskInfo path, not the summary. `list_tasks_changed_since` consumers (DirtyTracker) only read updated_at. **`result` is excluded from the summary projection** — `Codec.decode_result/1` is the heaviest per-row decode, and retaining decoded result maps in LiveView heaps is a memory driver (~25 MB observed). `opts` stays in `@summary_columns` — task_label needs it.

**Unified sidebar Active Tasks loader:**

- **Home**: `EvoDashWeb.LiveHooks.NodeAware` (node_aware.ex) — the shared node-aware sidebar loader applied to all LiveViews.
- `@active_statuses [:running, :pending, :finalizing, :completed]` (module attribute).
- `fetch_active_tasks/1` (node_aware.ex) — node-aware fetch returning `{running, pending}`. SOCKET-based shape (reads `socket.assigns[:current_node] || node()` and `[:current_node_id]` — this is what preserves the pending-remote guard): local → `TaskRegistry.list_tasks_summary(@active_statuses)`, remote → `EvoDash.NodeContext.list_tasks_summary(current_node, @active_statuses)`. Pending-remote guard: `current_node_id != nil and current_node == node()` → `{[], []}` (local tasks never appear in a pending remote context).
- `partition_active_tasks/1` (node_aware.ex) — PURE (no I/O): `{running, pending}` where running = status in `[:running,:pending,:finalizing]`; pending = completed + `review_status` nil + `show_review_button?/1`, sorted `{:desc, DateTime}` by `finished_at || started_at`.
- `assign_active_tasks/1` (node_aware.ex) — ARITY CHOSEN: `/1` socket-based (fetches via `fetch_active_tasks/1`, assigns `:running_tasks`/`:pending_tasks`). Consistent with `fetch_active_tasks/1`'s socket shape and the ProjectsLive event-handler call sites (they run on connected sockets).
- `load_running_and_pending_tasks/1` (node_aware.ex) — public delegating entry point (`assign_active_tasks/1`), used by on_mount, assign_node, reload_tasks, and projects_live.ex:1525's `:node_aware_reload_tasks` handler.
- `show_review_button?/1` (node_aware.ex) — PUBLIC, column-based: `%{status: :completed, branch_name: branch}` with `is_binary(branch) and branch != ""` → true. NEVER reads `result` from summary maps. There are no duplicate private copies in `Assigns` (assigns.ex).
- **Dead-render skip**: `on_mount/4` calls the loader ONLY when `Phoenix.LiveView.connected?(socket)`; the dead render keeps the empty-list `assign_new` assigns (no query). `:tasks_node_loaded` is seeded on BOTH paths so the first `handle_params` → `assign_node/2` never double-fetches. `load_running_and_pending_tasks/1` itself is NOT gated (tests call it on non-connected sockets).
- **No duplicates exist**: `Assigns.assign_running_and_pending_tasks/1`, `Assigns.show_review_button?/1` (assigns.ex), and `EvoDash.NodeContext.list_tasks/1` (node_context.ex, full TaskInfo, zero web-layer callers) are absent.
- **ProjectsLive call sites**: task_submit, cancel_task, clear_task_history, delete_task call `NodeAware.assign_active_tasks/1` (immediate UX on connected sockets); the former mount/handle_params/activate_project call sites are absent (covered by NodeAware on_mount/assign_node dedup + broadcast-driven debounced reloads).

`pending_tasks` derivation is `branch_name`-column-based (NO `result` matching); the evo_git `@summary_columns` (store.ex:78) excludes `result`.

## RPC Consumption Audit — AgentsLive / SystemLive (remote-SSH UX optimization input)

Verified against the current code (agents_live.ex, system_live.ex, system_live/charts.ex, system_live/update_card.ex, agents_live.html.heex). All `EvoDash.NodeContext.*` calls delegate to `EvoGit.RemoteNode` (local = direct call, remote = `:erpc`); none of these LiveViews call `EvoGit.AgentScheduler.RemoteAPI` or `EvoGit.RemoteNode` directly.

- **`NodeContext.list_agents/1`** — AgentsLive `agents_live.ex:879` (`load_agents/1`): full re-fetch on EVERY `{:agents_updated}` broadcast (`:125-161`), on mount (`:30`), on node switch in `handle_params` (`:81`), on remote `{:agent_registered}` via `full_reload` (`:243`, `:632-650`), and every 3s `:remote_poll` tick for remote nodes only (`:191-230`; `agent_updated`/`agent_removed` merge in-memory, NO re-fetch). Consumed summary keys: `id, task_local_id, repo_id, repo_root, task_id, task_number, status, depth, parent_id, worktree, retries, agent_module, model_id, objective, context_path, current_commit, base_commit, usage, total_tokens, compression_count`. `pending_sub_agents`/`sub_agent_results`/`task_ref`/`result_sent` are NOT read from the summaries (defaulted server-side). `children`/`has_children` computed server-side from `parent_id`; `compression_pct` computed from `total_tokens` + threshold. SystemLive `system_live.ex:1306` (`do_chart_tick/1`): every 3s `:system_chart_tick` on ALL nodes; ONLY `agent.status` + list length consumed (`Charts.status_counts`, charts.ex:90-101); the agents list is NEVER assigned to the template — reduced to 11-int sample maps in `@chart_samples`.
- **`NodeContext.get_agent_history/2`** — `agents_live.ex:990` (`load_agent_history/2`): per `select_agent` click (`:453`), per `{:agent_updated}` of the SELECTED agent (`:323`), per 3s remote poll for the selected agent (`:212`), per `{:agents_updated}` for the selected agent (`:146-150`), and `view_full_message` safety net (`:489`). Consumed `%ReqLLM.Message{}` fields (`message_to_history_entry/1`, `:1002-1021`): `metadata.turn`, `metadata.timestamp`, `role` (→ `Atom.to_string`), `content` (ONLY `:text` ContentParts, joined), `tool_calls` (→ `ToolCallDisplay.display/1`), `reasoning_details`, `name`. The FULL `metadata` map is carried into `data.metadata` but never rendered (dead weight over the wire). Uppercase entry types ("USER_PROMPT", "THOUGHT_CHUNK", "TOOL_CALL_START", "COMPLETE", "RETRY", "QUEUED", "CONTEXT_TREE") are dead template branches — nothing produces them (also noted in `agents_live/CONTEXT.md`).
- **`NodeContext.get_agent_state/2`** — NOT called by either LiveView (rg-verified).
- **`NodeContext.get_remote_config/1`** — SystemLive `system_live.ex:1327` (`chart_totals/3`): refetched every 10th tick (~30s) or on cache miss/node change (`chart_config_cache` = `{node, totals}`); ONLY `model_profiles[].concurrency` + `max_tool_concurrency` consumed (`Charts.config_totals`, charts.ex:107-116). AgentsLive `safe_compression_threshold/1` remote branch (`agents_live.ex:966`): reads `get_in(config, [:llm, :compression_threshold_tokens])` from the SCHEDULER config — which does NOT carry an `:llm` section (no compression_threshold_tokens in `agent_scheduler.ex`/`state.ex`), so the remote branch always falls back to `100_000`. Remote compression % bars are therefore computed against the default threshold; the local branch (`EvoGit.Config.resolve([:llm, :compression_threshold_tokens])`) works.
- **`NodeContext.get_remote_config_status/1`** — AgentsLive `agents_live.ex:979` (mount + node switch, feeds the layout config banner via `@config_status`); SystemLive `system_live.ex:1278` (`safe_system_checks/1` remote clause, run once per mount + `rerun_checks` event via async Task). Consumed keys: `ok?` (`Status.config_ok?`, status.ex:15-16), `missing` (list, `system_live.ex:377`), `validation_errors` (list, `:385-393`).
- **`NodeContext.paused?/1`** — SystemLive `system_live.ex:840` (per `handle_params`). ⚠️ GAP: mount (`:787`) and the `{:scheduler_config_updated}` broadcast handler (`:1168-1170`) use `load_paused_state/0` = LOCAL `EvoGit.AgentScheduler.get_config()` (`:1286-1288`) — on a remote view that broadcast (which only fires locally) clobbers the node-aware value with the LOCAL scheduler's paused state.
- **`NodeContext.list_task_ids/2`** — ONLY in `update_card.ex:41` (`active_task_ids/1`): passes `node()` (LOCAL always — the update card is desktop+local-only by design), statuses `@active_statuses = [:running, :pending, :cancelling, :finalizing]`, ONLY `:id` read (`Enum.map(& &1.id)`). Triggers: `request_apply_update` gate (`system_live.ex:1077`), `confirm_apply_graceful` (`:1114`), `force_kill_all` (`update_card.ex:116`), and the wind-down poll every 5s (`:update_winddown_poll_ms`, `:172`) until empty or deadline (~35min). `graceful_cancel`/`force_kill` call `EvoGit.TaskRegistry` directly (local).
- **Direct `EvoGit.AgentScheduler` calls** — SystemLive `toggle_pause` (`:908-947`): remote → `NodeContext.call_remote(node, EvoGit.AgentScheduler, :pause|:resume, [])` (`:915`, `:931`); local → direct `AgentScheduler.resume()/pause()` (`:917`, `:933`). `restart_remote`/`stop_remote` (`:965`, `:1007`) are remote-only RPCs (always `:ok`). `load_paused_state/0` (`:1286-1288`) is the only `AgentScheduler.get_config/0` call (see the paused? GAP above). AgentsLive has NO direct scheduler/RemoteAPI calls — all through NodeContext.

## Constraints
- Each LiveView uses either an inline `render/1` or a companion `.html.heex` template — not both. (Three render inline; `AgentsLive` uses a separate template.)
- Auto-refresh intervals must be gated behind `connected?/1` to avoid leaking timers.
- Naming: `<name>_live.ex` for modules, `<name>_live.html.heex` for companion templates.
- **No `try/rescue` around config/core-value loading.** LiveViews must NOT wrap `EvoGit.Config.*`, `EvoGit.AgentScheduler.*`, or `EvoGit.SystemCheck.*` calls in `try/rescue`. If these crash, the LiveView SHOULD crash truthfully — displaying a wrong default value (e.g. `false`, `%{}`, `100_000`) is worse than a visible crash, and the supervision tree handles recovery. Defensive `rescue _ -> default` around these functions is an anti-pattern and must be removed.
- **`try/rescue` for external boundaries requires a justification comment.** The only accepted use is around genuinely untrusted external execution (e.g. `System.cmd` running project-config-defined shell commands in `ProjectsLive`'s `run_command`), where a user-friendly error flash is better UX than a LiveView crash. Every retained `try/rescue` must carry a comment explaining why it is justified.
- **Atom conversion from untrusted input uses whitelist lookups, not `try/rescue`.** For converting user-supplied strings (URL params, form POST data) to atoms, use explicit `Map` lookups (`Map.get/2`, `Map.fetch/2`) or `case` against a known whitelist — never `String.to_existing_atom/1` wrapped in `try/rescue`, and never `String.to_atom/1` (atom-table exhaustion DoS risk).
