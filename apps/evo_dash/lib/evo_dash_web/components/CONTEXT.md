# UI Components

## Intent

Contains all reusable UI component modules and layout templates for the EvoDash web interface. This is the presentation layer — Phoenix function components rendered via HEEx templates, organized by domain concern.

## Routing Table

- `layouts/` → HTML layout templates (currently only the root layout shell with meta tags, CSRF, theme persistence)
- `agents_components/` → Sub-components of `AgentsComponents`: `CommitGraphView` (TEMPORAL git-commit-history left-panel view — `commit_graph_view/1`, geometry/routing/lane planning split into `agents_components/commit_graph/geometry.ex`) — full detail: `agents_components/CONTEXT.md`
- `review_components/` → Sub-components of `ReviewComponents`: `DiffViewer` (split diff + file tree + layouts), `Header` (page header, agent report, objective, task details), `Actions` (task-level actions + skills modal), `RepoCards` (per-repo merge/reject cards), `Stats` (diff stat row, commits list) — full detail: `review_components/CONTEXT.md`
- `settings_components/` → Sub-components extracted from `SettingsComponents`: `CategoryMetadata`, `SettingCard`, `ModelProfilesEditor`, `CustomAgentsEditor`, `CustomToolsPanel`, `ModelSelectionEditor`, `Sidebar`, `SearchResults`, plus the shared building blocks `SaveBar`, `SectionHeader`, `CardShell`, `FormFooter` (all four consumed by `SettingsComponents`/`SearchResults`/the editors — see the Modules table)

## API Surface

### Modules
| Module | File | Purpose |
|--------|------|---------|
| `EvoDashWeb.CoreComponents` | `core_components.ex` | Phoenix 1.8 scaffolded building blocks: `flash/1` (three kinds — see "Flash Component Kinds" below), `button/1`, `input/1` (text/checkbox/select/textarea), `header/1`, `table/1`, `list/1`, `icon/1` (hero + `brand-*` sets incl. `brand-github` used by the Projects top-bar GitHub button), `tabs/1`, `collapsible_card/1`, plus the `show/1`/`hide/1` JS-command wrappers and `translate_error/1`/`translate_errors/2`. NOT here: `flash_group/1` and both theme toggles live in `layouts.ex`, and there are NO `show_notification`/`toggle_dropdown`/`focus`/`close_parent` JS commands anywhere in the codebase (stale doc claims — do not reuse them for dropdown auto-close work; sidebar dropdowns are native `<details>` and any close logic must be JS-side). |
| `EvoDashWeb.GitHubComponents` | `github_components.ex` | GitHub issues modal (`issues_modal/1`, root id `github-issues-modal`) for the Projects page: state-filter buttons (`open`/`closed`/`all` via `phx-click="github_filter_state"`), issue rows (number, state badge, label badges, author/date, external GitHub link, per-row `github_fix_issue` Fix button), loading/empty/error states; public `error_message/1` surfaces `gh` CLI errors (`{:gh, _, output}` → output; anything else → generic "install/authenticate gh" message). Pure presentation — no gh/git calls. |
| `EvoDashWeb.ProjectComponents` | `project_components.ex` | **Command Palette** project control (`project_omnibox/1` — trigger button → centered modal overlay with search-filtered recent-projects list + "Open Project by Path" + "Create New Project"; keyboard-first via server-side `palette_keydown`; public `filter_projects/2` helper), sub-components `palette_menu/1`, `palette_open_path/1` (with PathAutocomplete hook), `palette_new_project/1`. Project settings panel + dropdown tab (`project_settings_panel/1`, `project_settings_tab/1`, shared `project_settings_body/1`: genesis.toml status, worktree script, dev commands, foreign repos — see "Foreign Repos — writable + base_sha + edit-in-place" below). Deliberately EXTENDED past the nominal per-file line-count policy (not split) to carry the foreign-repo add/edit UI — see the same Design Note. |
| `EvoDashWeb.TaskFormComponents` | `task_form_components.ex` | Single-card, two-layout objective editor (`task_form/1` — ONE `.input-card` containing the prompt textarea (`phx-hook="AdaptiveInput"` + `phx-update="ignore"`, NO `phx-change`) AND the bottom toolbar `.input-controls` (attach-kind dropdown | mode | agent | model | circular send button) as its last element; `data-layout` is server-seeded at render via `layout_for/1` and client-driven by the AdaptiveInput hook — see "Task Form — Single-Card Two-Layout Design" below), `task_options_tab/1` (Configure-dropdown Task Options; inputs carry `form="task-form"`), legacy `advanced_options/1` |
| `EvoDashWeb.TaskCardComponents` | `task_card_components.ex` | Task cards with accent bar, relative timestamps, expandable details, result/options rendering helpers (`render_result_full/1`, `render_options_full/1`, `objective_text/1`, `result_copy_text/1`). **Summary-map safe** — hardened so both full `%TaskInfo{}` structs (TasksLive) and lightweight summary maps render without KeyError (see "Task Cards & the Summary-Map Contract" below). |
| `EvoDashWeb.ArchiveComponents` | `archive_components.ex` | Per-agent archive records, nested agent hierarchy tree, recursive node renderer (uses `EvoDashWeb.ArchiveHelpers` — module at `../archive_helpers.ex`, outside this node) |
| `EvoDashWeb.SettingsComponents` | `settings_components.ex` | Settings page components with VS Code-inspired sidebar+content layout: `setting_card/1` (schema-driven config key card with input widget, description, default hint, validation error display), `category_section/1` (right content area for a category with grouped settings and save button), `settings_sidebar/1` (category sidebar with icons, key counts, and search filter). Also hosts the LLM Quick Setup flow (see "Settings Quick Setup" below). |
| `EvoDashWeb.SettingsComponents.SaveBar` | `settings_components/save_bar.ex` | Minimal pinned bottom-right save bar (`save_bar/1`, attrs `:label` + optional `:form` — HTML form-association id, default nil) — consumed by `category_section/1` (generic `:else` branch nests it inside the `save_category` form; LLM renders it outside its flat form at the pane bottom submitting via the `form` attr) and `SearchResults` |
| `EvoDashWeb.SettingsComponents.SectionHeader` | `settings_components/section_header.ex` | Shared sticky top section header (`section_header/1`, attrs `icon`/`title`/`description`) — consumed by `category_section/1` and `SearchResults` |
| `EvoDashWeb.SettingsComponents.CardShell` | `settings_components/card_shell.ex` | Shared editor card wrapper (`card_shell/1`, attrs `title`/`description`/`class` + `:actions` slot + `:inner_block`) — consumed by `ModelProfilesEditor`/`CustomAgentsEditor`/`ModelSelectionEditor` |
| `EvoDashWeb.SettingsComponents.FormFooter` | `settings_components/form_footer.ex` | Shared edit-form action footer (`form_footer/1`, attrs `cancel_event`/`save_label`: Cancel ghost btn + primary submit with `hero-check`) — consumed by the profile and custom-agent edit forms. `settings_components.ex` (`category_section`) and the settings editors (`ModelProfilesEditor`/`CustomAgentsEditor`/`ModelSelectionEditor`/`SearchResults`) consume all four building blocks. |
| `EvoDashWeb.SettingsComponents.CustomToolsPanel` | `settings_components/custom_tools_panel.ex` | Read-only "Custom Tools" panel (`custom_tools_panel/1` — attrs `:status` (required; `%{ok: [...], errors: [...]}` or `{:error, term()}`) + `:loading` (default `false`)) built on `card_shell/1`: lists the tool modules discovered in `<config_dir>/tools/` (name/module/file + read-only vs write badge), per-file load errors, an empty state, and a `{:error, reason}` banner; Refresh button emits `phx-click="reload_custom_tools"` (handler owned by `SettingsLive`). Also exposes the pure `custom_tool_names/1` helper (used to feed `CustomAgentsEditor`'s optional `:custom_tool_names` attr). All status reads are TOTAL (atom-or-string keys, no `try/rescue`). Detail: `settings_components/CONTEXT.md`. |
| `EvoDashWeb.AgentsComponents` | `agents_components.ex` | Agent tree visualization: recursive `agent_tree/1` with connector lines and status indicator helpers (color/background/border/icon) for pending/running/waiting states |
| `EvoDashWeb.AgentsComponents.CommitGraphView` | `agents_components/commit_graph_view.ex` + `agents_components/commit_graph/geometry.ex` | TEMPORAL (git commit history) left-panel view for the Agents page — public `commit_graph_view/1` renders the fully-prepared repo data (`:repos`/`:selected_id`/`:loading`/`:error`/`:node_key`) as a **VERTICAL, GitLens/GitKraken-style PER-AGENT-LANE commit graph**: ONE ROW per commit (globally interleaved, top → bottom), ONE LANE PER AGENT in the left gutter, CIRCULAR dots, ROUNDED cross-lane edge routing, DASHED `:spawn`/`:merge_back` connectors, sticky per-lane header chips and a horizontally-scrolling gutter; vertical scrolling is native — there is NO pan/zoom and NO viewport transform. Purely presentational (no I/O, no socket, no data assembly). Geometry/routing/lane planning live in the pure `Geometry` submodule (`@row_h 44`, `@col_w 24`, `@gutter_pad 12`, `@node_r 6`, `@base_r 4`, `@bend_r 7`, `@exit_gap 2`, `@chip_row_h 18`); the HEEx half owns markup + paint + selection. `path.cg-edge[data-commit-graph-anim="edge"]` (ALL kinds): a same-lane edge is a straight vertical line, a cross-lane edge routes orthogonally with BOTH corners rounded by `Q` quarter-turns (never a sharp `L x y L x y` double-corner); `:parent` solid width 2, `:merge`/`:spawn`/`:merge_back` dashed `4 3` width 1.6; a `:spawn` departs BELOW its fork dot, a `:merge_back` runs from the child tip back into the parent's lane — its `to_sha` may be nil for a VIRTUAL landing (the edge's own `to_column`/`to_row` are authoritative; the path just ends there). `g.cg-node[data-commit-graph-anim="node"]` is a `circle.cg-node-dot` (r 6) filled with its OWNER agent's depth hue, EXCEPT an owner's END node (`owner_id ∈ node.end_ids`) which uses the shared status color `Helpers.agent_status_svg_color/1` — a `:base`/`:noop` node is a smaller HOLLOW circle (r 4), an unowned node muted base ink. Selection (`:selected_id`) rings the selected agent's START (solid) / END (dashed) dots with `circle.cg-node-ring` (r + 3) and accents those rows; the click contract lives on the ROWS and the lane chips (`phx-click="select_agent"`), never on the (pointer-events-none) gutter. The lane header (`#cg-lane-header-<dom>`, sticky `top-0`, inside the `#cg-scroll-<dom>` `overflow-x: auto` wrapper so gutter + rows + headers scroll as ONE unit) carries ONE chip per lane: agent lanes a clickable `button#cg-lane-<dom>-<lane>` labelled `T<task_local_id>` in the agent's hue with a status dot (dimmed when `ended`), the neutral lane 0 a non-clickable "Pre-task" span. Emits the FROZEN DOM markers the assets animation targets: root `#commit-graph` + `phx-hook="CommitGraph"`, node-scoped `#commit-graph-body-<node_key>`, per-repo wrapper `div` id IS `repo_dom_id` verbatim (already `commit-graph-repo-<slug>-<hash>` shaped), `#cg-scroll-<dom>` → `#cg-lane-header-<dom>` + `#cg-list-<dom>.cg-list.relative` → `svg#cg-gutter-<dom>.cg-gutter` + `.cg-rows` (left-padded by the gutter width), per-row `div#commit-row-<dom>-<sha>.cg-row[data-commit-graph-anim="row"]`, agent chips `#commit-agent-tag-<dom>-<sha>-<start\|end>-<agent_key>`, ref chips `#commit-ref-tag-<dom>-<sha>-<ref_key>`, dots `#commit-node-<dom>-<sha>`, commit→parent edges `#commit-edge-<dom>-<from_sha>-<to_sha>` + agent-level edges `#commit-edge-<dom>-<kind>-<owner>-<from>-<to>` (`to_key` = `l<col>r<row>` for a virtual landing), readout `#cg-selection-readout-<repo_dom_id>`; `data-commit-graph-anim` takes EXACTLY `"edge"` / `"node"` / `"row"` (no `cg-svg`/`cg-viewport`/lane/toolbar). Detail: `agents_components/CONTEXT.md`. |
| `EvoDashWeb.ReviewComponents` | `review_components.ex` | Review page components — GitHub-PR-style tab layout, Adwaita-styled. Facade delegating to the `review_components/` sub-modules + locally owning `page_tabs/1` (underline tab bar: Conversation / Files changed / Commits / Archive, event `switch_tab`) and `archive_review_section/1`/`archive_tree_node/1` (archived agent tree). Delegated: `page_header/1` (compact header: back link, one-line `short_title`, review+task status badges, wrapping meta line, diff-stat row), `task_summary/1` (native `<details>` disclosure with Token & Cost usage), `agent_summary/1` (comment-style Agent Report card with Markdown/Raw toggle + copy), `objective_section/1`, `repo_cards/1` (per-repo merge/reject cards: async merge-check strip `:checking`/`:clean`/`:conflict` with **Auto-resolve conflict**, multi-repo switcher, "Merge into" target form), `task_actions/1` (primary-scoped task-level actions: Continue task; a task with NO changes in ANY repo passes `no_changes: true` to promote the dismissal to a **"Mark as read"** primary action firing the existing `ignore` event; the "…" overflow menu carries Create-View PR / Extract Skills / Export JSON / Ignore — Ignore suppressed via `show_ignore: false` once promoted — as the orphaned-branch escape hatch), `extract_skills_modal/1`, `diff_stats_bar/1`, `commits_list/1` (clickable rows → `inspect_commit`), `file_tree_sidebar/1` (server-driven tree + filter input), `diff_viewer/1` (split diff, client-side highlight.js), `split_diff_layout/1` (multi-repo toolbar + two panes), `commit_detail_header/1`, `commit_diff_layout/1`, plus the public `conflict_files_summary/1` helper. Detail: `review_components/CONTEXT.md`. |
| `EvoDashWeb.RemoteGateComponents` | `remote_gate_components.ex` | Shared SSH-remote connection gate: `remote_connection_gate/1` (spinner + "Connecting to %{name}…" for `:connecting`/`:bootstrapping`/`:disconnecting`; `alert-error` with `last_error` + Retry / Manage Connections / Switch to Local for `:error`/`:disconnected`; nothing when connected/local) + public predicate `gate_active?/1` (nil node → false; node + nil status → true; connecting/bootstrapping/disconnecting/error/disconnected → true; connected → false; unknown shapes → false). ⚠️ Call it fully-qualified with the assigns map (`<%= EvoDashWeb.RemoteGateComponents.remote_connection_gate(assigns) %>`) — do NOT use the declarative `<.remote_connection_gate assigns={assigns}/>` form: it mangles node-name resolution (renders "Local" instead of the target name). |
| `EvoDashWeb.Layouts` | `layouts.ex` | Layout function components — `app/1` (the **sidebar-based app shell**: fixed left `<aside id="sidebar">` with branding, nav links, active-tasks section, and a bottom bar containing the node selector (leftmost) + language/theme toggles + collapse toggle; `phx-hook="SidebarCollapse"` for collapsible mode persisted to `localStorage`; mobile hamburger + overlay present but not yet wired to JS), `flash_group/1` (renders `:info`, `:error`, `:warning` flashes), `theme_toggle/1`, `theme_toggle_compact/1`, `language_selector/1`. **Node-aware (SSH Remote Development):** `app/1` accepts optional backward-compatible attrs (`current_node`, `current_node_id`, `current_node_name`, `remote_targets`, `connection_statuses` — all defaulting to safe local values) and renders `<.live_component module={EvoDashWeb.NodeSelectorComponent}>` inside the sidebar's bottom bar (`drop_up={true}`); a private `with_node_param/2` helper appends `?node=<id>` to ALL nav links when a remote node is selected. The `simple_nav` attr (default `false`) hides nav links, active-tasks section, node selector, and bottom bar — leaving only branding (used by WelcomeLive). The app shell is sidebar-based — no top-bar header exists. |

### Templates
| Template | Path | Purpose |
|----------|------|---------|
| Root HTML layout | `layouts/root.html.heex` | Base HTML shell — meta tags, CSRF token, LiveView title, CSS/JS asset links, client-side theme persistence script (`phx:theme` localStorage) |

### Theme delivery chain (client-side theme state + server-driven global accent)
- **Theme state is 100% client-side.** `layouts/root.html.heex:28-79` embeds an inline script that on load reads `localStorage["phx:theme"]` (default `"system"`), resolves `system` via `matchMedia("(prefers-color-scheme: dark)")`, and sets `document.documentElement` attributes `data-theme` (= resolved `"light"`|`"dark"`) and `data-theme-mode` (= chosen `"system"`|`"light"`|`"dark"`). It also swaps the favicon (`logo.svg`/`logo-alt.svg`) and `theme-color` meta. Cross-tab sync via the `storage` event (root.html.heex:68-70); system-theme change re-resolves only in system mode (:76-78).
- **Toggle buttons never touch the server.** `theme_toggle_compact/1` (layouts.ex:552-595, rendered in the sidebar bottom bar at :240) and legacy `theme_toggle/1` (layouts.ex:658-694, used only by the legacy home template) fire `phx-click={JS.dispatch("phx:set-theme")}` with `data-phx-theme="system"|"light"|"dark"`; the inline script listens for the window `phx:set-theme` event (root.html.heex:73). Tailwind's `dark:` variant is bound to the DOM attribute: `@custom-variant dark (&:where([data-theme=dark], [data-theme=dark] *))` in `assets/css/app.css:99`. DaisyUI loads with `themes: false`, so no `--b1/--b2/...` namespace exists — custom rules use `--color-base-*`/`--color-primary` with `color-mix`.
- **The GLOBAL accent IS server-driven (implemented).** `EvoDashWeb.LiveHooks.Appearance` (`live_hooks/appearance.ex`, registered after `NodeAware` in `evo_dash_web.ex:52-57` — Guide stays LAST) resolves `EvoGit.Config.resolve([:appearance, :accent_color])` node-aware through a per-node accent cache (`EvoDash.AccentCache`, `lib/evo_dash/accent_cache.ex`): local nodes → synchronous config resolve on the `:handle_params` interceptor; remote `genesis_remote` nodes → cache read synchronously first (warm remote accents paint from the first paint), then an async `EvoDash.TaskSupervisor` fetch via `NodeContext.get_resolved_config/1`, stale-guarded, writes the cache on success — never an RPC on the render path. It seeds `@accent_color` on every LiveView. `Layouts.app` declares `attr(:accent_color, :string, default: "blue")` and sets `data-accent-color={@accent_color}` on the `#app-layout` shell div (layouts.ex:67-70); all ~9 `Layouts.app` render sites pass `accent_color={@accent_color}`. This activates the ten `[data-accent-color="<name>"]` override rules in app.css (see `assets/CONTEXT.md`) — un-layered author CSS after the daisyUI theme `@plugin` blocks that retarget `--color-primary`/`--color-accent` for the `#app-layout` subtree. The accent rides on the SAME theme switch as the client-side state: `<html data-theme=...>` is the ancestor, so the dark-theme variants (`[data-theme="dark"] [data-accent-color="purple|slate"]`) apply inside the shell. The OTHER server-computed accent remains ProjectsLive's per-project `--project-accent`/`--project-ring-accent` inline style on `#dashboard-root` (projects_live.ex:92-101, `EvoDashWeb.ThemeColor.accent_color_for_mode/2` / `accent_color/1`, theme_color.ex:30-80) — a per-project ring/mode accent scoped to the objective box, distinct from (and overridden by, inside `#dashboard-root`) the shell-wide config accent. The shell chrome is token-based (Adwaita): `#app-layout` + sidebar `bg-base-200`, content `#main-scroll`/`main#main-content` `bg-base-100` (the scroll-container background invariant below), body `bg-base-200` — no slate/white literals.

### Usage Pattern
All component modules use `use EvoDashWeb, :html` (which expands to `use Phoenix.Component` + HTML imports). Components are invoked as function components in LiveViews:
```heex
<.button phx-click="save">Save</.button>
<.task_card task={@task} />
<.agent_tree agents={@agents} />
<.flash kind={:warning} flash={@flash} />
```

### Flash Component Kinds
The `flash/1` component in `CoreComponents` supports three kinds:
| Kind | CSS Class | Icon | Use Case |
|------|-----------|------|----------|
| `:info` | `alert-info` | `hero-information-circle` | Success messages, informational notices |
| `:error` | `alert-error` | `hero-exclamation-circle` | Error messages, validation failures |
| `:warning` | `alert-warning` | `hero-exclamation-triangle` | Warnings, missing configuration alerts |

## Constraints

- Every component module **must** use `use EvoDashWeb, :html`.
- Styling is Tailwind CSS + daisyUI — no inline CSS or external stylesheets in components.
- Icons use the Heroicon set via the `icon/1` component (naming convention: `hero-<name>[-style]`).
- Layout templates live under the `layouts/` subdirectory; only the root layout exists currently.
- Components are pure functions — they receive assigns and return HEEx markup; no side effects or direct LiveView process calls.
- Shared helpers from `EvoDashWeb.Helpers` should be used for cross-component utility functions (status badges, formatting, etc.) to avoid duplication between the dashboard component modules (`ProjectComponents`, `TaskFormComponents`, `TaskCardComponents`, …) and `AgentsComponents`.
- **Task cancellation UX (graceful + force kill)** (`task_card_components.ex`): the inline Cancel button is GRACEFUL — `phx-click="open_cancel_modal"` (btn-outline neutral/info, NO `phx-confirm` — the modal confirms), visible `[:pending, :running]` only (NOT `:finalizing`/`:cancelling`). The three-dot dropdown adds a `text-error` "Force kill" item (`phx-click="open_force_kill_modal"`, visible `[:running, :cancelling]` = escalation, no `phx-confirm`) with an `<li class="menu-title">` `gettext("Danger zone")` divider above Delete (Delete keeps its `phx-confirm`). The `:cancelling` status renders as IN-PROGRESS (NOT terminal): badge shows a WARNING-amber pulsing dot + `gettext("Cancelling…")` label via a `cond` clause; `status_accent_color(:cancelling)` and `task_card_tint(%{status: :cancelling})` resolve through the shared `EvoDashWeb.Helpers.task_status_dot_class/1` / `task_status_tint/1` (busy = warning family — see `lib/evo_dash_web/CONTEXT.md` theming map). `layouts.ex` sidebar: `:cancelling` is included in the `is_running` check, both `Enum.split_with` running lists AND `task_status_dot_class(%{status: :cancelling}) → "bg-warning"` so cancelling tasks keep the running presentation (link to /agents, pulse animation, elapsed time). The sidebar has NO cancel button (task links only) and deliberately NO force-kill surface — the Tasks page dropdown is the force-kill surface.

### `try/rescue` Policy

`try/rescue` is normally an anti-pattern in Elixir. Within these component files:

- **Do NOT** wrap `String.to_existing_atom/1` in `try/rescue`. When normalizing potentially untrusted DB-sourced data (e.g., agent archive maps after a Jason.decode round-trip), use an explicit **whitelist map lookup** (`@known_agent_keys` in `normalize_agent_keys/1`) with `Map.get/3` defaulting to the original key. This avoids dynamic atom creation AND avoids try/rescue.
- If any new `try/rescue` is introduced, it MUST include a clear inline comment explaining why it is justified.

## Design Notes

### Settings Quick Setup — credentials gated on model selection (`settings_components.ex` + `settings_live.ex`)
The LLM Quick Setup is a **select-then-save** flow — provider buttons → variant buttons → model buttons (`phx-click="select_llm_model"`, sets `selected_model_string` assign; highlight `btn-primary shadow-md`), and only THEN do the credential inputs render:
- **Save Model form** (`phx-submit="save_quick_setup"`, hidden `provider_id` + `variant_id` + `model_string` + submit button `gettext("Save Model")`) renders ONLY when `@selected_model_string != nil`; its base_url input renders ONLY when `EvoGit.Config.LLMCatalog.requires_base_url?(@selected_provider_id)` is true (catalog function — the `provider[:requires_base_url]` map field is DEAD). The `save_quick_setup` handler (model_profile_events.ex) already defaults missing/empty base_url to `opts = []`.
- **API Key form** (`phx-submit="save_api_key"`) renders when `show_custom_input or @selected_model_string != nil` — custom-model providers (OpenRouter/OpenAI-Compatible) keep it immediately after provider selection (their `save_custom_model` form IS the model-selection step); preset providers get the hint `gettext("Select a model above to configure credentials.")` until a model is picked.
- `select_llm_provider` and `select_llm_variant` RESET `selected_model_string` to nil. The `select_llm_model` handler is whitelist-safe via `llm_model_string_known?/2` (validates the model_string against the selected provider's rendered shortcut buttons — same `resolve_provider_atom/2` resolution). `select_llm_model_shortcut` is a dead-but-harmless handler (no button fires it).

### Settings two-column layout — md+ independent scroll + config-path footer (`settings_live.ex` + `sidebar.ex`)

The Settings page (`SettingsLive.render/1`, `live/settings_live.ex`) is a bounded-height two-column layout on md+ (`≥ 48rem`); below md it stacks naturally and the whole page scrolls via the app shell's `#main-scroll`.

- **Height chain (md+)**: `#main-content` (layouts.ex `app/1`) is `flex-1 min-h-0` inside the `h-dvh` app shell, so the settings root wrapper (`flex flex-col md:h-full`) is bounded to viewport-minus-chrome; the two-column row (`flex flex-col md:flex-row md:flex-1 md:min-h-0`) takes the remaining height. The three warning banners (config-status / remote-config-error / no-LLM) are direct `shrink-0` children of the wrapper above the row — they never squish, and the row absorbs the leftover space.
- **Columns scroll independently**: the sidebar container (`settings_sidebar/1` in `sidebar.ex`) is `md:h-full flex flex-col` with a pinned search box + pinned footer and a `flex-1 overflow-y-auto min-h-0` category `<nav>`; every content column is a DIRECT flex child of the two-column row and must be height-bounded by the flex algorithm. Two height rules govern the columns:
  - NEVER `h-full` on a content-column root (percentage against a flex-sized row → content height → page inflation), AND
  - ⚠️ **every main-axis (column-direction) NON-scroll flex item between the bounded column and the inner scroll container MUST carry `min-h-0`** — flexbox's content-based automatic minimum size: a non-scroll flex item defaults to `min-height: auto` and cannot shrink below its content height, which INCLUDES the overflow content of a nested `overflow-y-auto` div → the intended inner scroll body never engages and the content spills up to `#main-scroll`, scrolling the whole page with both panes LINKED. Every save-capable category pane follows ONE unified shape: root column (`settings_components.ex:72`, `flex-1 flex flex-col min-w-0 min-h-0 bg-base-100`, `id="category-<cat>"`) → pinned `.section_header` top sibling → `flex-1 overflow-y-auto px-8 py-8 relative` scroll body (owns the pane's scrolling) → ONE minimal pinned bottom-right `.save_bar` (`shrink-0`, compact `px-8 py-2.5`, translucent backdrop + top hairline) as a NON-scrolling flex sibling that stays visible while the content scrolls beneath it. In `category_section/1`'s generic `:else` branch the chain is root → `save_category` form → scroll body → bar, and BOTH main-axis non-scroll intermediates carry `min-h-0`: the root div (`settings_components.ex:72`) and the form (`settings_components.ex:602-607`, `flex-1 flex flex-col min-w-0 min-h-0 relative`); the inner scroll body (`settings_components.ex:611`, `flex-1 overflow-y-auto px-8 py-8 relative`) then engages and owns the scrolling. The `:llm` category follows the SAME shape: its flat `settings-form-llm` form (`settings_components.ex:565-585`) + flat cards stay INSIDE the scroll body while its save bar (`settings_components.ex:594-597`) is a direct child of the root column OUTSIDE the form, submitting it via the `form="settings-form-llm"` HTML form-association attribute — no nested forms, the five sibling LLM forms remain valid.
  Safe shapes that need no `min-h-0` on the column itself: (a) the scroll container IS the column (search form, `:remote_connections`, `:agents` — `overflow-y-auto` on their `flex-1 flex flex-col min-w-0` wrapper; the search-results panel rendered inside the search form is scroll CONTENT, not a bounded column — `flex-1 flex flex-col min-w-0 bg-base-100`, never `h-full`; the SearchResults save bar renders inside that same `overflow-y-auto` `settings-form-search` form, where its `sticky bottom-0` classes genuinely pin it to the form's visible bottom), or (b) the scroll container is a DIRECT child of the column (`:llm` — the `flex-1 overflow-y-auto` scroll body at `settings_components.ex:88` is a direct child of the column root, the flat `settings-form-llm` form sits inside it, and the pinned `.save_bar` follows as a `shrink-0` sibling; a scroll container's own automatic min size is 0). The regression test (`describe "md+ independent scroll layout contract"` in `settings_live_test.exs:3361`) pins the contract: for each generic category (scheduler/sandbox/tools) it asserts `min-h-0` on BOTH the category root and the `save_category` form, refutes `h-full` on them, and requires the inner `.overflow-y-auto` scroll body — GREEN with the current classes.
- **Config-path footer**: the old content-area "Configuration file" header (guarded to hide on `:remote_connections`/`:agents`) is GONE. The config path lives ONLY in the sidebar footer (`sidebar.ex`, after `</nav>`): document icon + `gettext("Configuration file")` label + truncating `<code>` + the copy button with the test-pinned DOM contract `id="settings-config-path-copy"` / `phx-hook="ClipboardCopy"` / `data-content={config_path}`, plus `gettext("File does not exist yet")` when `not config_file_exists`. The sidebar component declares `attr(:config_path, :string, required: true)` and `attr(:config_file_exists, :boolean, required: true)` — SettingsLive passes both assigns. The footer renders for EVERY category (sidebar is outside the category-specific content).
- The md+ height chain means `mix tailwind evo_dash` output must contain `md:h-full` / `md:flex-1` / `md:min-h-0` / `overflow-y-auto` / `min-h-0` (gitignored CSS build — rebuild after utility-class edits, see the comment at `settings_live.ex` render).

### Task Form — Single-Card Two-Layout Design (`task_form_components.ex`)

The dashboard launch panel is ONE card (`.input-card`) containing the objective textarea AND the bottom toolbar (`.input-controls`) as its last element — in normal document flow, never `position: fixed`. The layout is **server-seeded at render + client-driven**: `EvoDashWeb.TaskFormComponents.layout_for/1` computes the initial `data-layout` on `.input-layout` from the `@prompt` attr. There is NO per-keystroke server event for the objective textarea (no `phx-change`/`phx-debounce` — `@task_prompt` is updated ONLY by `restore_state` and `task_submit`; because the textarea carries `phx-update="ignore"`, re-renders never clobber the user's typing; prompt draft persistence is purely client-side via the StatePersistence input watcher in app.js, 300ms debounce). The **AdaptiveInput JS hook** mirrors `layout_for/1`'s thresholds client-side and flips `data-layout` directly while typing (it also autogrows the textarea by measuring content height), AND it re-asserts the computed layout whenever the server re-seeds the attribute: a MutationObserver on `.input-layout` (attributeFilter: ['data-layout']) re-runs the computation after any server re-render (e.g. toggling mode/model), so the layout can never snap back to compact while a long prompt remains in the box; it converges immediately (the hook only writes the attribute when the computed value differs) with zero network events. It does NOT position any floating panel (no `--input-layout-center` / `position: fixed` logic).

- **Threshold**: `@short_objective_threshold 1200` — `:expanded` when `String.length(prompt) > 1200` graphemes OR the prompt has > 32 explicit lines (`line_count/1`, newline-split); otherwise `:compact`. Non-binary input → `:compact`. Public API: `layout_for/1` (pure, unit-tested).
- **Layout A — `data-layout="compact"`**: unified objective box; the textarea auto-grows up to ~16 lines and flips to expanded the instant content would exceed the cap.
- **Layout B — `data-layout="expanded"`**: the textarea fills the card (`flex: 1`, internal `overflow-y: auto`, NO max-height cap — the card's `overflow: hidden` flex containment bounds it on any viewport height), keeping the in-flow bottom toolbar pinned to the card's bottom regardless of prompt length.
- **Bottom toolbar — same DOM order AND visual order in both layouts**: `.input-controls` (ChatGPT/Gemini input-box style) holds ALL controls in ONE row at the card's bottom: the attach-kind dropdown FIRST (bottom-LEFT), then free space, then a RIGHT-ALIGNED cluster — mode select → (custom-agent select, only when `@custom_agents != []`) → model select (only when `@model_profiles != []`) → the circular icon-only send button LAST (the cluster's last element = the row's far right; it has no auto margin of its own — plain DOM order, no `order-*` overrides, no `mx-auto` centering). The row's auto-margin lives on the MODE select's `ml-auto`, which absorbs the free space after the attach control and packs the cluster at the right edge with the uniform flex `gap` between members. The selects use the compact `select-sm text-sm` scale with `min-w-0 truncate max-w-*`; the attach control is a native `<details>` dropdown whose `<summary>` trigger is `btn btn-ghost btn-sm btn-square` with a `hero-plus` icon (see the Attach-kind dropdown bullet below). Pinned via Floki class checks + a DOM-order test in the component test.
- **Attach-kind dropdown (bottom-LEFT — the FIRST element of `.input-controls`)**: a native `<details class="dropdown dropdown-top" phx-hook="FilePicker">` (`id="objective-file-attach"`) whose `<summary id="objective-file-button">` is the visual "+" trigger (`btn btn-ghost btn-sm btn-square` + `hero-plus` icon; `aria-label`/`title` = gettext("Attach file")) — the summary id and labels are the stable test/UI markers.
- The `FilePicker` JS hook lives on the `<details>` and click-delegates the menu's `[data-picker-kind]` items — menu buttons carry NO `phx-click`, and every one is `type="button"` (inside the task form a bare button would submit).
- On pick the hook closes the details and pushes the `"file_pick"` event carrying the item's `data-picker-id`.
- Menu = one `ul.menu.menu-sm.dropdown-content` with exactly three `<li><button type="button" data-picker-kind="…" data-picker-id="…">` items: `text` / `objective_file` (the UNCHANGED text-attach pipeline — native dialog + picked text appended to the objective), `image` / `objective_file_image`, `audio` / `objective_file_audio` (the last two stage the file server-side into `@staged_attachments`).
- `dropdown-top` opens the menu UPWARD — `.input-card`'s `overflow: hidden` flex containment (expanded layout) would clip a downward-opening menu below the card.
- The manual fallback `#objective-file-manual` stays a DIRECT child of `.input-controls`, a sibling right after the `<details>` — the hook locates it via `closest(".input-controls")`, and its absolute-position anchor is the toolbar.
- **Staged media attachments (image/audio)**: image/audio picks stage server-side into the OPTIONAL `@staged_attachments` assign (seeded by ProjectsLive; the component renders without it via `Map.get(assigns, :staged_attachments) || []`) — a list of STRING-keyed maps `%{"type" => "image"|"audio", "name" => basename, "media_type" => IANA, "data" => raw binary}` (defensive string/atom-key reads via the private `attachment_attr/2`). The attr IS declared on `task_form/1` — `attr(:staged_attachments, :list, default: [])` (required to silence Phoenix's undefined-attribute warning) — and both `task_form/1` call sites in projects_live.ex (local + remote form zones) forward `staged_attachments={@staged_attachments}`, so the chip row renders in the LiveView.
- The chip row (`id="staged-attachments"`) renders ABOVE `.input-controls` inside the card in a normal (non-`phx-update="ignore"`) re-render region so staging appears immediately; each chip shows a kind label (gettext("Image")/gettext("Audio") via the private `staged_kind_label/1`) + the basename + a remove button (`type="button"`, `phx-click="remove_staged_attachment"`, `phx-value-index={index}`).
- Chip rendering is METADATA ONLY — the `"data"` raw binary is NEVER rendered nor encoded into any attribute/event.
- **Circular icon-only send button**: `button#task-launch-button` (`type="submit"`, the `id` is a stable test/UI marker that survives icon swaps) is `btn btn-primary btn-circle btn-sm shrink-0` — a small filled circle with `<.icon name="hero-arrow-up" class="size-4" />` and NO visible text label (ChatGPT-send style); `aria-label`/`title` = `gettext("Launch")`. It carries `data-mode={@mode}` — driving the per-mode hover ring color via CSS keyed on `.input-controls button[data-mode="..."]:hover` (in `assets/css/app.css`) — and `data-resume` (resume-ring variant); `task_change` re-renders the button on mode change, keeping the attribute in sync. It is the cluster's LAST element with NO auto margin — its far-right position comes from the mode select's `ml-auto`, not from the button itself.
- **CSS**: the class names are `.input-layout` / `.input-card` / `.input-prompt` / `.input-controls` (used by the hook's `closest(".input-layout")` and the CSS selectors). The accent decorations — accent border-color, layered box-shadow glow, and the `::before` top-edge gradient — are defined on the base `.input-card` rule and apply to BOTH layouts; layout-specific sizing/containment stays in the `[data-layout="compact"]` / `[data-layout="expanded"]` overrides. `.input-prompt` uses uniform small padding (`1.25rem 1.25rem 0.75rem`) — no top "clearance" (the attach trigger no longer floats over the textarea). `.input-controls` is the in-flow bottom toolbar: `position: relative` (anchor for the `.file-manual` popover), flex `nowrap` with `gap`, and NO divider under the textarea (the post-change design has no `border-top` hairline); the row's auto-margin lives on the mode select's `ml-auto`, which absorbs the free space so the trailing cluster (mode | agent | model | send) packs at the right edge (no `space-between`, no `mx-auto`). The hidden `.file-manual` manual-path panel is `position: absolute; bottom: calc(100% + 0.4rem); left: 0.75rem` — a popover just above the toolbar's left side near the "+" trigger, nudged off the container edge (the exact offset lives in app.css, changed in the sibling assets pass). CSS lives in `assets/css/app.css` "Adaptive Input Layout" section (sibling node — see `./assets/`).
- Tests assert the bottom-toolbar contract: the attach-kind dropdown first inside `.input-controls` (summary `#objective-file-button` + the three `[data-picker-kind]`/`[data-picker-id]` menu items), mode/agent/model selects in DOM order (compact `select-sm` classes, `min-w-0`/`truncate`), the row's `ml-auto` on the MODE select (it leads the right-aligned cluster), the circular `#task-launch-button` LAST with NO auto margin (`btn-circle`, `hero-arrow-up`, `aria-label`/`title` = gettext("Launch"), `data-mode`), plus `type="submit"`, `disabled={@disabled}`, mode-option values/order, and the disabled welcome overlay. The send button's `aria-label` reuses the existing `gettext("Launch")` msgid (no new translatable string). Note: `task_form_components_test.exs` DOM pins for the attach area are updated by this feature's dedicated test pass — interim failures of the stale pins are expected and tracked, not regressions.
- **Mobile keyboard-aware heights**: the app shell + input layout use dynamic-viewport heights on touch viewports — `h-dvh` on `body`/`#app-layout`/`#sidebar` (`layouts/root.html.heex`, `layouts.ex`), the viewport meta carries `interactive-widget=resizes-content`, and the AdaptiveInput hook syncs `--app-vh` from `window.visualViewport` so the `@media (max-width: 640px)` overrides in `assets/css/app.css` (app-shell height + 6-line expanded textarea floor) track the area above the virtual keyboard.

### Foreign Repos — writable + base_sha + edit-in-place (`project_components.ex`)

The project-settings foreign-repo editor implements the **multi-repo read-write** foreign repos UI. The core type is `%EvoGit.Core.ForeignRepo{}` (`{id, root, description, writable: false, base_sha: nil}` — lives in `:evo_git`, do NOT re-implement; the file aliases `EvoGit.Core.ForeignRepo`).

- **Add form** (`project_settings_body/1`, `phx-submit="add_foreign_repo"`): existing `repo_id`/`path`/`description` fields plus a `writable` checkbox (`name="writable"` `value="true"`, default unchecked) and an optional `base_sha` input (starting commit, blank = HEAD). Tooltips: writable repos get their own worktree + `evogit-agent-*` branch and are tracked in the task report but NEVER merged by the task; base_sha = branch or SHA, blank = HEAD. New user-facing strings are gettext-wrapped with zh_CN meaning-anchoring comments ("Writable", "branch or SHA, blank = HEAD").
- **Repo-list rows**: `badge badge-warning badge-sm` "Writable" badge when `repo.writable`; `repo.base_sha` rendered in a `<code>` (`text-xs font-mono`) when non-nil. Each non-primary row gains an edit button (pencil icon, `phx-click="edit_foreign_repo"` + `phx-value-repo_id={repo.id}`) next to the existing remove (trash) button — same `opacity-0 group-hover:opacity-100` hover-reveal wrapper.
- **Edit-in-place**: when `Map.get(assigns, :editing_foreign_repo_id) == repo.id` the row body swaps to an inline form (`phx-submit="save_foreign_repo"`): read-only `repo_id` input (`readonly` — still submitted with the form), editable `path`/`description`, `writable` checkbox, `base_sha` input, Save (`btn btn-primary btn-xs`) + Cancel (`phx-click="cancel_edit_foreign_repo"`, `btn btn-ghost btn-xs`). Stays inside the same card container.
- **Defensive assigns** (seeded by ProjectsLive; the component must render without them — component tests build assign maps manually): `new_repo_base_sha` (default ""), `editing_foreign_repo_id` (default nil), `foreign_repo_edit_form` (default nil — map `%{description:, writable:, base_sha:}`, may also carry `path`/`repo_id`). ⚠️ **`Map.get(nil, ...)` raises `BadMapError`** — normalize first: `edit_form = Map.get(assigns, :foreign_repo_edit_form) || %{}`, then `Map.get(edit_form, :path, repo.root)`-style reads fall back to the repo struct's own values.
- **Event contract** (handled by ProjectsLive — the LiveView half of this feature): `edit_foreign_repo` / `save_foreign_repo` / `cancel_edit_foreign_repo`; the existing `add_foreign_repo` / `remove_foreign_repo` handlers now also receive `writable`/`base_sha` params.
- **Do NOT add `phx-click` to the `foreign-repo-path-browse-button`** (it carries only `phx-hook="DirectoryPicker"` — see the double-fire Known Issue below).
- **File-size policy**: `project_components.ex` is deliberately EXTENDED (not split) past the nominal per-file line-count policy to carry the foreign-repo add/edit UI — splitting is deferred so this components half merges cleanly with the parallel ProjectsLive-handler half of the feature; a future split into a `project_components/` subdirectory is the sanctioned refactor. Component tests for this UI: `test/evo_dash_web/components/project_components_test.exs` ("foreign repo writable/base_sha UI" describe block).

## Known Issues

### Command Palette (`project_omnibox`) — client-side wiring

- **Keyboard**: `phx-keydown="palette_keydown"` lives on the search input (`input#palette-search-input` in `palette_menu/1`). Phoenix LiveView's keydown handler fires ONLY when the event TARGET (the focused element) itself carries the binding — it does NOT walk ancestors — so the binding must be on the search input, not the overlay div. The search input carries both `phx-keydown="palette_keydown"` and `phx-change="palette_search"` (they coexist; the server no-ops printable keys via `handle_palette_key/3` catch-all). The `:open_path` mode path input keeps its own `phx-keydown` + `phx-key="Escape"`. Do NOT add `phx-window-keydown` — `handle_palette_key(socket, "Enter", :menu)` would activate a project on ANY Enter press page-wide (no `@palette_open` gate server-side).
- **Click-outside**: `phx-click-away="close_project_palette"` is on `.project-palette-overlay` — clicks anywhere outside the overlay (including the sidebar, which paints above the fixed backdrop) close the palette. The backdrop's `phx-click="close_project_palette"` remains as belt-and-braces.
- **Overlay backgrounds**: the `--b1/--b2/--b3` DaisyUI legacy variable namespace does not exist in the vendored DaisyUI 5 themes (only `--color-base-*` etc. are defined) — the `.project-palette-overlay` background rules in `app.css` use `color-mix(...)` / `var(--color-base-*)`.
- **Backdrop is viewport-relative**: `.dashboard-topbar` has NO `backdrop-filter` — a `backdrop-filter` would create a containing block that traps the fixed-position backdrop/overlay (descendants) to the topbar strip. Without it, the fixed-position backdrop/overlay are viewport-relative.
- **Latent focus bug (open, benign)**: the `FocusInput` hook (`assets/js/app.js`) re-focuses on `updated()` only when the element HAS class `focus-on-update` (inverted intent), but no markup adds that class. Benign while morphdom preserves input focus; if a full element replacement ever loses focus, flip the check or add the class to the search input.
- **Regression guard**: `test/evo_dash_web/components/project_components_test.exs` asserts the search input's `phx-keydown`, the overlay's `phx-click-away="close_project_palette"` (and the backdrop's matching `phx-click`), and trigger rendering/typography.

### Sidebar must stay `overflow-visible` — SSH node-selector dropdown clipping

The app shell sidebar (`EvoDashWeb.Layouts.app/1`, `layouts.ex`) is a `z-50` stacking context above the main content (`#main-scroll` is `z-0`), so its overflowing dropdown menus paint above the main body. The sidebar must never clip them: the `SidebarCollapse` hook (`assets/js/hooks/sidebar_collapse.js`) now adds `overflow-visible` in BOTH the collapsed and expanded branches of `applyCollapsed/1`, and the `<aside id="sidebar">` additionally carries `overflow-visible!` (Tailwind v4 important modifier) as defense-in-depth — removing either clips the SSH node-selector dropdown (`w-72` = 288px) at the sidebar edge (`w-60`/`w-16` = 240px/64px). If the hook is ever changed again, keep the pin; it can relax to plain `overflow-visible` only when the hook is trusted to own overflow in every branch. Note: the long comment above `<aside id="sidebar">` in `layouts.ex:98-111` still claims the hook re-applies `overflow-hidden` on expand — STALE since commit 37cbed215; do not act on it.

### Sidebar bottom-bar dropdowns never auto-close (native `<details>`)

All three sidebar bottom-bar dropdowns are DaisyUI `<details>`-based dropdowns: `theme_toggle_compact/1` (`layouts.ex:553`), `language_selector/1` (`layouts.ex:732`), and `EvoDashWeb.NodeSelectorComponent` (`../live/components/node_selector_component.ex:25`). Native `<details>` semantics: the panel opens/closes ONLY when its `<summary>` is clicked — clicking elsewhere on the page does NOT close it, and `SidebarCollapse.applyCollapsed/1` toggles only classes and never removes `open` from a `details[open]`, so collapsing the sidebar leaves an open dropdown floating over the content (overflow-visible means it is not clipped, it just stays open). After picking a THEME the dropdown also stays open (`phx:set-theme` in `layouts/root.html.heex` only re-applies theme — no reload, no server event); picking a LANGUAGE reloads the page (`phx:set-locale` → `window.location.reload()`), which incidentally closes it. There is NO reusable outside-click-close infra: app.js has no generic `details`-close or click-away handler (its only window-level click listener is dev-only live-reload), and the old CoreComponents `toggle_dropdown`/`close_parent` JS commands do not exist. The Projects-page Configure dropdown is a different, page-local server-driven idiom (`toggle_configure_dropdown`/`close_configure_dropdown` in `projects_live.ex`: CSS-show/hide of always-in-DOM content + a click-catcher overlay) — not applicable to the global sidebar without per-page handlers. Minimal fix direction (owned by `apps/evo_dash/assets/`): in the SidebarCollapse hook, close `sidebar.querySelectorAll('details[open]')` when collapsing, plus a document-level outside-click listener that closes open sidebar `details` — no markup/CSS changes needed, and the `<details>` panels then behave like standard dropdowns.

- **z-index contract** (do not disturb): sidebar `<aside>` `z-50`; main content `#main-scroll` `z-0` (stacking context, traps page modals below the sidebar); `.dropdown-content { z-index: 50 !important }` (app.css, keeps dropdown panels above sidebar siblings); mobile overlay `z-40`; config warning banner `z-40`; mobile hamburger `z-50`. Lowering the sidebar below `z-50` or raising `#main-scroll` above `z-0` re-breaks the stacking.
- **`simple_nav` mode** (WelcomeLive) renders branding **only** — the node selector lives INSIDE the `if !@simple_nav` block (bottom bar), so it is hidden in simple_nav mode along with the nav links, active-tasks section, and bottom bar; `overflow-visible` is safe there too.

### `Layouts.app` content background lives on the scroll container (`#main-scroll`)

The page content background is `bg-base-100` on **`#main-scroll`** (`layouts.ex` ~line 255) — the ACTUAL scroll container (`flex-1 overflow-auto` inside the fixed `h-dvh` chain `body` / `#app-layout` `bg-base-200` → `#desktop-quit-hook` `flex-1 min-h-0`). It must stay there: a scroll container's own background paints its visible box at every scroll offset, whereas a background on the non-scrolling inner `main#main-content` only paints the first viewport and would reveal the shell's `base-200` as a bottom seam when page content exceeds one viewport (System/Settings-style page-scrolled pages). `main#main-content` (`flex-1 min-h-0`, ~line 267) keeps a redundant same-color `bg-base-100` (paints the initial viewport) and MUST keep `min-h-0` — main being viewport-capped is what makes Projects' expanded task-form pin-to-bottom and Home's internal chat scroll work. Keep the two background utilities in sync with any theme/background changes (Adwaita layering: chrome `base-200`, content/view `base-100`).

### `phx-click` + JS hook on the same element — double firing

`phx-click` + a JS hook (`phx-hook`) on the SAME element sends BOTH the hook's handling AND the `phx-click` event to the server. Don't put both on one element unless a server-side `handle_event/3` clause exists.

The three directory-picker "Browse" buttons in `project_components.ex` (`project-path-browse-button`, `new-project-location-browse-button`, `foreign-repo-path-browse-button`) carry ONLY `phx-hook="DirectoryPicker"` — the hook fully owns the click: it pushes a `"directory_pick"` event to the server (ProjectsLive runs the picker via `EvoDash.DirectoryPicker` and pushes the result back as `picker_result:<picker_id>`). Do NOT add `phx-click` to these buttons — a `"pick_directory"` event has no matching `handle_event/3` clause → `FunctionClauseError` → LiveView crash. Regression guard: `test/evo_dash_web/components/project_components_test.exs` asserts the buttons keep the hook and have no `phx-click`.

**Remote-node gating**: the same three Browse buttons are ALSO gated on `!@remote` — the render condition is `@tauri_detected and !@remote`, so when the dashboard views a REMOTE node (`?node=` param) the native picker buttons are hidden and the manual path inputs (`project-path-input`, `new-project-path-input`, `foreign-repo-path-input` with PathAutocomplete) remain the fallback. Rationale: the picker runs on the LOCAL dashboard machine, so a remote pick is meaningless (the backend already pushes `picker_result:<id> %{unavailable: true}` for `directory_pick` on remote nodes). The `remote` attr is threaded from `project_omnibox/1` into `palette_open_path`/`palette_new_project` and from `RemoteView.top_bar/1` into `project_settings_tab/1` (via the shared `project_settings_body/1`); `project_settings_panel/1` carries the attr for consistency (no callers).

## Task Cards & the Summary-Map Contract

The dashboard's sidebar "Active Tasks" section in `EvoDashWeb.Layouts.app` is fed by the `@running_tasks` / `@pending_tasks` assigns — lightweight **summary maps** from the statuses-filtered `EvoGit.TaskRegistry.list_tasks_summary([:running, :pending, :finalizing, :completed])`. `layouts.ex` declares only `attr(:running_tasks, ...)` / `attr(:pending_tasks, ...)` — there is NO `@tasks` attr (the dashboard renders no main task list). Full `%TaskInfo{}` structs (heavy `logs`/`usage`/`archive_metadata` columns) are used only on the Tasks page (TasksLive paginated full loads) and the Review page.

### The summary-map contract

Plain maps with keys: `id, status, review_status, result, started_at, finished_at, type, project_path, opts, branch_name, model_id, agent_count, base_sha, commit_sha, lease_expires_at`. `opts` is a decoded keyword list (same as `TaskInfo.opts` — includes objective/prompt/mode). EXCLUDED: `logs`, `usage`, `archive_metadata` (and all other TaskInfo fields like `ref`).

### Field-access rules for task-card components

- **Dot access (`task.field`) is safe ONLY for contract keys.** On a plain map, a missing key raises `KeyError`; on a struct it returns nil. So any dot access to a non-contract key crashes once a summary map is fed in.
- **`Map.get(task, field)` is safe for any key** (returns nil when absent).
- **Non-contract heavy fields** (`logs`, `usage`, `archive_metadata`) must be guarded: `Map.get(task, :logs) not in [nil, []]`, `Map.get(task, :usage)`, `Map.get(task, :archive_metadata) not in [nil, []]`. Guarded sections simply hide on summary maps (correct — the dashboard sidebar never renders these fields) and render on full structs.
- **Sidebar grouping is deliberately TOTAL against non-map entries** — `group_tasks_by_project/2` (`layouts.ex`) filters the combined `running_tasks ++ pending_tasks` list to `is_map/1` entries BEFORE `Enum.group_by/3`, so a stray non-map element (possible because `@running_tasks`/`@pending_tasks` are seeded from the shape-agnostic `EvoDash.ActiveTasks` hub) is omitted from the sidebar instead of crashing the page with `Map.get(:c, :project_path, nil)` → `BadMapError`. Do NOT remove this filter (the row loop at ~L195 then only ever sees maps).

### Task detail view — flattened Objective / Agent Message cards (expanded card + zoom modals)

The expanded task card detail view (`@show_details`, TasksLive only) shows **two direct cards** in a 2-column grid (no nested card level):

- **Objective card** (always, when an objective text exists): header `gettext("Objective")` + copy button (`id="task-<id>-objective-copy"`, `phx-hook="ClipboardCopy"`, `data-content={objective_text(@task.opts)}`) + "Full" button (`view_full_options` → Full Objective zoom modal). Body = `render_options(@task.opts)`: FULL objective text (no truncation) in a `max-h-48 overflow-y-auto` scrollable container + mode/path badges.
- **Agent Message card** (guarded `Map.get(@task, :result)`): header `gettext("Agent Message")` + copy button (`id="task-<id>-result-copy"`, `data-content={result_copy_text(@task.result)}`) + "Full" button (`view_full_result` → Full Result zoom modal). Body = `render_result(@task.result)` — full content, scrollable; preserves the commit_sha/branch_name/tag/pr_url badges, the No Changes notice, and the Error/Crashed states (their sub-headers remain inside the body).

Public pure helpers added for this: `objective_text/1` (`(opts[:prompt] || opts[:objective] || "") |> String.trim()` — shared with the collapsed-card preview) and `result_copy_text/1` (success/no-changes binary result → the string; `{:error, _}`/`{:exit, _}` → `inspect(reason, limit: :infinity)`; fallback → pretty inspect). Both are reused by the zoom modals in `tasks_live.ex` (Full Result modal `id="full-result-copy"`, Full Objective modal `id="full-options-copy"`, both in the `<:actions>` slot). The zoomed (`truncate: false`) branches keep `max-h-[70vh] overflow-y-auto`; TasksLive has the `"copied"` → "Copied to clipboard" flash handler (required for the ClipboardCopy hook push).

**Failed/error-result rendering — every `{:error, reason}` reason is INSPECT-d, never raw**: `render_result({:error, reason}, opts)` (task_card_components.ex:688) and `{:exit, reason}` (:717) assign `reason: inspect(reason, limit: :infinity)` (:701/:730) and render it in a red "Error"/"Crashed" `<pre>` box (both in the expanded Agent Message card AND the Full Result modal via `render_result_full/1` → `truncate: false`); `result_copy_text({:error, reason})` (:995) and `{:exit, reason}` (:996) inspect the same way (test-pinned: `result_copy_text({:error, "boom"}) == ~s("boom")` — string reasons come out QUOTE-WRAPPED, atoms like `:killed` as `":killed"`). Any non-tuple/map value falls to the catch-all render clauses (:956/:998) which pretty-inspect the whole term. **Tuples never survive the Store Codec round trip**: `EvoGit.Store.Codec.encode_result({:error, reason})` falls back to storing `inspect(reason)` when `Jason.encode` fails on non-JSON-safe reasons (tuples/pids — `apps/evo_git/lib/evo_git/store/codec.ex:413-424`), so a `{:error, {128, "fatal: ..."}}`-style term arrives at the card as `{:error, "{128, \"fatal: ...\"}"}` (reason is the INSPECT STRING) and the render/copy inspect then adds another quote/escape layer — the user sees a machine-ish escaped dump, not the plain inner text. Only plain-string reasons (JSON-safe) survive verbatim, and even those render quote-wrapped. Consequence for core error-term design: persist a human-readable STRING as the reason (readable if quote-wrapped), not a nested tuple (raw dump + leaks the repo path). Task-card surfaces showing a failed task's result are ONLY TasksLive: collapsed card shows nothing (result_repos/1 returns nil for error/exit), expanded card's Agent Message section (guarded truthy `Map.get(@task, :result)`), its Copy button, and the Full Result modal (`@selected_result` from `ModalHelpers.view_full_result/2`, guarded `if @selected_result`). Browser notifications are a separate surface that also inspects the reason (`projects_live/project.ex` `task_notification_content/1` → `{"Task failed", inspect(reason)}`).

### Multi-repo `repos` result rendering (per-repo commit/branch)

Task results (the runtime `{:ok, %{...}}` value inside `result`) may carry a top-level `repos` map — STRING keys: `%{repo_id => %{"commit_sha" => sha, "branch_name" => branch | nil}}`, `"primary"` ALWAYS present (branch_name nil when the primary produced no changes), writable foreign repos with commits present, read-only repos ABSENT. After the Store Codec round trip the top-level `"repos"` key stays STRING-keyed (unknown result keys are never atomized — `EvoGit.Store.Codec.decode_result_data/1`), so readers must handle both atom and string keys.

- **`result_repos/1`** (public pure helper) — normalizes the result VALUE (accepts `{:ok, %{...}}` or the data map directly) into a deterministic list `[%{id:, commit_sha:, branch_name:}]` — `"primary"` first, foreign repos sorted by id. Returns `nil` for legacy results / error/crash results / summary maps without a usable `repos` map, so callers guard with `if repos = result_repos(...) do` and render NOTHING (legacy behavior byte-identical). Non-map entries dropped; inner keys read via `Map.get(entry, :commit_sha) || Map.get(entry, "commit_sha")` (same for `branch_name`).
- **`result_repos_badges/1`** (function component, `attr :repos` + `attr :size_class` default `"text-xs"`) — one compact badge row per repo: `id:` (badge-ghost mono) + short sha (first 7 chars, badge-ghost mono) + branch badge (badge-primary mono) when present. A nil branch renders just the sha, plus a muted `gettext("no changes")` hint for the `"primary"` repo only (its nil branch means no primary commits; a foreign repo's nil branch means branch-creation failure, NOT no-changes).
- **Render sites** (all `Map.get`-guarded, summary-map safe): (1) collapsed task card — compact indicator row between the objective and the bottom row, hidden when `@show_details` (the expanded detail already shows the full section — no duplication); (2) expanded detail view — a `gettext("Repositories")` h5 section inside `render_result/2` for both the success-with-commit_sha clause AND the `no_changes: true` clause (a no-changes primary can still carry foreign commits); (3) the Full Result zoom modal (`render_result_full/1` → `truncate: false` branch). TasksLive itself never reads `repos` — it passes the full decoded `result` through, and the component stays contract-safe.
- **File-size note**: `task_card_components.ex` grew from 869 to ~1024 lines with this feature — deliberate (extend, don't split; per-file policy threshold is ~1000, and the card surface is cohesive).

### Task-card Review button candidacy

`show_review_button?/1` (private) decides whether the collapsed card's primary-colored Review link renders — TasksLive invokes `task_card/1` with full `%TaskInfo{}` structs.
Candidacy is deliberately RESULT-AGNOSTIC: EVERY `:completed` or `:cancelled` task shows the Review button, because a multi-repo task may have changed only writable FOREIGN repos — requiring a non-empty primary `branch_name` or a `no_changes: true` result would hide those legitimate review candidates.
The ONLY exclusion is `:reflect` (repo-less) tasks — no code to review; their `type` is read defensively via `Map.get/2`, and pending/running/finalizing/failed tasks never render the button.

### Field-usage audit — conclusions

| Component / render path | Fields | Access | Contract-fed by |
|---|---|---|---|
| `Layouts.app` sidebar Active Tasks (`layouts.ex`, `task_label/1`, `task_status_dot_color/1`, `group_tasks_by_project/1`) | `status`, `id`, `started_at`, `finished_at` (dot); `opts`, `project_path` (`Map.get`) | dot on contract keys | dashboard (`running_tasks`/`pending_tasks` only) + every LiveView mount (NodeAware) |
| `TaskCardComponents.task_card/1` | `type`, `opts` (`[:mode]`/`[:prompt]`/`[:objective]`), `id`, `status`, `started_at`, `finished_at`, `agent_count`, `result`, `model_id` (dot); `review_status`, `error` (`Map.get` — guarded reads via private `task_error/1`/`error_field/2`, never dot) | dot on contract keys | TasksLive only (full structs via `list_tasks_paginated`) — NOT dashboard-rendered; written contract-safe anyway |
| `TaskCardComponents.task_card/1` — "Token & Cost Usage", "Execution Logs", archive sections | `usage` + nested `usage.*`; `logs`; `archive_metadata` | `Map.get` guard (`not in [nil, []]`); inner accesses via `Map.get(@task, :logs, [])` | TasksLive only (sections hidden on summary maps) |
| `TaskCardComponents.render_result_full/1` (TasksLive only; the dashboard has no full-result modal) | input is `Map.get(task, :result)` — the **result VALUE** (runtime tuple), not the task map | — | TasksLive modal only |
| `Helpers.task_description/1` (lives in `helpers.ex`, outside this node) | `type`, `opts` (map pattern match + keyword access) | pattern | TasksLive |

**Findings:**
1. The ONLY dashboard-rendered task-list markup under `components/` is the sidebar Active Tasks section in `layouts.ex`. It uses only contract keys (status/id/started_at/finished_at via dot; opts/project_path via Map.get) — summary-safe.
2. `task_card_components.ex` is rendered ONLY by TasksLive with full structs; its heavy-field sections are `Map.get`-guarded — behavior-identical for structs, KeyError-safe for maps.
3. **No heavy field (logs/usage/archive_metadata) is needed by any dashboard-rendered card** — the dashboard card surface is the sidebar only (no full-result modal exists on the dashboard), and it needs only contract fields. Nothing is fetched lazily.
4. The structured failed-task record `error` (map with atom keys `kind`/`source`/`message`/`stacktrace`, decoded via the lenient Store `Codec.decode_error/1` on both the full `%TaskInfo{}` path and the summary projection; nil unless `status == :failed`) renders a compact error-tinted strip (truncated `error.message`) on the COLLAPSED failed card and a full-detail block at the top of the EXPANDED detail area (kind/source caption via `Helpers.task_error_kind_label/1`/`task_error_source_label/1`, full untruncated message, LAST ≤8 stacktrace frames in a `<pre>` block). All reads are guarded (`status == :failed` AND `is_map(error)`), atom/string-key tolerant, and total for nil/non-map/legacy shapes — legacy failed rows render byte-identically to pre-feature. The record is SEPARATE from the legacy `{:error, _}`/`{:exit, _}` result (both may coexist on one card; the collapsed line prefers the structured message, the expanded view shows both blocks).

## Notes for Agents — narrow-width (mobile) layout conventions
- **Branch-name badges** (`task_card_components.ex`, 3 sites: the per-repo badge in `result_repos_badges/1` + the two `render_result/2` top-level branch badges): truncate on narrow widths via the shared private **`branch_badge_class/0`** (`min-w-0 max-w-[10rem] sm:max-w-[14rem] md:max-w-none`) plus an inner `<span class="truncate min-w-0">` around the branch text — daisyUI `.badge` is `inline-flex` with no wrapping, so the ellipsis must live on a blockified inner span, and flex parents need `min-w-0` to shrink. Reuse the helper for any NEW branch-badge site (keep the class string a literal so the Tailwind v4 scanner picks it up); the full branch name always shows at `md`+ (`md:max-w-none`).
- **Agent tree** (`agents_components.ex` `path_tree/1`): the per-level agents-row wrapper carries **`pl-7 xl:pl-0`** — below `xl` the row stacks (`flex-col`) and the 28px indent (= folder icon `size-5` 20px + `gap-2` 8px) puts the first agent box AND every wrapped line exactly at that level's directory-text left edge, clear of the children trunk (`absolute left-2.5`, spans the full content row). Do NOT remove the indent or the wrapped boxes overlap the trunk connector on mobile; `xl:pl-0` keeps the desktop two-column layout byte-identical.

## Design Conventions — Adwaita token grammar (markup)

Durable rules for writing NEW markup in this subtree (applied repo-wide in the Adwaita restyle audit; see the parent `lib/evo_dash_web/CONTEXT.md` catalog for the full record):

- **Alpha floor** (base-content on light, binding constraint): user-relevant text never below `/50`; labels/meta ≥ `/60`; small/primary content ≥ `/70`; fold `text-[10px]`/`text-[11px]` → `text-xs` where feasible. Decorative large icons and "·" separators may keep `/30`–`/40`.
- **Hairlines/dividers on flat `bg-base-100` content surfaces** use `border-base-300` (never `border-base-200` — ≈1.06:1 invisible); tinted wells/`bg-base-200/30`-style strips may keep `border-base-200/80`.
- **Corners**: cards/panels `rounded-xl` max (prefer `rounded-lg`); NORMAL action buttons `rounded-lg`/`rounded-md`; only genuine pill/badge elements keep `rounded-full`.
- **`*-content` color tokens are tuned for solid fills** — never `text-*-content` on translucent tints; on `/10`–`/30` tints use `text-error`/`text-warning`/`text-success` with the semantic token, and `-content` only on solid fills (e.g. `btn-primary` content).
- **No raw color literals** (`text-red-500`, `bg-white/10`, `bg-black/10`, `bg-neutral text-neutral-content`, `bg-base-50` — the last is a dead class that compiles to no fill) → theme tokens (`text-error`, `bg-error/10`, `bg-base-200`, `bg-base-content/5`, …).
- **Empty/loading states**: exactly ONE dim layer (icon + copy should not both be at floor alpha).
- **Locked contracts — never restyle helper OUTPUTS**: the `EvoDashWeb.Helpers.task_status_*` / `agent_status_*` / `connection_status_*` class helpers, the task-form bottom-toolbar DOM order (attach-kind dropdown | mode | agent | model | send) + `data-layout`, `phx-hook`/`phx-click` bindings, and chart CSS-var strings. Markup class LITERALS may be polished; helper function outputs are the single source of truth.
