# EvoDash Web Interface

## Intent
The web interface layer for the EvoDash Phoenix application — a real-time dashboard for the EvoGit evolutionary software development system. Contains the Phoenix endpoint, router, telemetry supervisor, and delegates interactive UI to LiveView pages, reusable components, and classic controllers in subdirectories.

## Routing Table
- `live/` → Phoenix LiveView pages (Projects, Home, Tasks, Agents, Settings, System, Review, Welcome) + `live/components/` LiveComponents
- `live_hooks/` → On-mount hooks: `SetLocale`, `NodeAware`, `DesktopQuit`, `UpdateStatus`, `Guide` — registered in this order by `EvoDashWeb.live_view/0`
- `components/` → Reusable HEEx UI components and layout templates
- `controllers/` → Classic HTTP controllers and error handlers
- `plugs/` → HTTP middleware plugs (Locale)

## API Surface

### Top-Level Modules
| Module | File | Purpose |
|--------|------|---------|
| `EvoDashWeb.Endpoint` | `endpoint.ex` | Phoenix endpoint with LiveView socket, static files, and Plug pipeline. |
| `EvoDashWeb.Router` | `router.ex` | Browser-pipeline routes to all LiveView pages, a classic controller route for JSON export, and the Phoenix LiveDashboard. |
| `EvoDashWeb.Telemetry` | `telemetry.ex` | Supervisor with TelemetryPoller for endpoint/channel/VM metrics. |
| `EvoDashWeb.Helpers` | `helpers.ex` | Shared utilities for status badges, formatting, and icon helpers. Also hosts the **legacy tool-call display helpers**: `tool_call_display/1` returns `{context_label, content}` — `{"Shell call", command}` for shell calls (`run_bash`/`run_powershell`, command extracted from the arguments JSON `"command"` field) and `{function_name, pretty_arguments}` otherwise — built on the defensive `tool_call_name/1` / `tool_call_arguments/1` (tolerate `%ReqLLM.ToolCall{}` structs + atom/string-keyed maps; non-crashing `Jason`). The Agents page renders tool calls via `EvoDashWeb.AgentsLive.ToolCallDisplay` (`live/agents_live/tool_call_display.ex`), NOT these helpers; they remain public and test-pinned — do not remove. |
| `EvoDashWeb.Gettext` | `gettext.ex` | Gettext backend for i18n (`use Gettext, otp_app: :evo_dash`). Imported via `html_helpers/0` into all LiveViews/components. |
| `EvoDashWeb` | `evo_dash_web.ex` | Shared `__using__` macro dispatching to role-specific `import`s/`use`s. The `live_view/0` macro registers six on-mount hooks in order: `SetLocale` → `NodeAware` → `Appearance` → `DesktopQuit` → `UpdateStatus` → `Guide` (Guide LAST), so ALL LiveViews are locale-set, node-aware, accent-seeded (`@accent_color`, see `live_hooks/CONTEXT.md`), desktop-quit-aware, update-aware, and guide-aware. Deep hook behavior lives in `live_hooks/CONTEXT.md`. |
| `EvoDashWeb.LiveHooks.SetLocale` | `live_hooks/set_locale.ex` | On-mount hook setting the Gettext locale from the `locale` session/param. |
| `EvoDashWeb.LiveHooks.NodeAware` | `live_hooks/node_aware.ex` | On-mount hook + helpers for **SSH Remote Development** (node-aware dashboard): sets node-context assigns (`@current_node`, `@current_node_name`, `@current_node_id`, `@remote_targets`, `@connection_statuses`, `@remote_status`), subscribes to `EvoGit.PubSub` `"remote_connections"`, and provides `assign_node/2`, `handle_node_selected/2`, `handle_connection_status/2`. Detail in `live_hooks/CONTEXT.md` + the SSH Remote UX section below. |
| `EvoDashWeb.ThemeColor` | `theme_color.ex` | Accent-color resolution for the dashboard: `accent_color_for_mode/2` (task-mode accent) and `accent_color/1` (stable project-name-hash accent via `hsl_to_hex/3`). Drives `--project-accent`/`--project-ring-accent` on `#dashboard-root`. See the CSS Map section. |
| `EvoDashWeb.ArchiveHelpers` | `archive_helpers.ex` | Archive-tree construction for the archive UI: `build_archive_tree/1` (dashboard) + `build_archive_tree_for_review/1` (review), string/atom-key tolerant via `normalize_agent_keys/1` + `agent_key/2`. `normalize_agent_keys/1` (applied to every record at both tree chokepoints) atomizes top-level keys AND the nested `usage` sub-map through a whitelist of the exact keys core `complete_task.ex` `format_usage_for_archive/1` writes (`input_tokens`, `output_tokens`, `total_tokens`, `cached_tokens`, `cache_creation_tokens`, `input_cost`, `output_cost`, `total_cost`, `cache_hit_rate` — no plain `cost` key); unknown keys stay string-keyed (never bare `String.to_atom`). See Known Issues below. |

### Task Cancellation Model (graceful cancel + force kill)

The backend (`:evo_git`) provides two cancels: `EvoGit.RemoteNode.cancel_task/2` = GRACEFUL (`:pending` → immediate `:cancelled`; `:running` → `:cancelling`, agents messaged to save + exit with a 3-turn grace, then `:cancelled` WITH result/archive preserved) and `EvoGit.RemoteNode.force_kill_task/2` = BRUTAL (kills all agents + wrapper → `:failed`, result nil'd; escalation from `:cancelling`). `EvoDash.NodeContext.cancel_task/2` and `EvoDash.NodeContext.force_kill_task/2` delegate to them (see `./evo_dash/CONTEXT.md`).

Dashboard UX: the task card's inline button is a graceful **Cancel** (`phx-click="open_cancel_modal"`, visible `[:pending, :running]`); the three-dot dropdown has a destructive **Force kill** (`phx-click="open_force_kill_modal"`, visible `[:running, :cancelling]`). TasksLive implements both as two-step server-side confirmation modals (SystemLive warning-modal pattern — see `./live/CONTEXT.md`). The `:cancelling` status is treated as IN-PROGRESS everywhere: violet badge (`Helpers.task_status_badge`), pulsing-dot card label (`task_card_components.ex`), violet sidebar dot (`layouts.ex`), in the sidebar `@active_statuses` + running partition (`NodeAware`), and filterable via the TasksLive status dropdown (pure SQL string filter).

### Agent Status Display (frontend merges `:blocked` into `:pending`)

The frontend presents `:blocked` and `:pending` agents identically as **"Pending"** on all agent status surfaces — to the user, both mean "queued / waiting to be scheduled" (`Helpers.agent_status_label/1` is the single source of label text; the four `agent_status_*` presentation helpers delegate `:blocked` → the `:pending` clause). The backend scheduler keeps `:blocked` and `:pending` distinct. Note: the SystemLive scheduler charts (`live/system_live/charts.ex`) consume the raw `:blocked` COUNT as the slot-saturation "waiting" telemetry series — that is scheduler telemetry, not an agent-status display.

### Subdirectories
| Directory | Purpose |
|-----------|---------|
| `live/` | Phoenix LiveView pages: Home, Projects, Agents, Settings, System, Tasks, Review, Welcome. Also contains the `live/components/` subdirectory (LiveComponents like `NodeSelectorComponent`). |
| `live/components/` | LiveComponents (`use EvoDashWeb, :live_component`): `NodeSelectorComponent` (navbar node selector + connection manager modal for SSH Remote Development). |
| `live_hooks/` | On-mount hooks: `SetLocale`, `NodeAware`, `DesktopQuit`, `UpdateStatus`, `Guide` (global overlays). |
| `components/` | Reusable HEEx components: CoreComponents, ProjectComponents, TaskFormComponents, TaskCardComponents, AgentsComponents, RemoteGateComponents, Layouts. |
| `controllers/` | Classic HTTP controllers and error handlers. Includes `TaskExportController` (JSON export of archived task metadata). |

### LiveView Routes
| Route | LiveView | Purpose |
|-------|----------|---------|
| `GET /` | `ProjectsLive` | Projects page (root) — project selector, task form, project settings, task history. URL-based project state via `?project=<path>` query param. |
| `GET /help` | `HomeLive` | Home chat page — chatbot-style conversation interface. |
| `GET /projects` | `ProjectsLive` | Projects page — same LiveView + action as `/`, direct route, no redirect. |
| `GET /welcome` | `WelcomeLive` | Onboarding page — stepwise first-LLM quick setup (provider → variant → model → credentials); `skip`/`get_started` redirect to `/welcome/complete`. |
| `GET /welcome/complete` | `WelcomeCompleteLive` | Onboarding completion page — example task (copyable demo objective via the `ClipboardCopy` hook) + "Go to Dashboard" button that completes onboarding and navigates to `/projects`. |
| `GET /tasks` | `TasksLive` | Task history page — full task list with status filters, pagination, archive details, cancel/force-kill actions. |
| `GET /agents` | `AgentsLive` | Agent tree inspector with real-time hierarchy |
| `GET /settings` | `SettingsLive` | Runtime scheduler configuration |
| `GET /system` | `SystemLive` | System page — Software Update card, System Self-Check (health banner + check grid incl. the Genesis Source card), scheduler-status charts, and the grouped System Controls section (System Dashboard + scheduler pause/resume + VM restart/stop) |
| `GET /review/:task_id` | `ReviewLive` (`:show`) | Code review page — diff viewer with expandable context, commit list, merge/reject/resume actions. Supports post-merge re-review via persisted SHAs. |
| `GET /review/:task_id/commit/:commit_sha` | `ReviewLive` (`:commit`) | Single-commit inspection — reuses the shared diff viewer component to show changes for one commit. |
| `GET /tasks/:task_id/export` | `TaskExportController` (`:export`) | Downloads a task's `archive_metadata` as a JSON file (`archive-<task_id>.json`). 404 when the task or archive data is missing. Node-aware via `?node=` (remote task resolution) — detail in `controllers/CONTEXT.md`. |
| `GET /dashboard` | `ProjectsLive` (`:system_dashboard`) | Full-bleed Phoenix LiveDashboard wrapper: renders `Layouts.app` with an iframe filling the main content area (`src=~p"/phoenix/dashboard/home"`, `min-height: calc(100vh - 2rem)`), no header chrome. |
| `/phoenix/dashboard` | `Phoenix.LiveDashboard` | Built-in metrics/telemetry dashboard (embedded via the `/dashboard` iframe). |

### Task mode resolution & `:reflect` (web↔core contract)

Dashboard combined-mode strings (`"genesis_new"`/`"genesis_existing"`/`"evolve_simple"`/`"custom_agent"`) map to `{task_type, core_mode_string}` in `ProjectsLive.do_task_submit/4` (`projects_live.ex:2123-2133`): `{:genesis, "new"|"existing"}`, `{:evolve, "simple"|"custom"}` (unknown strings fall back to `{:evolve, "simple"}`). The core `EvoGit.TaskRegistry.RuntimeOpts.mode_atom/2` (`apps/evo_git/lib/evo_git/task_registry/runtime_opts.ex:124-125`) dispatches to `genesis_mode_atom/1` (`"new"`/`"existing"`) or `evolution_mode_atom/1` (`"simple"`/`"custom"` — `"custom"` IS accepted, runtime_opts.ex:98-99). Unknown evolution modes raise `ArgumentError` (runtime_opts.ex:101-102); `:genesis` with mode `"custom"` raises "custom mode is evolve-only" (runtime_opts.ex:110-112).

**:reflect (repo-less chat) facts**: reflect tasks carry NO `:path` in opts and run with nil `project_path`; result shape `{:ok, %{result: <text>, commit_sha: nil, branch_name: nil, tag: nil}}`. `:reflect` IS in `EvoGit.Store.Codec` `@known_atoms` (store/codec.ex:217-218). `TaskExecutor.execute_task(:reflect, ...)` routes directly to `Runtime.SelfReflective.run`, deliberately bypassing `build_common_runtime_opts` (which `Keyword.fetch!(:path)`-raises) — `RuntimeOpts.mode_atom/2` needs no reflect clause. Full chat/send-flow detail: `live/home_live/CONTEXT.md`; core agent/runtime: `apps/evo_git/CONTEXT.md` "Self-Reflective Agent (repo-less)".

## i18n / Gettext Backend

`EvoDashWeb.Gettext` (`gettext.ex`) is the umbrella's only Gettext backend and is deliberately declared with `split_module_by: [:locale, :domain]` — Gettext then emits one small `EvoDashWeb.Gettext.T_<locale>_<domain>` module per locale (compiled in parallel) instead of a single ~16k-clause module, keeping the evo_dash compile fast. Keep this option; the public API (`gettext`/`lgettext`/`put_locale`/`known_locales` and every `use Gettext, backend: EvoDashWeb.Gettext` site) is unchanged.

## Constraints
- All web modules use `use EvoDashWeb, <role>` as their entrypoint — do not bypass the shared `__using__` macro.
- New interactive pages should be LiveViews in `live/`, not controllers in `controllers/`.
- Subdirectory naming conventions: `<name>_live.ex` for LiveViews, `<name>_components.ex` for component modules, `<name>_controller.ex` for controllers.
- Static assets served from `priv/static` under paths defined in `EvoDashWeb.static_paths/0`.
- Styling is Tailwind CSS + daisyUI throughout.
- **Professional design language** (Settings, Tasks, Review, System pages): compact, dense, "power-tool" aesthetic inspired by VS Code / GitHub — `rounded-lg`/`rounded-md` radii (no `rounded-3xl`/`rounded-2xl` large cards), subtle borders, restrained color, no gradient hero boxes or decorative blurs. Settings uses one-config-value-per-full-width-row layout; the config file path is shown at the top with a copy-to-clipboard button (`ClipboardCopy` hook).
- **`try/rescue` is an anti-pattern here.** Do NOT wrap config/core-value loading (`EvoGit.Config.*`, `EvoGit.AgentScheduler.*`, `EvoGit.SystemCheck.*`) in `try/rescue` — if these crash, the LiveView SHOULD crash truthfully so the real error is visible (displaying a wrong default like `%{}`, `false`, or `100_000` is worse than a visible crash). The only accepted `try/rescue` use is: genuinely untrusted external execution like `System.cmd` running user-defined shell commands (`ProjectsLive`). Every retained `try/rescue` MUST carry an inline justification comment. For converting untrusted strings to atoms, use whitelist `Map` lookups (`Map.get/2`/`Map.fetch/2`) — never bare `String.to_existing_atom/1` on client input, and never `String.to_atom/1` (atom-table exhaustion DoS risk).

## Known Issues — Task Archive Display (archive_components.ex / review_components.ex / archive_helpers.ex)

Current bugs/gotchas verified against the code (core semantics in `apps/evo_git/CONTEXT.md` "Archive record — exact structure & crash semantics"):

- **Crashed/failed/aborted agents are NEVER in `archive_metadata`.** The core writes an archive record only when `complete_task` runs with `archive && base_commit`, collected only on successful root completion (`{:ok, %EvoGit.Agent.Result{}}`); records have no status/outcome field. The dashboard archive tree therefore shows only completed agents — a crashed agent's failure is visible only via the task-level status badge. `archive_details`/`archive_review_section` render nothing when the root never completed.
- **Task-level totals come from `TaskInfo.usage`** (a `%EvoGit.Agent.Usage{}` struct, atom-keyed after `decode_usage` — works), NOT from summing archive records; evo_dash never aggregates per-agent usages. Export envelope carries both: `usage` (task-level) + `archive_records` (per-agent, raw).
- **Where archive UI renders**: (1) Tasks page expanded task card (`tasks_live.ex:166` → `task_card_components.ex:386-391`), (2) Review page "Archive" tab (`review_live.ex:154-165` → `review_components.ex`). The Projects page task list strips `archive_metadata` (`projects_live/assigns.ex:130`) and `EvoDash.NodeContext.list_tasks_summary` omits it — sidebar lists never show it.
- **ArchiveHelpers guardrails**: `build_archive_tree/1` and `build_archive_tree_for_review/1` use a visited-set + `agent_id` nil/"" filter to survive cyclic/self-referential or string-keyed data (pinned by `test/evo_dash_web/components/archive_tree_test.exs` and `review_live_test.exs`).
- **nil `parent_id` is preserved end-to-end — the dashboard can neither drop it nor invent it.** Export keeps every map key (`Map.new(map, fn {k,v} -> {to_string(k), ...} end)`), so a stored nil `parent_id` is emitted as `"parent_id": null` (test-pinned) and a stored non-nil id survives unchanged (no key rename, no struct mismatch). The tree groups records by `agent_key(&1, :parent_id)` and treats `by_parent[nil]` as ROOTS — multiple roots are fully supported, each nil-parent record renders as its own top-level node. Orphans (non-nil parent_id matching no `agent_id`) never render but remain in JSON + the raw `length(@archive_metadata)` count. There is NO dashboard write path (`archive_metadata` is only read/stripped/exported; Review "Resume" only prepares a new evolve form; AgentsLive `parent_id` mutations are display-only ETS views) — N stored nil-parent records originate in the CORE-written archive data (`CompleteTask.complete/4` agent-state `parent_id`), not in export/UI logic.

## Dashboard UI Markup & CSS Map (top bar + adaptive task form)

Durable pointer map for UI-refinement work on ProjectsLive (GET /projects). File:line refs are verified against the current code; re-verify before editing.

- **Top bar**: the single top-bar component is `EvoDashWeb.ProjectsLive.RemoteView.top_bar/1` (`live/projects_live/remote_view.ex:75-237`) — `projects_live.ex` defines no `top_bar`; it renders the component for the LOCAL top bar (`projects_live.ex:125`) AND the remote-connected one (`:257`, `remote={true}`); gate renders (`:416`/`:437`) pass `hide_palette` (GitHub button hidden). Container `.dashboard-topbar` (`assets/css/app.css:1286-1302`): sticky, `border-radius: 1rem`, `color-mix` base-100/80 background; **NO `backdrop-filter`** (it would create a containing block trapping the palette's fixed-position backdrop to the ~50px strip) and no `overflow: hidden` (dropdowns escape); accent `::before` line (`:1308-1325`), children `z-index: 1`. LEFT = `ProjectComponents.project_omnibox/1` (`components/project_components.ex:62-120`): trigger `.project-palette-trigger`, overlay carries `phx-keydown="palette_keydown"` + `phx-click-away="close_project_palette"`, backdrop `phx-click="close_project_palette"` — details in `components/CONTEXT.md`. RIGHT = Configure dropdown (`btn btn-ghost`, panel `w-80 sm:w-96`, sections "Task Options" / "Project Settings").
- **Task form**: `TaskFormComponents.task_form/1` (`components/task_form_components.ex:131-456`). `.input-layout` carries `data-layout={layout_for(@prompt)}` — server-seeded (`layout_for/1`, threshold 1200 graphemes OR > 32 lines; `@short_objective_threshold`) and client-driven by the `AdaptiveInput` hook (adds a height-based trigger + hysteresis; re-asserts the computed layout via a MutationObserver on `.input-layout` whenever the server re-seeds a stale `data-layout`, so the layout never snaps back to compact while a long prompt remains — zero network events). `.input-card` has NO Tailwind classes except `relative`; all styling in app.css. Textarea `.input-prompt`: `phx-update="ignore"` + `phx-hook="AdaptiveInput"` — NO `phx-change`/`phx-debounce` (no per-keystroke server event). **Bottom toolbar** (`.input-controls`, the card's LAST element, rendered `unless @disabled`): ONE ChatGPT/Gemini-style row holding ALL controls in DOM order — **attach "+" button** first (bottom-LEFT, `button#objective-file-button`, `type="button"` — CRITICAL, inside the form a button without it would submit; `phx-hook="FilePicker"` + `data-picker-id="objective_file"`, `btn btn-ghost btn-sm btn-square`), The FilePicker hook pushes `"file_pick"` with `%{picker_id, prompt}` (DOM textarea value); ProjectsLive snapshots it into `file_pick_bases` (reset on node switch) and runs `EvoDash.DirectoryPicker.pick/3` in `:file` mode; results read via `EvoDash.AttachedFile.read/1` (invalid PDFs → `{:invalid, reason}`). Because the textarea is `phx-update="ignore"`, the server pushes `picker_result:objective_file` with `%{prompt, block, attached, name}` and the hook writes the DOM. **Manual fallback**: `#objective-file-manual` (hidden inline path input, `phx-update="ignore"`) lives INSIDE the toolbar row (direct sibling of the "+" button) and is CSS-anchored (`position: absolute`) as a popover just above the toolbar near the bottom-left "+" attach button; on `unavailable` the hook reveals it; Enter/confirm pushes `"file_pick_manual"` (`%{picker_id, path, prompt}`), validated (`File.regular?/1`) and routed through the SAME pipeline — shared logic in **`EvoDashWeb.ProjectsLive.AttachFile`** (`live/projects_live/attach_file.ex`). Manual reads run on the LOCAL node's filesystem (remote reads unsupported — `genesis_remote` ships no ex_pdf). Controls row `.input-controls flex-nowrap` (same DOM order in BOTH layouts; **ONE-LINE CONTRACT** — `min-w-0 truncate max-w-*` on the selects so long labels clip with an ellipsis instead of wrapping): compact mode select (`select-sm`, options `genesis_existing`/`genesis_new`/`evolve_simple`/`custom_agent`, `phx-change="task_change"`), optional custom-agent select (`select-sm`, `phx-change="select_agent"`, only when `@custom_agents != []`, "Auto (recommended)" hidden in `custom_agent` mode), compact model select (`select-sm`, `phx-change="select_model"`, only when `@model_profiles != []`, option label = bare profile id), and the **circular icon-only send button LAST** (`button#task-launch-button`, `type="submit"`, `btn btn-primary btn-circle btn-sm shrink-0` — the right-aligned cluster's last element = far right, no auto margin; `hero-arrow-up` icon, `aria-label`/`title` = gettext("Launch"), carries `data-mode`/`data-resume`). The MODE select LEADS the cluster carrying the row's `ml-auto`, so mode | (custom-agent select) | model | send form a RIGHT-ALIGNED CLUSTER at the row's right edge while the attach "+" stays FIRST at bottom-left.
- **CSS "Adaptive Input Layout"** (`assets/css/app.css:1749-2182`, followed by the Help Chat Page section at `:2183-2212`): daisyUI loads with `themes: false`, so there is NO `--b1/--b2/--b3/--bc/--p` namespace — all custom rules use `--color-base-*`/`--color-primary`/`--project-accent` with `color-mix` (see `assets/CONTEXT.md`). `.input-card`: `border-radius: 1rem`, `backdrop-filter: blur(8px)`. Compact card (`:1843-1854`): `max-width: 48rem`, auto margins (vertical centering), accent border + ring+glow in `box-shadow` (`:1808-1812`); the EXPANDED layout intentionally has no ring/glow. Compact `::before` accent top line (`:1820-1837`); expanded card `flex: 1 1 auto; min-height: 0` (`:1866-1870`); compact prompt capped (`max-height: calc(1.6em * 16)`), expanded prompt min 10 lines. `.input-prompt` uses uniform small padding (`1.25rem 1.25rem 0.75rem`) — the old 3rem top "clearance" (reserved for the floating attach button) is gone. `.input-controls` (bottom toolbar): `position: relative` (anchor for the `.file-manual` popover), `flex-wrap: nowrap` (no `border-top` divider); the MODE select's `ml-auto` absorbs the row's free space, right-aligning the [mode | custom-agent select | model | send] cluster (no `space-between`, no order-*/mx-auto; the send button carries no auto margin). `.file-manual` manual-path panel: `position: absolute; bottom: calc(100% + 0.4rem); left: 0.75rem` — a popover just above the toolbar near the "+" button, nudged off the container's left edge; its inline error extends further up (`bottom: calc(100% + 0.25rem)`).
- **Accent colors**: `--project-accent` (mode accent) + `--project-ring-accent` (project-name-hash accent) set inline on `#dashboard-root` (`live/projects_live.ex:101-103`; `:root` fallbacks at app.css:1507-1511). Mode palette (`theme_color.ex`): genesis_new → red `oklch(0.62 0.19 25)`, genesis_existing → blue `oklch(0.62 0.19 255)`, evolve* → green `oklch(0.72 0.17 152)`, custom_agent → violet `oklch(0.68 0.17 290)`; evolve/custom with non-blank resume → lighter green/violet. The Launch button hover ring mirrors the palette via `[data-mode]`-keyed rules (`app.css:~L1981-2019`, incl. the dedicated `[data-mode="custom_agent"]:hover` violet rule) with `[data-resume="true"]` variants; `task_form_components.ex` sets `data-resume={String.trim(@resume_from) != ""}` on the Launch button. `accent_color/1` maps `:erlang.phash2(name, 360)` continuously across the full 0-360 hue range via `hsl_to_hex/3` (no sector quantization).
- **Tests**: `test/evo_dash_web/components/task_form_components_test.exs` (async — ALL PASSING): bottom-toolbar contract assertions (attach "+" first inside `.input-controls`, then mode/agent/model selects in DOM order, circular send button `#task-launch-button` LAST (rightmost) with no auto margin — the MODE select carries the row's `ml-auto`, leading the right-aligned cluster; compact `select-sm` classes; `hero-arrow-up` icon + `aria-label` "Launch"; `data-mode`), `layout_for/1` boundary tests, bare profile-id option labels, textarea `AdaptiveInput`/`phx-update="ignore"` (no `phx-change`), disabled-state overlay + hidden attach button. `projects_live_test.exs` uses `button#task-launch-button` presence as the enabled/disabled-form marker. `test/evo_dash_web/components/project_components_test.exs` covers the palette wiring.

## Task Summary Data Flow — SQL-side filtering + debounce optimization

Coordinated design across `live_hooks/`, `live/`, `components/`, and the `evo_git` core. Detailed per-subtree notes in `live/CONTEXT.md`, `live_hooks/CONTEXT.md`, `components/CONTEXT.md`.

- **Sidebar summary is SQL-filtered + UNIFIED (single loader)**: `EvoDashWeb.LiveHooks.NodeAware` is the ONE sidebar Active Tasks implementation — `fetch_active_tasks/1` (node-aware: local `TaskRegistry.list_tasks_summary(@active_statuses)` / remote `NodeContext.list_tasks_summary(node, @active_statuses)` RPC; pending-remote guard → `{[], []}`) → `partition_active_tasks/1` (pure) → `assign_active_tasks/1` (assigns `:running_tasks`/`:pending_tasks`); `load_running_and_pending_tasks/1` delegates. The SQL query pushes the statuses filter down (`WHERE status IN (...)`), skipping finished tasks that can never appear in the sidebar. `show_review_button?/1` is public and summary-based (reads only `status`/`type`; true for every `:completed` task except repo-less `:reflect`) — NO `result` reads from summary maps. on_mount fires the load ONLY on the connected mount (dead-render skip).
- **Debounced task reloads (push-based, no poll)**: `{:task_updated, task_id, status, node}` / `{:task_deleted, task_id, node}` events → `NodeAware.handle_task_info/2` (returns `{:noreply, socket}` — do NOT re-wrap in LiveViews) applies the node filter via `event_from_current_node?/2` BEFORE a 300ms trailing-edge debounce (`:node_aware_reload_tasks`, bursts coalesced into one reload). The actual reload runs in each LiveView's `handle_info(:node_aware_reload_tasks, socket)` clause: simple views call `NodeAware.reload_tasks(socket)`; `projects_live` runs a custom full reload (project-config refresh + browser notifications + sidebar) and ends with `NodeAware.clear_task_reload_pending(socket)`; `tasks_live` additionally refreshes its page data (`reload_current_page`); `review_live` runs a broadcast-guarded async reload (only a node-matching event for THE reviewed task re-fetches review data via the async `start_async_load/2`, never blocking `handle_params`). The same debounced path serves BOTH local and remote — no poll. `{:recent_projects_updated}` is intentionally NOT debounced.
- **`assign_node/2` dedup guard**: tracks `:tasks_node_loaded` (`{current_node_id, current_node}`); skips the sidebar reload when the resolved node context is unchanged (prevents the mount double-fetch; pagination/palette push_patches do not re-fetch). Node switches (local↔remote, pending→connected) reload.
- **No dashboard main task list**: no `Assigns.current_tasks/1` / `assign(:tasks, ...)` sites. The sidebar (`@running_tasks`/`@pending_tasks`) is statuses-filtered summary maps (no full-row decode; no logs/usage/archive_metadata). Browser notifications use the minimal id-only projection (`TaskRegistry.list_task_ids([:completed, :failed, :cancelled])`) + per-new-id `TaskRegistry.get_task/1`.
- **Summary map contract** (from `EvoGit.TaskRegistry.list_tasks_summary`): plain maps with `id, status, review_status, started_at, finished_at, type, project_path, opts, branch_name, model_id, agent_count, base_sha, commit_sha, lease_expires_at, updated_at, error` (opts = decoded keyword list; `updated_at` = raw fixed-precision ISO string; `error` = structured failure-record map with atom keys `kind`/`source`/`message`/`stacktrace`, decoded via the lenient `Codec.decode_error/1` — `nil` unless the task is `:failed`). Heavy fields EXCLUDED — `result` is deliberately dropped from the projection (`EvoGit.Store` `@summary_columns`/`decode_summary_row`); evo_dash never reads `result` from these maps. Components must dot-access ONLY contract keys (`Map.get` for optional) — a dot access on a missing map key raises KeyError (see `components/CONTEXT.md` audit table).
- **No periodic polling**: `tasks_live` and `agents_live` have no poll timers. Remote-node updates arrive through the same node-filtered push events as local ones. The page-load path (`list_tasks_paginated`) still transfers FULL TaskInfo structs for the expandable cards (archive_metadata/logs/usage) — but only on explicit page loads/reloads, never on a timer.
- **Statuses API**: the statuses variants `EvoGit.TaskRegistry.list_tasks_summary/1`, `list_tasks_summary_by_path/2`, `EvoDash.NodeContext.list_tasks_summary/2` / `EvoGit.RemoteNode.list_tasks_summary/2` exist; the statuses filter is SQL-pushed-down to `WHERE status IN (...)`. Summary maps include `opts`; `result` is excluded from the projection.

## SSH Remote UX — Connection Gate, Node-Locked Dashboard, Path Autocomplete
- **`:remote_status` assign** (NodeAware): `assign_node/2` sets it for every LiveView — nil for local; for `?node=` contexts resolved to a target it derives from `EvoDash.NodeContext.connection_status/1`: `%{phase: phase}` maps pass through as-is; any other result (the `:disconnected` atom when the subsystem is unavailable, or an empty `%{}` fallback) → `%{phase: :disconnected, last_error: nil}`; unknown target → nil. `handle_connection_status/2` recomputes it live whenever `current_node_id` is set, so a connecting→error transition updates the gate without a push_patch; the transition push_patch logic (connected/disconnected local↔remote transitions, preserving `?node=`) applies.
- **Pending-context task isolation**: `load_running_and_pending_tasks/1` assigns EMPTY running/pending tasks when `current_node_id` is set AND `current_node == node()` (remote context requested but not yet connected) — local tasks never leak into the sidebar in a remote context.
- **Connection gate (shared)**: `EvoDashWeb.RemoteGateComponents` (`components/remote_gate_components.ex`) — `remote_connection_gate/1` + public predicate `gate_active?/1` (truth table: nil node → false; node + nil status → true; phases `:connecting`/`:bootstrapping`/`:disconnecting`/`:error`/`:disconnected` → true; `:connected` → false; unknown shapes → false). Renders spinner + "Connecting to %{name}…" for connecting phases; `alert-error` with `last_error` + Retry (`retry_remote_connection`) + Manage Connections (link to `/settings?category=remote_connections`, deliberately WITHOUT node param) + Switch to Local (`switch_to_local`) for error/disconnected; renders nothing when connected/local. Wired into agents/tasks/settings/system/review LiveViews (branch inside the `Layouts.app` shell; `agents_live` gates in its template). ⚠️ Call it fully-qualified with the assigns map (`<%= EvoDashWeb.RemoteGateComponents.remote_connection_gate(assigns) %>`) — do NOT use the declarative `<.remote_connection_gate assigns={assigns}/>` form: the Declarative wrapper merges attr defaults and mangles node-name resolution (renders "Local" instead of the target name). Name resolution inside: explicit `name` attr → `current_node_name` → "Local".
- **Dashboard node-locking** (`projects_live.ex`): `handle_params/3` captures `prev_node_id` BEFORE `assign_node/2`; on node switch it clears ALL client-side state (task_prompt, task_resume_from, task_starting_commit, show_advanced, task_archive, active_project/active_project_path, foreign_repos, palette_* assigns) and re-persists. ANY `current_node_id != nil` skips local project activation/auto-load; `recent_projects` = `NodeContext.list_recent_projects(current_node)` ONLY when connected, else `[]`. `restore_state` skips ALL persisted form values when the persisted `node` key != current context (`current_node_id || "local"`; a missing node key defaults to "local"). Per-node state persistence: `maybe_persist_state/1` stores `"node"`; `#dashboard-root` carries `data-node-id={@current_node_id || "local"}`; the JS StatePersistence watcher merges `node: this.el.dataset.nodeId || "local"` — single `dashboard_state` key, server-side node check does the isolation.
- **Remote view** (`live/projects_live/remote_view.ex`): `RemoteView.top_bar/1` (remote badge: server-stack icon + node name; Configure dropdown shown — task management is available remotely; `data-remote="true"` on `.dashboard-topbar`) + info banner. CSS variant `.dashboard-topbar[data-remote="true"]` (app.css ~line 1308): `--color-info` tint via nested `color-mix`. The palette (`project_omnibox`/`palette_menu`) takes a `remote` attr: "Create New Project" hidden when remote (creation is local-only); "Open Project by Path" stays and is remote-aware.
- **Node param in URLs**: `EvoDashWeb.Helpers.with_node_param/2` (nil-safe) — used by sidebar nav links, the config warning banner link, system_live links, review_live back links, and the task-card Review button. ⚠️ GOTCHA: it appends with `?`, so URLs that ALREADY have a query string (`/projects?project=...`, `~p"/settings?category=llm"`) must use a manual `&node=` suffix instead — see `project_flow.ex` `project_url/2`, `handle_palette_key`, and `system_live.ex:374` (deliberate comments there). The task-card Review button links to `/review/:task_id` via `with_node_param`, so remote-node tasks are reviewable.
- **Path autocomplete**: `EvoDash.NodeContext.list_path_suggestions/2` + `dir?/2` wrappers (graceful degradation → `[]` / `false`). `Project.path_suggestions/3` is node-aware (recent-project paths match the typed value as a case-insensitive SUBSTRING of the full path — infix, so "proj" surfaces "/home/x/sources/project" — dedup, recents first; filesystem suggestions come from `EvoGit.PathSuggestions.suggest/1` on BOTH branches: local delegates directly, remote via `NodeContext.list_path_suggestions` — so `<dir>/` lists the directory's own contents and `~` lists home, identically everywhere). Remote `open_project`/`select_project`: validate `NodeContext.dir?(current_node, expanded)` → on success add recent project + display-only `active_project` + push_patch with `&node=`; on failure error flash "Directory does not exist on the remote node: <path>". `create_project` stays local-only. Foreign repo path input (`project_settings_body/1`): `phx-hook="PathAutocomplete"` + `list="foreign-repo-path-suggestions"` + `phx-change="foreign_repo_path_input"` (`phx-debounce="150"`), datalist fed by `:foreign_repo_path_suggestions`; the event computes LOCAL suggestions; the Tauri Browse button stays.
- **Tests**: `node_aware_test.exs` is `async: false` (XDG_CONFIG_HOME mutation + shared `EvoGit.RemoteConnection.Registry`; uses a fake ConnectionManager GenServer registered in the real Registry — copy this pattern for remote-context LiveView tests; no SSH machinery involved). `projects_live_test.exs` covers node-switch state clearing, gate states, connected remote view, `&node=` push_patch preservation, restore_state stale-node skip, remote open_project failure flash. `remote_gate_components_test.exs` covers gate states + the full `gate_active?/1` truth table. NOTE: the remote `dir?` success path (push_patch with `&node=`) is untestable end-to-end with a fake BEAM node (`:erpc` fails fast) — covered by the error-path assertion + switch_to_local inverse.

## Theming & Color System (server-rendered markup map)

Executed GNOME/libadwaita (Adwaita) design language. Current-state map of theme/color mechanics that live in **markup** (`.ex`/`.heex`); CSS token values + consumption live in `assets/css/app.css` (see `assets/CONTEXT.md`).

- **Theme mechanism is 100% client-side; the server never pushes theme/config colors to the DOM** (the ONE exception is the config accent `data-accent-color` — next bullet but one). The render-blocking inline `<script>` in `components/layouts/root.html.heex:28-126` is the whole theme engine: tri-state light/dark/**system** (system = default; `localStorage "phx:theme"` stores ONLY `"light"`/`"dark"` — system mode REMOVES the key, L41-48). `<html>` (root.html.heex:2-5 carries `lang`/`dir`) receives `data-theme` = resolved `"light"|"dark"` AND `data-theme-mode` = `"system"|"light"|"dark"` (L33-39). `data-theme-mode` exists ONLY to position the unused slider thumb in `Layouts.theme_toggle/1` (layouts.ex:662-664 — `[[data-theme-mode=light]_&]:translate-x-9` / dark `translate-x-[4.5rem]`). Boot apply L65; cross-tab `storage` listener L68-70; toggle buttons are server-marked `phx-click={JS.dispatch("phx:set-theme")} data-phx-theme="system|light|dark"` (layouts.ex:569/577/585 compact + 668/677/686 large) caught at L73; OS `prefers-color-scheme` change re-resolves ONLY in system mode (L76-78, matchMedia). No flash-of-wrong-theme (render-blocking, applies before paint). `applyTheme` side effects: `<meta name="theme-color">` flips `#f6f5f4` (light window) ↔ `#242424` (dark window) (L14/L50-55 — matching the Adwaita base-200 chrome tones; the ONLY theme hex literals in server markup) + favicon swap `/images/logo.svg` ↔ `/images/logo-alt.svg` (L57-62). All Tailwind `dark:` classes in markup follow `[data-theme=dark]` via `@custom-variant dark` (app.css:209) — `html[data-theme]` is the single dark switch. No other JS touches documentElement theme (assets/js adaptive_input.js sets only the non-color `--app-vh`). **Toggle widgets**: `theme_toggle_compact/1` (layouts.ex:551-594 dropdown; sidebar bottom bar layouts.ex:249) and the large 3-segment `theme_toggle/1` (layouts.ex:657-693) — the large one is used ONLY by the UNROUTED Phoenix scaffold page `controllers/page_html/home.html.heex:56` (`EvoDashWeb.PageController` is NOT in router.ex; whole file is dead markup incl. coral SVG fills #EE7868/#FF9F92/#FA8372/#E96856/#C42652/#A41C42/#FD4F00 at L10-46). Neither toggle marks the current mode (no selected state).
- **Shell-wide config accent — `data-accent-color` on `#app-layout` (server-driven)**: `EvoDashWeb.LiveHooks.Appearance` (`live_hooks/appearance.ex`, registered in `live_view/0` AFTER `NodeAware`) resolves `EvoGit.Config.resolve([:appearance, :accent_color])` (schema default `"blue"`, validated to one of the ten CSS-known names) and seeds `@accent_color` on every LiveView; `Layouts.app` declares `attr(:accent_color, :string, default: "blue")` and sets `data-accent-color={@accent_color}` on the `#app-layout` shell div (layouts.ex:63-69 attr, 75-79 div) — inside `<html data-theme=...>`, so the ten `[data-accent-color="<name>"]` override rules in app.css (un-layered author CSS after the daisyUI theme `@plugin` blocks, `assets/CONTEXT.md`) retarget `--color-primary`/`--color-accent` for the shell subtree, incl. the dark-theme variants `[data-theme="dark"] [data-accent-color="purple|slate"]`. Resolution is node-aware through a per-node accent cache (`EvoDash.AccentCache`, `lib/evo_dash/accent_cache.ex` — boot-created ETS keyed by connection-target id, `nil` = local): the seed AND every `handle_params` re-resolution read the cache SYNCHRONOUSLY (a local ETS read, never an RPC), so a warm remote/pending page paints its own accent from the first paint. Local (no `?node=`, `"local"`, or unknown id) → SYNCHRONOUS `EvoGit.Config.resolve/1` inside the hook's attached `:handle_params` interceptor (correct on first paint, incl. dead render). Remote connected `?node=` target → the cache is read synchronously FIRST (warm → instant); on a connected socket the hook ALWAYS spawns an async `EvoDash.TaskSupervisor` fetch via `EvoDash.NodeContext.get_resolved_config/1` (the SettingsLive RPC) landing through a stale-guarded `:handle_info` interceptor (seq + node-param match) — a matching successful result assigns AND writes the cache (one fetch serves all later loads); failures are no-ops (never regress a warm value). Pending (known-but-not-connected) targets prefer their warm cache value over the local accent — only a genuinely cold target falls back to local. Re-resolve only when the node context changed across `handle_params`; never an RPC on the render path. No PubSub — the accent is static per node. Detail: `live_hooks/CONTEXT.md`. Unlike the per-project `--project-accent`/`--project-ring-accent` inline custom properties on `#dashboard-root` (next bullet), this is the user-configured GLOBAL accent applied shell-wide via a data attribute.
- **Server→DOM CSS custom properties — exactly ONE site**: `live/projects_live.ex:101-103`, `#dashboard-root` inline `style="--project-accent: #{ThemeColor.accent_color_for_mode(@task_mode, @task_resume_from)}; --project-ring-accent: #{ThemeColor.accent_color(@active_project && @active_project.name)}"`. Values from `theme_color.ex` (`EvoDashWeb.ThemeColor`): mode accents are **OKLCH strings** — genesis_new `oklch(0.62 0.19 25)` red, genesis_existing `oklch(0.62 0.19 255)` blue, evolve `oklch(0.72 0.17 152)` green (resume `0.78 0.16 152`), custom_agent `oklch(0.68 0.17 290)` violet (resume `0.78 0.16 290`), unknown/nil → default **hex `#6366f1`** indigo-500 (theme_color.ex:19,84-114); project ring accent `accent_color/1` = `:erlang.phash2(name, 360)` → HSL (sat 70 / light 54) → hex (theme_color.ex `accent_color/1` + public `hsl_to_hex/3`; continuous 0-360 hue, no quantization). CSS consumers: top-bar palette trigger hover `hover:ring-(--project-ring-accent)/50` (project_components.ex:285/359/756); `.input-card` accent + Launch-button `[data-mode]`/`[data-resume]` hover rings in app.css (Launch button carries `data-mode` at task_form_components.ex:~160-167; `data-resume={String.trim(@resume_from) != ""}`). Second server→DOM color site: `live/system_live/charts.ex` legend dots/SVG emit STATIC CSS-var strings — inline `style="background-color: var(--chart-…)"` / `style="stroke: var(--chart-…)"` (no server-computed chart colors; per-theme hex values live in the app.css theme blocks as `--chart-*`, see `assets/CONTEXT.md`).
- **Accent-token split — `text-primary-standalone` vs `text-primary`**: markup/text-role accent consumers were flipped to the standalone token (`text-primary-standalone`, resolving `var(--color-primary-standalone)` — hand-tuned per accent/theme for ≥4.5:1 on base surfaces, defined in `assets/css/app.css`). Roles that FLIPPED: (a) emphasis text/icons on base surfaces (spinners `text-primary-standalone`, section header icons, key-path code chips, cost/agent-count figures, suggestion-chip icons, guide sparkle, project folder icon); (b) text on `bg-primary/10` tints — the tint fill STAYS `bg-primary/10`, the text flips (active sidebar nav row, language-selector + node-selector selected rows and their check glyphs, review file-tree selected row, quick-select buttons, search-results category chip); (c) outline roles — focus rings (`focus:ring-primary-standalone/…`, `focus-visible:ring-primary-standalone`), decorative borders/underlines (review tab underline `border-primary-standalone`, editor form borders, agent-card selection rings `ring-primary-standalone`, suggestion-chip hover borders). Roles that STAY on the fill token: solid fills (`btn-primary`, `bg-primary` solids, `badge-primary`, `toggle-primary`, progress bars, `selection:bg-primary/30`) and any text sitting ON a solid fill → `text-primary-content` (settings sidebar active row, Home Send button, repo-header icon on `bg-primary`). The daisyUI classes `btn-primary.btn-soft`, `btn-outline.btn-primary`, and `link-primary` have their foregrounds routed to the standalone token BY THE CSS — markup unchanged.
- **Semantic surface keyed to `--color-primary` (adapts with a DaisyUI accent)**: Launch button `btn btn-primary` (task_form_components.ex:363), guide "Go" (layouts.ex:411), Retry (remote_gate_components.ex:85), GitHub per-issue Fix (github_components.ex:160), task-card Review (task_card_components.ex:162), Export JSON `btn-outline btn-primary` (review_components.ex:258, archive_components.ex:32), Settings active category `bg-primary text-primary-content` (settings_components/sidebar.ex:63, chips :78/:84 `bg-primary-content/20 text-primary-content`; INACTIVE chip = `bg-primary/10 text-primary-standalone`), Settings Save/Add buttons, Home Send `bg-primary text-primary-content` (home_live.ex:151), active pagination (tasks_live.ex:271), update Download/Restart (system_live.ex:146/170), welcome CTAs, `toggle-primary` (task_form archive :580, setting_card :144), CoreComponents default button variant = `btn-primary btn-soft` (core_components.ex:112). **Review Merge is NOT primary**: `btn btn-success` (review_components/actions.ex:54/66), Reject `btn-outline btn-error` (:77), Resume `btn-outline btn-success` (:110), auto-resolve `btn-warning` (:218), danger force-kill `btn-error` modals.
- **Accent-substitute surfaces now track the standalone token** (were indigo literals; text roles use `text-primary-standalone`, tint fills stay `bg-primary/10` so they follow the accent): active sidebar nav row + icon (layouts.ex:506/516), guide sparkle icon (layouts.ex:388), language-selector active row/check (layouts.ex:750/760), body text-selection `selection:bg-primary/30` (root.html.heex:128 — fill role, kept), node-selector selected row + check (live/components/node_selector_component.ex — `bg-primary/10 text-primary-standalone`, check `text-primary-standalone`). Settings sidebar active state = `bg-primary text-primary-content` (unchanged).
- **Task-status colors — ONE coherent semantic scheme, single source of truth in `EvoDashWeb.Helpers`** (LOCKED mapping: transitional/busy states = warning amber family; terminal good = success; terminal bad = error; idle/terminal-neutral = neutral gray — distinct states show via label/animation, not hue). `task_status_badge/1` (helpers.ex): running/finalizing/cancelling → `bg-warning/10 text-warning`, completed → `bg-success/10 text-success`, failed → `bg-error/10 text-error`, cancelled/pending → `bg-base-content/10 text-base-content/60`. NEW shared helpers (added with the Adwaita overhaul; all return class strings WITHOUT animation — call sites keep pulse/ping): `task_status_dot_class/1` (running/finalizing/cancelling `bg-warning`, completed `bg-success`, failed `bg-error`, pending/cancelled `bg-base-content/40`, fallback `bg-base-content/30`), `task_status_tint/1` (busy `bg-warning/5 shadow-warning/10 border-warning/20`, completed `bg-success/5 shadow-success/10 border-success/20`, failed `bg-error/5 shadow-error/10 border-error/20`, pending/cancelled/fallback `bg-base-200/40 border-base-300/20`). Consumers: sidebar dots (layouts.ex `task_status_dot_class(task.status)` + `animate-pulse`), task cards (`task_card_components.ex` — `status_accent_color/1` delegates to the dot helper; `task_card_tint/1` keeps ONLY the completed+review_status variants (merged→success, rejected→error, continued→secondary, ignored→neutral) and delegates the plain-status catch-all to `Helpers.task_status_tint`; the cancelling ping badge is the warning dot), home chat `assistant_message.ex` (`card_border/1`: busy → `border-warning/25`, completed → `border-success/25`, failed → `border-error/25`, cancelled/pending/fallback → `border-base-200/60`; ping dots via the dot helper). Update-status dots (layouts.ex:538-540): `:available` → `bg-warning`, `:ready` → `bg-success animate-ping`. Agent-status helpers (helpers.ex agent_status_*) are unchanged (semantic tokens). Connection-status dots/badges are ALSO deduped into helpers: `connection_status_dot_class/1` (local `bg-info`, connected `bg-success`, connecting/disconnecting `bg-warning`, error `bg-error`, disconnected/unknown/fallback `bg-base-content/40`) and `connection_status_badge_class/1` (`badge-ghost|badge-success|badge-warning|badge-error` modifiers; settings_live composes `"badge badge-sm " <> modifier`) — consumers: `live/components/node_selector_component.ex` and `live/settings_live.ex` (local per-file dot helpers DELETED).
- **Shell chrome is token-based (Adwaita layering) — no slate/white literals remain**: body (root.html.heex:128) `bg-base-200 text-base-content antialiased selection:bg-primary/30`; `#app-layout` + sidebar + hamburger (layouts.ex:77/84/116) `bg-base-200`, sidebar `border-r border-base-300`, hamburger `border-base-300 shadow-md`; mobile overlay `bg-black/50` (kept); `#main-scroll` (layouts.ex:264) + `main#main-content` (:276) = `bg-base-100` (view tone — the INVARIANT holds: the content background class lives on the scroll container); wordmark gradient (layouts.ex:136) `from-base-content to-base-content/60`; config-warning banner `bg-warning/10 text-warning border-warning/30`; guide panel `bg-base-100 border-base-300`; theme toggles `bg-base-300 ring-base-content/10` / thumb `bg-base-100`; separators in system_live.ex:223, charts.ex, runtime_controls.ex:36, settings_live.ex:41, CoreComponents accordion (`border-base-300`, hover `hover:bg-base-200`) all `border-base-300`. Modal backdrops stay `bg-black/30|50 backdrop-blur-sm` (unchanged, ×11).
- **Charts are theme-consistent via `--chart-*` CSS vars** (no hex palette in charts.ex): series colors are module attributes holding `var(--chart-capacity|llm|tool|waiting|agents|agents-waiting|pending)`; SVG strokes/fills are inline `style="stroke: var(--chart-…)"` (CSS `var()` is invalid in SVG presentation attributes), legend dots `background-color: var(--chart-…)`, gridlines `style="stroke: var(--color-base-content)"` at 0.08. Per-theme hex values live in the app.css theme blocks (light/dark series, `assets/CONTEXT.md`). Chart pins in `charts_test.exs` assert the var strings.
- **Locale vs theme are fully separate**: `Plugs.Locale` (plugs/locale.ex:37-49; cookie → accept-language → "en", `put_session(:locale,…)`), `LiveHooks.SetLocale` re-applies it in LiveView processes (live_hooks/set_locale.ex:11-13); root.html.heex L3-4/13 render `lang`/`dir` + `<meta name="locale">`; the inline script's locale section (L80-124) persists `localStorage "phx:locale"` + `locale` cookie + rtl direction, reloads on cross-tab change. The `live_hooks/` node itself contains ZERO markup/color code (100% assign/attach; dialogs/dots render in Layouts; `guide_highlight` pushes only a selector, `.guide-highlight` styling = CSS `outline: 2px solid var(--color-primary)`).

## Design Conventions — Adwaita token grammar (markup)

Durable rules from the Adwaita restyle audit + markup polish pass (all audit defect sites are fixed; per-line audit history: `git log -p -- CONTEXT.md`; sibling convention records: `components/CONTEXT.md`, `live/system_live/CONTEXT.md`, `live/components/CONTEXT.md`). Apply when writing NEW markup:

- **Alpha floor**: never below `/50` for user-relevant text; labels/meta ≥ `/60`; small/primary content ≥ `/70`. Fold `text-[10px]`/`text-[11px]` to `text-xs` at ≥ `/70`. Decorative large icons may keep `/30`–`/40`; empty/loading states get exactly ONE dim layer.
- **Hairlines/borders on content surfaces**: `border-base-300` (base-200 on a base-100 surface is ≈1.06:1 invisible; base-200 is only correct on base-200 chrome). Cards/panels `rounded-xl` max (prefer `rounded-lg`); NORMAL action buttons `rounded-lg`/`rounded-md`; pill badges may keep `rounded-full`.
- **`bg-base-50` is a dead class** — only base-100/200/300 tokens exist in both custom themes; it compiles to no fill.
- **`*-content` tokens must never sit on translucent tints** (polarity-swapped per theme — e.g. `bg-success/20 text-success-content` is invisible in both) — use full `text-<semantic>` on `bg-<semantic>/10..20`; `-content` only on the solid fill.
- **`badge-ghost` on tinted/gray strips = gray-on-gray** → `badge-outline border-base-content/25 text-base-content/80` grammar.
- **No raw color literals** (`text-red-500`, `bg-white/10`, `bg-black/10`, `bg-neutral text-neutral-content` — the last two are unmapped stock DaisyUI tokens) → theme tokens (`text-error`, `bg-primary-content/10`, `bg-base-content/5`, `bg-base-200`).
- **Sidebar brand wordmark** is plain `text-base-content` — no gradient/bg-clip-text (the `/60` tail fails on the base-200 sidebar and `truncate` ellipsis breaks bg-clip-text).
- **`--color-secondary` IS mapped in both themes** — `badge-secondary`/`text-secondary`/`bg-secondary/*` are usable; for small body text (≈3.8:1) prefer `text-base-content/80`.
- Locked single-source schemes — never "fix": `Helpers.task_status_*`/`agent_status_*`/`connection_status_*` class mappings (tests pin them), chart `--chart-*` CSS-var strings, `theme_color.ex` palette, the home chat bottom fade.
