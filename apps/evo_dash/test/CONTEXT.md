# Test Directory

## Intent
Holds all test files for the EvoDash application. Provides the ExUnit test runner configuration and shared test support modules (e.g., `ConnCase`) used across controller and endpoint tests.

NOTE: The domain-layer modules (`Store`, `TaskRegistry`, `TaskInfo`, `RecentProject`) live in the `:evo_git` app as `EvoGit.Store`, `EvoGit.TaskRegistry`, `EvoGit.TaskInfo`, `EvoGit.RecentProject` and are tested there — this app has no domain-layer test suite for them. Web-layer tests reference `EvoGit.TaskRegistry` / `EvoGit.Store` / `EvoGit.TaskInfo` directly (isolated setup via `EvoGit.Store` / `EvoGit.TaskRegistry` under `EvoDash.Supervisor`).

## Routing Table
- `support/` → Shared test support modules (ConnCase for connection based tests)
- `evo_dash/` → Domain-layer tests (DirectoryPicker, MarkdownRender, NodeContext)
- `evo_dash_web/` → Web-layer tests (controller tests, error handler tests, component tests under `evo_dash_web/components/`)
- `evo_dash_web/live_hooks/` → NodeAware live-hook tests (`node_aware_test.exs`)

## API Surface

### Top-Level
- `test_helper.exs` — Bootstraps ExUnit (`ExUnit.start()`). Entry point for the test suite.

### `support/`
- `conn_case.ex` — `EvoDashWeb.ConnCase` module: a shared `ExUnit.CaseTemplate` for connection-based tests. Provides:
  - Endpoint registration (`@endpoint EvoDashWeb.Endpoint`)
  - Verified routes via `use EvoDashWeb, :verified_routes`
  - Imports: `Plug.Conn`, `Phoenix.ConnTest`, and the case module itself
  - Default setup returning a built `%Conn{}`
- `test_helpers.ex` — `EvoDashWeb.TestHelpers`: shared `flush_loading/4` — polls the LiveView test proxy's rendered HTML until a loading marker disappears (async `Task.Supervisor`-backed page loads) or `flunk`s on timeout. The LiveView test files' private `flush_*_load` helpers are one-line delegates to it.

### `evo_dash/`
- `markdown_render_test.exs` — `EvoDash.MarkdownRenderTest` — Markdown-to-HTML rendering edge cases (nil, empty, headings, code blocks, tables, bold).
- `directory_picker_test.exs` — `EvoDash.DirectoryPickerTest` (async: false) — Real picker GenServer + `EvoDash.DirectoryPicker.Wx.Fake` (per-test `enabled: true` override + `:directory_picker_wx` env injection): `pick/2` and `pick/3 (file mode)` describes — file pick delivers the file path via the fake's `new_file_dialog` + ref-typed `get_path`, `pick/2` ≡ `pick/3 :directory`, kind-agnostic busy serialization (`set_gate/1` dialog block), wx init-failure/server-death degradation to `:unavailable` with busy cleared, disabled-config rejection.

### `evo_dash_web/components/`
- `project_components_test.exs` — `EvoDashWeb.ProjectComponentsTest` — Component-level tests for the command-palette project selector (`project_omnibox/1`): trigger rendering (active project name/path, "Open a project..." placeholder, enlarged `px-4 py-2` trigger + `text-base font-bold text-base-content truncate leading-tight` name span typography), and the client-side wiring — the search input (`input#palette-search-input`) carries `phx-keydown="palette_keydown"` + `phx-change="palette_search"`, the overlay (`.project-palette-overlay`) carries `phx-click-away="close_project_palette"`, and the backdrop (`.project-palette-backdrop`) carries `phx-click="close_project_palette"`. Uses `render_component/2` + Floki helpers (`trigger_class/1`, `attribute/3`, `parse/1`). NOTE: the active_project fixture must include a `:path` key (the component renders `@active_project.path` unconditionally).
- `task_form_components_test.exs` — `EvoDashWeb.TaskFormComponentsTest` — `layout_for/1` boundary tests (600-char / 16-line thresholds, non-binary fallback; the client hook additionally flips to expanded on a height-based trigger — see `assets/js/hooks/adaptive_input.js`) plus rendering smoke tests for the UNIFIED control order: both layouts share identical DOM and visual order — mode (order-1) | Launch (order-2, centered via `mx-auto`) | model (order-3); only the textarea size differs per layout. Also covers the no-model-profiles case (Launch stays centered, no `name="model_id"` select), disabled state, mode select options, and the textarea bindings — `phx-update="ignore"` + `phx-hook="AdaptiveInput"` with NO per-keystroke server event (layout switching is client-side via the AdaptiveInput hook). A one-line contract test ("controls row stays on one line (flex-nowrap)") pins `.input-controls` as `flex-nowrap` (never `flex-wrap`) with `min-w-0` + `truncate` on both selects. Also covers the attach-file "+" button (`button#objective-file-button`): renders only when a project is active (`unless @disabled`) with `phx-hook="FilePicker"` + `data-picker-id="objective_file"` + `type="button"` + `aria-label`/`title` "Attach file" + `hero-paper-clip` icon + `absolute top-2 right-2` classes, and is NOT inside `.input-controls` (pins the placement so the 3-children DOM-order test stays valid).
- `diff_viewer_test.exs` — `EvoDashWeb.DiffViewerTest` — Unit + component tests for `EvoDashWeb.ReviewComponents.DiffViewer` (the review diff viewer). 20 tests in 3 describes: (1) `parse_hunk_header/1` (5 tests, pure); (2) `build_split_pairs/1` (9 tests, pure pairing algorithm — context on both sides, deletion-left/addition-right, consecutive del+add zipping with `nil` padding, line-number advancement, empty body, deleted/new files); (3) `split-view rendering` (6 tests) — pins the current rendering contract: diff content renders as ESCAPED plain text (no HTML injection from `<script>`/`<div>` inside the diff), NO server-side highlight markup (no `hljs-`/`lumis`/`l-line` classes, no `<span style=`), a `data-language` attribute on each `.diff-file-section` div (asserted via Floki), hunk headers render as plain text, split-view pairs render with gutters/content on both sides, and the file header shows path + diff stats. Floki is used purely as a generic HTML query helper (test-only dependency, bundled pure-Elixir Mochiweb parser). NO other test file asserts on diff body content — `review_live_test.exs` renders the review page (even with real git repos) but only asserts merge-check/action UI, never the diff body.

### `evo_dash_web/live/`
- `home_live_test.exs` — `EvoDashWeb.HomeLiveTest` (async: false) — The **Home chat page** (`GET /`, ChatGPT-style chat wired to the `:reflect` self-reflective agent; the Projects page now lives at `GET /projects`). 22 tests: idle render (message input + Send ENABLED, New chat enabled, Stop disabled), send-message (real `render_submit` → assert the persisted task ROW via `EvoGit.Store.safe_select_all_tasks`: `mode: "reflect"`, NO `:path` key; FIRST message objective = bare text, a SECOND message's objective = `Transcript.build_preamble` + `"New message: "` + text), whitespace no-op, new-chat reset (transcript cleared → empty state, input re-enabled), onboarding dead-render redirect (`{:error, {:live_redirect, %{to: "/welcome"}}} = live(conn, "/")` when `EvoGit.Config.VersionState.onboarding_needed?/0` — per-test XDG_CONFIG_HOME isolation), streaming display (3 — inject `{:agent_registered, id, summary, node()}` with `Map.get(summary, :task_id) == chat_task_id` + `{:agent_updated, id, [:message_count], node()}`; agent history arrives via home_live's OWN result messages `{:chat_agent_lookup, node, seq, task_id, agent}` / `{:chat_history_loaded, node, seq, agent_id, result}` — the two-seq stale-guards (`chat_fetch_seq`/`chat_task_fetch_seq`) are read dynamically from `assigns(view)` right before injection; foreign-node events ignored), completion/error rendering (8 — seed a `%EvoGit.TaskInfo{}` fixture via `EvoGit.Store.put_task` and inject `{:task_updated, id, :completed|:cancelled|:failed, node()}` → `AgentStream.extract_final_text(result)` text / "Stopped." / "The task failed."; `{:task_deleted, id, node()}` → "The conversation was deleted."), stop/cancel flow (2 — `render_click(view, "stop")` → `EvoDash.NodeContext.cancel_task`; `:cancelling` state; then inject `:cancelled` event → final state), node-awareness (3 — fake ConnectionManager registered in `EvoGit.RemoteConnection.Registry` (copy `node_aware_test.exs` pattern), `?node=` param resolves, task start routes through NodeContext, node switch resets the chat). Uses an isolated Store+TaskRegistry (terminate/restart production children, temp SQLite, per-test XDG_CONFIG_HOME). **IMPORTANT — ETS sweep**: the real-send tests (8) spawn a REAL reflect agent that stays alive/blocked (no LLM creds in the isolated config); `cleanup_task_on_exit/1` cancels + deletes the task AND sweeps `:evogit_sched_meta`/`:evogit_agent_state` rows by task_id (cancel/delete do NOT remove them) — without the sweep the blocked agent leaks into `agents_live_test.exs` (which expects an empty agent registry) and fails 8 of its tests. Real-send tests never assert `chat_status` synchronously (the reflect runtime fails fast / races); assert persisted rows and branch on `chat_task_id(view)` nil.
- `settings_live/model_profile_helpers_test.exs` — `EvoDashWeb.SettingsLive.ModelProfileHelpersTest` — Pure unit tests (async, no LiveView) for `ModelProfileHelpers`: draft-profile dropping/keeping, quick-setup regression (no profile without `:model`), `parse_model_profile_params/2` serialization (compact `"provider:model"` string vs map spec with overrides), profile **id naming** (derived from model value: after-first-colon / plain string / map `:id`, slugified), conflict suffixes (`-2`, `-3`), `profile-N` fallbacks, and `generate_profile_id/2` (base/base-2/base-3 skipping existing ids, nil/one-arity → `profile-N`).
- `welcome_live_test.exs` — `EvoDashWeb.WelcomeLiveTest` — Welcome/onboarding page: flat model grid (multi-provider, variants, alphabetical sort), search, all-set state, skip/get-started redirects (to the `/welcome/complete` page), credential detection, **back navigation** (Back button with client-side `history.back()` fallback — NOTE: the rendered onclick HTML-escapes quotes, so assert `window.location.href = &#39;/&#39;;`), and the **LLM connection test** UI (Test Connection button only when a model is selected, `:testing` spinner, `Connected` + model display name, error reason, status reset on model select). The last test ("typed API key is saved before the connection test runs") spawns a real background task with a bogus key that fails fast — same precedent as settings_live_test. The example-task teaching section test asserts the `/welcome` page does NOT render "Try an example task" / the example objective / `welcome-example-copy` — the example lives on `/welcome/complete` (see `welcome_complete_live_test.exs`).
- `welcome_complete_live_test.exs` — `EvoDashWeb.WelcomeCompleteLiveTest` — Dedicated onboarding-completion page (`/welcome/complete`): renders the high-level-prompt explanation + example objective (`EvoDashWeb.ExampleTask.example_objective()`, asserted via HTML-special-free distinctive lines since `<`/`&` are escaped in the `<pre>` block) + copy button (`welcome-example-copy`, `ClipboardCopy` hook, `data-content`), the `"copied"` event info flash, and the "Go to Dashboard" CTA — clicking `go_to_dashboard` asserts `assert_redirect(view, "/projects")` (the Projects page moved to `/projects` when the Home chat page took over `/`) AND onboarding completion via `EvoGit.Config.VersionState` (`onboarding_needed?() == false`, version file exists, `get_version() == current_version()`), under the same per-test temp `XDG_CONFIG_HOME` isolation as `welcome_live_test.exs` (VersionState's `:persistent_term` cache is path-keyed, so unique temp dirs need no manual invalidation).
- `projects_live/project_flow_test.exs` — `EvoDashWeb.ProjectsLive.ProjectFlowTest` — Pure unit tests (async, no LiveView) for `ProjectFlow.normalize_project_path/1` contract (blank → `:blank`; bare/volume-relative/root-relative/`~foo` → `:relative` — never cwd-joined; Windows drive paths asserted shape-only, no filesystem access; `/tmp/foo` idempotent; `~/foo` home-joined), the cross-OS remote variants `normalize_remote_project_path/2` (POSIX/Windows absolute pass-through verbatim, tilde via the `:remote_path_expand_runner` seam) and `absolute_path_for_node?/2`, node-aware `build_foreign_repo/4` (remote nodes store the raw root verbatim; local nodes keep `ForeignRepo.new/3` semantics), node-aware `Project.load_foreign_repos/3` genesis.toml loading, and `Project.path_suggestions/2,3` (relative recents entries filtered out; absolute input yields only absolute suggestions; `filesystem_suggestions/1` is `defp` — test through the public `path_suggestions`).
- `projects_live/state_persistence_test.exs` — `EvoDashWeb.ProjectsLive.StatePersistenceTest` — Pure unit tests for `StatePersistence.maybe_restore_foreign_repos/2` using minimal `%Phoenix.LiveView.Socket{}` structs (pattern from `node_aware_test.exs`): remote sockets restore foreign-repo roots verbatim (node-aware raw storage), local sockets keep `ForeignRepo.new/3` expansion, nil/empty/invalid inputs leave the socket unchanged.
- `projects_live_test.exs` — `EvoDashWeb.ProjectsLiveTest` — Projects page: task form, project palette, project settings, task notifications. The Projects page is served at **`GET /projects`** — the Home chat page (`EvoDashWeb.HomeLive`, tested in `home_live_test.exs`) owns `/` — so the suite navigates with `live(conn, ~p"/projects")` / `~p"/projects?node=..."` and assert-redirects to `"/projects"`. Includes the "project path normalization regression" describe block (create/open/select_project reject relative input with the "Enter a full path" flash, no dir created, no recents registration; absolute-but-missing still flashes "Directory does not exist"; relative `?project=` URL param silently ignored). NOTE: this file is ~1863 lines — a legitimately large suite; don't split casually. **Empty-state contract**: when no project is active the launch panel (`.input-controls`, `hero-rocket-launch`) is NOT rendered — instead the hint overlay `"Open a project to get started"` shows. Tests in "projects page without active project" and "task notifications" (which clear recent projects) pin this with `refute html =~ "hero-rocket-launch"` + the hint string. Tests with an active project still assert the launch button. Includes the "directory picker" describe (browse flows: remote-node push-unavailable, disabled-config, fake-module picked-path push — the fake delivers its result synchronously during `render_hook`, `assert_push_event` catches the handle_info-driven push) and the "file attach" describe (`file_pick` event + `"objective_file"` picker id: happy path pins the EXACT appended block `"\n\n---\n## Attached file: <basename>\n\n<content>\n"` + the `task_prompt`/`file_pick_bases` assigns, missing-file error flash "Failed to attach file", `:cancelled`/`:unavailable` pushes, remote-node + disabled-config immediate-unavailable, and the **manual fallback** `file_pick_manual` event: valid-path append pins the same block/assigns, empty/nil/missing-params/nonexistent-path → `%{error: true, reason: ...}` push with no LiveView crash).
- `settings_live_test.exs` — `EvoDashWeb.SettingsLiveTest` — Settings page: search input rendering, search handler (value-key matching, results panel, no-results message, clear-to-category-view), and search-input-within-form regression. Also covers custom model providers (OpenRouter / OpenAI-Compatible): form rendering (model name, base URL, placeholders, warning, hidden quick-select buttons), saving (openrouter: spec pre-fill + openai-compatible map spec pre-fill), and validation errors (empty model name, empty base URL). Also covers whitelist safety regression: unknown category/provider/variant/key-path values safely map to nil/default instead of crashing (with positive cases confirming valid conversions).
- `tasks_live_test.exs` — `EvoDashWeb.TasksLiveTest` — Cross-project task list: search (by prompt/objective/ID, case-insensitive), filter selects, pagination, node-aware behavior, `:cancelling` status display, review button, and the cancel/force-kill modal actions. **Event-driven broadcast tests** (the page is fully push-based — no polling): the ":task_updated broadcast handling" describe pushes `{:task_updated, id, status, node()}` / `{:task_deleted, id, node()}` manually via `send(view.pid, ...)` (does-not-crash + catch-all for unknown messages), and the "async page load" describe covers stale page-load results (dropped by the `tasks_load_seq` stale-guard), a matching-node `{:task_updated, ...}` broadcast triggering the debounced reload (asserted with the two-phase `:tasks_reload_pending` true→false pattern), foreign-node broadcasts being ignored, and `{:task_deleted, ...}` triggering a debounced reload. Uses an isolated TaskRegistry + TaskStore (unique temp root + SQLite database file; terminates production children and restarts them in `on_exit`, `async: false`). Async page loads are flushed via `EvoDashWeb.TestHelpers.flush_loading/4` (file-local `flush_tasks_load/2` delegate).
- `platform_info_test.exs` — `EvoDashWeb.PlatformInfoTest` — Pure unit tests (async, no LiveView harness) for `EvoDashWeb.PlatformInfo` (platform-gating helper used by Settings/System pages): `os_for_node/1` with the `Application.put_env(:evo_dash, :platform_os_override, ...)` injection seam (known-atom override wins, `:unknown` passes through, bogus values ignored, fake remote node → `:unknown` without raising), `show_sandbox?/1` + `sandbox_backend_for/1` truth tables, `filter_schemas_by_category/2` for linux/windows/unknown/macos, and the **round-trip safety pin** — `ConfigIO.build_config_from_category_params/4` with macOS-filtered schemas must NOT deep-delete hidden sandbox keys (`[:sandbox, :resources]`/`[:sandbox, :linux]`) absent from form params (the save-path protection behind the macOS filtering). NOTE: the `:atom`-typed `[:sandbox, :mode]` fixture schema needs `validation: [in: [:auto, :enabled, :disabled]]` for `"auto"` to parse via `SettingsUtils.parse_atom/2`. `sandbox_backend_for/1` remains the OS-only static fallback (`:linux→:systemd_run`), while the live UI backend clauses additionally handle `:bwrap` — `Status.format_backend/1`, `Status.sandbox_status/1`, and the `SettingsComponents` sandbox banner — with unit + integration coverage in `system_live_test.exs` ("Status sandbox helpers — bwrap" describe + a Linux `:platform_os_override` badge test).
- `review_live_test.exs` — `EvoDashWeb.ReviewLiveTest` — Review page: non-existent task error display, and the "ignore" action (button always shown; clicking sets review_status to :ignored and navigates to the Projects page — post-action redirects target `/projects` since the Home chat page owns `/`). Uses the production TaskRegistry + TaskStore directly (seeds tasks via `EvoGit.Store.put`, cleans up via delete_task).
- `error_html_test.exs` — `EvoDashWeb.ErrorHTMLTest` — Validates HTML error templates (404 → "Not Found", 500 → "Internal Server Error").
- `error_json_test.exs` — `EvoDashWeb.ErrorJSONTest` — Validates JSON error responses (404/500 with appropriate `%{errors: %{detail: ...}}` shape).

### `evo_dash_web/live_hooks/`
- `node_aware_test.exs` — `EvoDashWeb.NodeAwareTest` (async: false) — Tests `EvoDashWeb.LiveHooks.NodeAware` WITHOUT booting a real LiveView: builds a minimal `%Phoenix.LiveView.Socket{}` via a `socket/1` helper (assigns incl. `__changed__`, `redirected: nil`), asserts `push_patch` via a `patch_to/1` extractor (`redirected: {:live, :patch, %{to: path}}`). Covers `handle_connection_status/2` transitions (local→remote `:connected` and remote→local `:disconnected`/`:error` push_patch; non-transitions do NOT), `load_running_and_pending_tasks/1` node-aware sourcing (local `TaskRegistry.list_tasks_summary(@active_statuses)` / remote `NodeContext.list_tasks_summary/2` RPC with fast-fail `:noconnection` / pending-remote empty-list guard), `assign_node/2` sidebar reload + `:remote_status` assign. Uses an isolated Store+TaskRegistry (`setup_isolated_registry` — terminates production children, starts fresh temp SQLite, restarts in on_exit), a fake `ConnectionManager` GenServer registered in `EvoGit.RemoteConnection.Registry` (dies at test end via `start_supervised!`), and per-test `XDG_CONFIG_HOME` isolation (`isolate_config_dir`).

## Notes for Agents — testing the push-based event contract

The dashboard is fully push-based (node-identity PubSub events on `EvoGit.PubSub`; see "Push-based event contract" in `apps/evo_dash/CONTEXT.md`). LiveView tests drive events by **manual injection** — `send(view.pid, {:task_updated, id, status, node()})`, `send(view.pid, {:task_deleted, id, node()})`, `send(view.pid, {:agent_updated, id, changed_fields, node()})`, `send(view.pid, {:system_sample, node, seq, sample})`, etc. — with **foreign-node broadcasts** (`node != node()`) asserted ignored. Async page loads still run in `EvoDash.TaskSupervisor` children that `render_async/2` cannot see: flush with `EvoDashWeb.TestHelpers.flush_loading/4` (file-local `flush_*_load/2` delegates) and stale-guard paths with `send(view.pid, ...)` result-message injection (`{:tasks_page_loaded, seq, node, result}`, `{:agents_data_loaded, ...}`, `{:agents_refresh_result, seq, node, result}`, `{:system_samples_seeded, seq, node, result}`). The 300ms reload debounce is asserted with the two-phase `:tasks_reload_pending` true→false pattern (tasks_live_test.exs). SystemLive chart tests use the `:system_samples_runner` app-env seam (stub the seed RPC) + `await_view_assign/3` view-assign polling + `await_ets_count/4` call-count assertions (system_live_test.exs). **Any test asserting on `chart_samples` MUST stub the seed runner** (`Application.put_env(:evo_dash, :system_samples_runner, stub)`) in the test body BEFORE mounting the view — the runner is resolved at spawn time. The default runner reads the REAL `EvoGit.SystemSampler` ring buffer, which is non-empty whenever any sampler tick has occurred in the VM: umbrella `mix test` runs evo_git + evo_dash in ONE VM, and evo_git's `system_sampler_test.exs` ticks the registered sampler via the public `tick/0` seam (observed leak in the foreign-node test: `llm_capacity: 25`/`tool_capacity: 8` = real user config). `config/test.exs:32` disables the periodic TIMER only (`:system_sample_interval_ms, 86_400_000`) — it never protected against manual ticks. Full story: `./evo_dash_web/live/CONTEXT.md` → Notes for Agents.

## Notes for Agents — task-cancellation UX tests

The backend ships `EvoGit.RemoteNode.cancel_task/2` (graceful: `:pending`/`:running` → `:cancelling` → persisted `:cancelled` with result preserved) and `EvoGit.RemoteNode.force_kill_task/2` (brutal: kills agents+wrapper from `:running`/`:cancelling`, result nil'd), with `EvoDash.NodeContext.cancel_task/2` and `force_kill_task/2` delegating. Current test coverage and facts:

- **`:cancelling` is store-safe**: `EvoGit.Store.Codec`'s `@known_atoms` whitelist includes `:cancelling` (and `:cancelled`); `Store.put_task` round-trips it (verified in evo_git `store_test.exs:1452-1478`). A fixture is created with the standard `insert_fixture!(status: :cancelling, ...)` idiom (tasks_live_test.exs:36-55, node_aware_test.exs:48-67) — direct `EvoGit.Store.put_task(EvoGit.Store, %TaskInfo{status: :cancelling, ...})` — no backend changes needed.
- **`filter_tasks` has NO status whitelist**: `status_filter` stays a string end-to-end (tasks_live.ex:482-491) and binds into SQL as a string (`Store.Queries.build_where`), so the filter `<option value="cancelling">` (tasks_live.ex:39-62) works without core changes.
- **Cancel-button affordance**: task_card_components.ex shows the graceful Cancel button for `[:pending, :running]` (line 139); the three-dot dropdown "Force kill" item shows for `[:running, :cancelling]` (line 183) with a "Danger zone" divider. The menu `<ul>` is always in the DOM (CSS-hidden) so tests assert item presence/absence directly. `:finalizing`/`:cancelling`/terminal statuses get NO cancel button.
- **`partition_active_tasks/1` unit tests**: `node_aware_test.exs` has a pure `describe "partition_active_tasks/1 — pure partitioning"` (`:cancelling` → running partition; `:cancelled` → not; `:running`/`:pending`/`:finalizing` regression) plus an integration test in the `load_running_and_pending_tasks/1` describe (a `:cancelling` fixture — `finished_at: nil` — appears in `running_tasks`).
- **Cancel/force-kill handler tests**: `tasks_live_test.exs` has `describe "cancelling status display"`, `describe "cancel task action"`, `describe "force kill action"` (15 tests) covering the modal open/close idiom, `:pending`→`:cancelled` and `:running`→`:cancelling` store-verified transitions, `:completed`→"Failed to cancel task" flash, nil-guard no-ops, filter `value="cancelling"`, and the force-kill success path via `:sys.replace_state(EvoGit.TaskRegistry, ...)` owned-wrapper injection (fun runs inside the registry process so `owner: self()` satisfies `Task.shutdown/2`). `helpers_test.exs` has the exact-string `:cancelling` badge test + rounded-full loop entry + differs-from-fallback test.
- **`node_context_test.exs`** (`test/evo_dash/`): `EvoDash.NodeContext.cancel_task/2` / `force_kill_task/2` delegation smoke tests on the real local path (`{:error, :not_found}` for missing ids) + a `:pending`→`:cancelled` round-trip (`:pending` cancels immediately — no live wrapper needed).
- **Force-kill msgid**: the force-kill error-flash test asserts the msgid `"Failed to force kill task"` (tasks_live.ex:807). Do NOT weaken that assertion back to the old shared `"Failed to cancel task: %{reason}"` wording.
- **Modal-test idiom** (system_live_test.exs:201-306): `render_click(view, "request_X")` opens (assert modal title string appears), `render_click(view, "cancel_X")` closes (refute). Handlers that would tear down the local VM are tested via direct `SystemLive.handle_event/3` on a hand-built `%Phoenix.LiveView.Socket{}` with a non-local `current_node` (safe: RPC to a non-existent node degrades to `:ok`); the local VM-killing confirm paths are intentionally NOT unit-tested (comment at system_live_test.exs:178-185).
- **Dropdown idiom** (projects_live_test.exs:513-541): dropdown content stays in the DOM (hidden via CSS); tests assert the `phx-click="close_configure_dropdown"` click-catcher overlay appears/disappears on `toggle_configure_dropdown`.

## Notes for Agents — attach-file (picker file-mode) tests

The dashboard attach-file flow (`file_pick` event → `EvoDash.DirectoryPicker.pick/3 :file` → `handle_info({:directory_picker_result, "objective_file", ...})` in projects_live.ex) is tested end-to-end via the two support fakes:

- **Module-level fake** (`EvoDash.DirectoryPicker.Fake`, installed via the `:directory_picker_module` env): `pick/2` and `pick/3 :directory` always deliver `{:ok, "/fake/picked/dir"}`; `pick/3 :file` delivers the result stored under `:persistent_term` key `{__MODULE__, :file_result}` (default `{:ok, "/fake/picked/file.txt"}`). Set it per test with `set_file_result/1` (`{:ok, path} | :cancelled | :unavailable`): the happy path must point at a REAL temp file (EvoDash.AttachedFile reads it), the error path at a nonexistent one. Always call `Fake.reset()` in on_exit so the persistent_term result never leaks across tests.
- **wx seam fake** (`EvoDash.DirectoryPicker.Wx.Fake`, installed via the `:directory_picker_wx` env): `new_file_dialog/2` returns `{:wx_ref, 1, :wxFileDialog, []}`; `get_path/1` branches on the ref type (`:wxFileDialog` → `"/fake/picked/file.txt"`, `:wxDirDialog` → `"/fake/picked/dir"`). The `set_gate/1` dialog-block is kind-agnostic (busy serialization tests work for both kinds).
- **Exact block shape** (pinned by the happy-path test): `"\n\n---\n## Attached file: <basename>\n\n<content>\n"` appended to the `file_pick` event's `prompt` snapshot — NOT the stale `@task_prompt` (the `file_pick_bases` snapshot is consumed and cleared). `EvoDash.AttachedFile.read/1` trims plain text; a missing file flashes `"Failed to attach file: File not found: <path>"` and pushes `%{error: true}`.
- Remote-node and disabled-config `file_pick` cases push `%{unavailable: true}` synchronously from `handle_event` (no picker involvement); `:cancelled`/`:unavailable` result kinds hit the generic `handle_info` clauses.

## Known Issues

- **`File.mkdir_p/1` returns `:ok | {:error, reason}` — never `{:ok, _}`**: `EvoDashWeb.ProjectsLive.ProjectFlow.create_project/2` must match the plain `:ok` returned by `File.mkdir_p/1`. The projects_live_test cases "creates and activates a new project" (:890) and "creates a fully non-existent nested path recursively" (:922) pin this behavior.

## Constraints
- Follow standard Phoenix test conventions: mirror the `lib/` directory structure under `test/`.
- All connection-based tests must `use EvoDashWeb.ConnCase`.
- Support modules live in `support/` and are compiled automatically by Mix.
- Test file naming: `*_test.exs`.
- Tests requiring an isolated TaskRegistry/TaskStore terminate the production children from `EvoDash.Supervisor` and restart them via `Supervisor.restart_child/2` in `on_exit` to avoid breaking other suites. (These children live in `EvoGit.Application`'s supervision tree; tests reference them via `EvoGit.Store` / `EvoGit.TaskRegistry`.)
- All task persistence in tests goes through `EvoGit.Store` (SQLite-backed) — no other storage formats are used.
- **`try/rescue`/`catch` policy for test files:** While the codebase's design philosophy treats `try/rescue` as an anti-pattern, in test files it is acceptable — and encouraged — in `on_exit`/teardown cleanup paths, because a teardown failure should NOT mask or obscure an actual test failure. Such cleanup rescues must be accompanied by a brief comment documenting why. Rescues that wrap actual test assertions or mask real behavior under test are forbidden; if an assertion would fail, the test must fail loudly.
- **LiveViewTest `render/1` cannot see `:sys.replace_state` mutations** on the LiveView process: the test proxy (ClientProxy) renders from its own cached `html_tree`, updated only via channel diffs — a direct `:sys.replace_state` on the live view pid is never reflected by `render/1`. To test transient states (like a `:testing` spinner), drive the real event path (`render_click`/`render_change`) or send the actual `handle_info` message to `view.pid` — not `:sys.replace_state`.
