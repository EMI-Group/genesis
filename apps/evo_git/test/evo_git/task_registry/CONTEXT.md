# Test Directory — task_registry

## Intent

ExUnit tests for `EvoGit.TaskRegistry` and `EvoGit.Store` persistence/lifecycle behavior: status transitions & guards, lease/heartbeat, cleanup, skip-and-log of undecodable rows, archive metadata, restart durability, the merge/resume/runtime-opts context builders, the `:reflect` task-type routing, and failed-transition diagnostics.

## API Surface

| File | Covers |
|------|--------|
| `persistence_test.exs` (1945 lines) | `set_review_metadata/3`, `TaskInfo` field backfill (`normalize_tasks` on restart), store CRUD, registry restart round-trips (`stop_supervised(EvoGit.TaskRegistry)` + `start_supervised({TaskRegistry, task_store: EvoGit.Store, data_dir: ..., name: EvoGit.TaskRegistry})` — store kept running), recent-project persistence, GenServer resilience, corruption resilience, `archive_metadata`, `describe "status recovery from spurious :failed"` (661), `describe "cross-node PubSub filtering"` (792), `describe "startup reconciliation of orphaned :finalizing tasks"` (843, incl. orphaned `:cancelling` → `:cancelled`), `list_tasks_summary`/`list_task_ids` since-filter semantics, `recheck_task` resolution, recent-projects nil `last_opened_at`, and the graceful `cancel_task`/`force_kill_task` block (1459) — incl. the exactly-one-`:cancelling`-broadcast test (1506) |
| `lease_heartbeat_test.exs` (165 lines) | lease cleared on completion; heartbeat renews only owned leases (never sweeps — even an expired-lease unowned task stays `:running`); one-shot `:lease_sweep` message sweeps expired-lease unowned `:running` tasks → `:failed`; heartbeat renews an owned `:cancelling` task's lease |
| `cleanup_test.exs` (231 lines) | `task_history_config/0` defaults; `cleanup_expired_tasks` age/count limits; never cleans `:running`/`:pending` |
| `store_skip_and_log_test.exs` (272 lines) | `Store.safe_select_all_tasks/1`, `safe_select_all_projects/1`, `safe_select_paginated_tasks/2` skip-and-log semantics: undecodable rows are SKIPPED + `Logger.warning`, never moved (the only tables in `sqlite_master` stay `tasks`/`projects`; bad rows remain untouched in the live table). Injects genuinely-undecodable rows via raw `Xqlite.open`/`XqliteNIF.execute`: malformed JSON `opts` (`[1,2,3]` — raises in `decode_opts/1`) for tasks, INTEGER `last_opened_at` in a pre-created INTEGER-affinity `projects` table (raises in `decode_datetime/1`). No store restart needed — WAL mode lets the store's own connection see raw-injected rows. `with_log` asserts the `"Store: skipping undecodable row in <table> (id: ...)"` warning; negative case (all rows decodable → no warning); paginated `total_count` is SQL-level and includes skipped rows |
| `merge_context_test.exs` (236 lines) | `TaskRegistry.MergeContext` — the merge-conflict-resolution context builder for evolve tasks created with `:merge_from`/`:merge_target`. `apply_merge_context/4` strips the merge keys, overrides `starting_commit` with the previous task's `commit_sha` (preserves the caller's when nil/empty), carries over `foreign_repos` normalized from persisted string-keyed maps to `%ForeignRepo{}` structs (unparseable entries dropped; key absent when the prev task has none), prepends the context block to the objective, passes unrelated caller opts through untouched; missing previous task → stripped opts returned unchanged. `build_merge_context_block/2` content (task id, base/end SHAs, branch, merge target, goal, hints: incremental milestone merges, `git log/diff` both sides, commit before delegating), nil base_sha/branch/target fallbacks, non-TaskInfo input → `""`. Uses `EvoGit.TaskRegistryCase`; the TaskExecutor hook (`execute_task(:evolve, ...)`) is deliberately not tested (would invoke the full Evolution/LLM runtime) |
| `resume_context_test.exs` (215 lines) | `TaskRegistry.ResumeContext` — the resume-context builder for evolve tasks resuming via `:resume_from`. `build_resume_context_block/1` (pure over `%TaskInfo{}`: fully delimited `--- Previous Task Context ---` block with commits `base..end`, objective, and result), `extract_result_summary/1`. `apply_resume_context/3` is deliberately not tested (needs a running TaskRegistry + Store). `async: true` |
| `runtime_opts_test.exs` (303 lines) | `TaskRegistry.RuntimeOpts` — pure runtime-opts builder for genesis/evolve/skill-extraction tasks. `build_common_runtime_opts/3`: minimal opts (repo_path, mode default `:simple`, task_id) for evolve and genesis (genesis requires an explicit `"new"`/`"existing"` mode); optional keys (`node_path`, `starting_commit`, `foreign_repos`, `archive`, `model_id`, `build_system`, `agent`, `model_id_locked`) threaded only when present — empty-string/nil `model_id`/`agent` omitted, `model_id_locked` only when truthy. Mode normalization: `evolution_mode_atom/1`, `genesis_mode_atom/1` (custom → evolve-only error), `mode_atom/2`. `async: true` |
| `task_executor_reflect_test.exs` (144 lines) | The `:reflect` clause of `TaskExecutor` (repo-less self-reflective task type): `execute_task(:reflect, opts, task_id)` tolerates opts WITHOUT `:path` — the reflect clause deliberately skips `RuntimeOpts.build_common_runtime_opts/3` (which `Keyword.fetch!`s `:path`) — and routes into `EvoGit.Runtime.SelfReflective.run`; unexpected opts keys don't crash. End-to-end: `start_task(:reflect)` without `:path` completes `:failed` with `{:error, :llm_not_configured}` under `without_model_profiles/1` (the no-real-LLM idiom from `test/evo_git/runtime/evolution_test.exs`). Uses `EvoGit.TaskRegistryCase` |
| `diagnostics_test.exs` (236 lines) | `TaskRegistry.Diagnostics` — failed-transition diagnostic logging used by `TaskRegistry`. `log_failed_transition/4` (greppable `"TaskRegistry: FAILED_TRANSITION"` prefix + task_id/source/prev_status; `:result` opt; `:extra` fields; `:caller_info` tuple → caller_pid + caller stack), `capture_stacktrace/1`, `format_stacktrace/1`, `format_stacktrace_frame/1`, `format_function/2`, `format_location/1`. Pure Logger/Process.info functions — no GenServer/Store, `async: true` |

## Key Status-Guard Semantics (pinned by `persistence_test.exs:661-790`)

- **PubSub path** (`handle_info({:task_updated, id, status, node})`, lib `task_registry.ex`): updates are applied only when `node == node()` — a cross-node broadcast is ignored (pinned by `describe "cross-node PubSub filtering"`, `persistence_test.exs:792-841`). Stale update rejected ONLY when current status is `[:completed, :cancelled, :cancelling]`. `:failed` is deliberately NOT in the guard — a `:finalizing` broadcast after a spurious `:failed` is ACCEPTED (`persistence_test.exs:698-731`, "recovery before completion": task seeded `:failed`, `Phoenix.PubSub.broadcast(EvoGit.PubSub, "tasks", {:task_updated, task_id, :finalizing, node()})`, assert `status == :finalizing`).
- **Cast path** (`handle_update_status`, lib `task_registry.ex`): rejects only terminal→terminal transitions where `task.status != status` and `status != :completed` — i.e. `:failed` → `:completed` recovery is allowed (`persistence_test.exs:662-696`); `:completed`/`:cancelled` are never overwritten (`persistence_test.exs:733-758`, `760-789`).
- **Terminal set is `[:completed, :failed, :cancelled]`** (cast path) but PubSub guard uses only `[:completed, :cancelled, :cancelling]` — `:failed` is recoverable in both paths.
- Runtime emits ONLY `:finalizing` on the `"tasks"` topic (`Helpers.notify_finalizing/1` → `Phoenix.PubSub.broadcast(EvoGit.PubSub, "tasks", {:task_updated, task_id, :finalizing, node()})`, lib `runtime/helpers.ex`); a `:failed` received on PubSub triggers `Diagnostics.log_failed_transition(..., :task_updated_pubsub, ...)`.

## Known Test Gaps

1. **No restart-recovery test for an unowned expired-lease `:running` row**: the `:finalizing` case IS covered by `describe "startup reconciliation of orphaned :finalizing tasks"` (`persistence_test.exs:843-944`): seeds a `:finalizing` row + a `:running` control row (both with future leases), restarts the registry, and asserts the orphaned `:finalizing` row → `:failed` (finished_at set, lease cleared, result a binary — init-time reconciliation via `Store.select_running_lease_info/1` + `handle_update_status/6`) while the `:running` row with a valid lease is left untouched (orphaned-`:running` sweeping is the `:lease_sweep` path). Untested: a fresh registry against a `:running` row with a valid lease leaves it untouched (only pinned as the startup-reconciliation control row), and expired-lease unowned `:running` rows get swept only by the one-shot sweep 150s after init (or `{:recheck_task, id}` resolution). The restart/backfill tests (`persistence_test.exs:184-249`, `251-277`, `104-183`, `537-659`) only use `:completed` tasks. Closest analogues:
   - `lease_heartbeat_test.exs:76-108` — seeds an expired-lease unowned `:running` row and sends `:lease_sweep` DIRECTLY (no restart); simulates the startup sweep, which fires once at `@sweep_after` (lib:42, `(@lease_duration + 30) * 1000`; one-shot `Process.send_after` at lib:300) — never during a test run — and asserts `:failed`.
   - `store_test.exs:1459-1484` — status field round-trips as atom for all known values incl. `:finalizing` (Store only, no registry).
   - evo_dash `tasks_live_test.exs:227` — LiveView `:finalizing` broadcast handling (not registry restart recovery).
2. **`:failed` → `:finalizing` via the CAST path** (`update_task_status/4`) is untested — only the PubSub path is covered (`persistence_test.exs:698-731`).

### Known lib quirk (verified, tests pin the reachable semantics)
`TaskRegistry.resolve_recheck_task/3`'s `{:ok, %{branch_name: _}}` result-extraction branch is **unreachable at runtime** — `handle_info({:recheck_task, _})` gates on `Lease.sched_meta_has_active_agents?/1` (true when ANY ETS entry exists for the task_id → reschedules), while `Lease.lookup_sched_meta_result/1` (which supplies the result) REQUIRES an entry with `parent_id == nil` to exist. Mutually exclusive → the only reachable recheck outcome is `{:completed, nil}` (warning logged); the handler is unreachable (nothing schedules the first recheck).

## Harness — `test/support/task_registry_case.ex` (82 lines)

- 6 test modules use `EvoGit.TaskRegistryCase, async: false` (persistence, lease_heartbeat, cleanup, store_skip_and_log, merge_context, task_executor_reflect); the pure modules (diagnostics, resume_context, runtime_opts) use plain `ExUnit.Case, async: true`.
- `setup`: `Supervisor.terminate_child(EvoGit.Supervisor, EvoGit.TaskRegistry)` + `(..., EvoGit.Store)` (kills production children so they don't auto-restart); creates a unique temp dir `System.tmp_dir!()/evogit_test_tasks_<unique>`; starts a FRESH `EvoGit.Store` (`start_supervised({EvoGit.Store, data_dir: sqlite_path})`) and FRESH `TaskRegistry` (`start_supervised({TaskRegistry, task_store: EvoGit.Store, data_dir: root, name: EvoGit.TaskRegistry})`) — fresh empty SQLite DB per test.
- `on_exit`: `File.rm_rf(root)` + `Supervisor.restart_child` of Store and TaskRegistry.
- Helpers: `trigger_cleanup!/0` (direct `Cleanup.cleanup_expired_tasks(EvoGit.Store)`), `cleanup_process/1`, `old_age_days/0` / `within_age_days/0` (read runtime config, fallback 14).
- NO `XDG_DATA_HOME` redirection here (unlike evo_dash's test_helper) — isolation comes entirely from the terminate-child + fresh-temp-DB pattern. (Note: root CONTEXT.md flags `config/test.exs` sets `:evo_dash, :data_dir` while `EvoGit.Store` reads `:evo_git, :data_dir` — the app-level guard is broken; only the per-test isolation pattern protects the production DB.)

## Constraints

- `async: false` via `EvoGit.TaskRegistryCase` for the Store/registry-touching files; `async: true` for the pure modules (diagnostics, resume_context, runtime_opts).
- No mocking — real SQLite via xqlite, real `EvoGit.Store`/`TaskRegistry` GenServers; corrupt rows injected via raw `XqliteNIF.execute`.
- Sync idiom: `update_task_status` is a cast → always `TaskRegistry.list_tasks()` (a call) afterwards to flush the mailbox before asserting.
- Restart pattern: `stop_supervised(EvoGit.TaskRegistry)` then `start_supervised({TaskRegistry, task_store: EvoGit.Store, data_dir: data_dir, name: EvoGit.TaskRegistry})` — keep the SAME Store running (it is durable on disk) so data survives the registry restart.
