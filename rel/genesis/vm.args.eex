## Customize flags given to the VM: https://www.erlang.org/doc/man/erl.html
## -mode/-name/-sname/-setcookie are configured via env vars, do not set them here

## -----------------------------------------------------------------------------
## Desktop Optimization Flags
## -----------------------------------------------------------------------------

## Disable Scheduler Busy-Waiting (Saves battery and background CPU)
+sbwt none
+sbwtdcpu none
+sbwtdio none

## Shrink Pre-allocated Table Limits (Lowers baseline idle memory floor)
+P 40000
+t 80000

## Disable the Erlang Port Mapper Daemon (Prevents lingering background processes)
## EPMD is disabled by default. When SSH remote development is enabled, the
## connection is established via EPMD-less distribution over an SSH tunnel with
## a pinned port, so EPMD is still not needed.
-start_epmd false
-epmd_module Elixir.EvoGit.EpmdDist
