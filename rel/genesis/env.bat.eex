@echo off
REM Enable Erlang distribution for SSH remote development when opted in.
if "%GENESIS_REMOTE_DEV%"=="1" (
  set RELEASE_DISTRIBUTION=name
  if "%GENESIS_NODE_NAME%"=="" set RELEASE_NODE=genesis@127.0.0.1
  if "%RELEASE_COOKIE%"=="" set RELEASE_COOKIE=genesis_remote_cookie
)
