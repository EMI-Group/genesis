## Customize flags given to the VM: https://www.erlang.org/doc/man/erl.html
## -mode/-name/-sname/-setcookie are configured via env vars, do not set them here

## Increase number of concurrent ports/sockets
##+Q 65536

## Tweak GC to run more often
##-env ERL_FULLSWEEP_AFTER 10

<%= if @release.name == :genesis_remote do %>
## Remote Daemon — EPMD-less distribution over pinned port (for SSH tunneling)
-start_epmd false
-erl_epmd_port 9000
-dist_listen true
-kernel inet_dist_listen_min 9000 inet_dist_listen_max 9000
<% else %>
## Local Dashboard — EPMD-less distribution for SSH tunneling to remote
## The dashboard only initiates outgoing connections (Node.connect)
## and never accepts incoming distribution connections.
-start_epmd false
-dist_listen false
-kernel inet_dist_listen_min 9000 inet_dist_listen_max 9000
<% end %>

## -----------------------------------------------------------------------------
## Desktop Optimization Flags
## -----------------------------------------------------------------------------

## Disable Scheduler Busy-Waiting (Saves battery and background CPU)
+sbwt none
+sbwtdcpu none
+sbwtdio none

## Shrink Pre-allocated Table Limits (Lowers baseline idle memory floor)
+P 40000
+t 80000
